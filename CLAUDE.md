## 하네스: PropAI 부동산개발 플랫폼

**목표:** 부동산개발 전주기 AI 자동화 플랫폼의 개발 작업을 전문 에이전트 팀이 협업하여 수행

**트리거:** PropAI 관련 개발 작업(모듈 구현, STEP 구현, API 구현, UI 구현, AI 서비스 개발) 요청 시 `propai-orchestrator` 스킬을 사용하라. 단순 질문은 직접 응답 가능.

## 버그수정 기본정책 (기록 + 전역 전파방지) — 필수

오류·버그·개선을 수정할 때마다 **항상** 두 가지를 함께 수행한다(단발 국소 패치 금지):

1. **기록·저장·공유**: 증상·근본원인·수정과정·라이브검증 결과를 남긴다(커밋 메시지 + 세션 메모리 / `_workspace`).
2. **전역 전파방지(★핵심)**: 버그의 **패턴**을 일반화해, 같은 문제가 **다른 페이지·기능·분석폼·엔드포인트에서도 재발하는지 플랫폼 전역을 스윕**하고 발견한 진짜 오점을 함께 고친다. 수정은 **공용 함수/헬퍼·표준 계약으로 추출**해 한 곳을 고치면 전역이 따라오게 한다.

수정 워크플로우: 근본원인 확정(라이브 그라운드 트루스) → **정답 기준선**(올바르게 동작하는 경로)과의 격차로 패턴 정의 → 공용화 수정 → 전역 스윕(진짜/오탐 트리아지, 대규모는 병렬 오케스트레이션) → 라이브검증 → 기록. (예: 2026-06-19 산/임야 용적률 과대표시 버그 = 90초진단 카드뿐 아니라 `land_report` PDF에도 동일 결함 → 공용헬퍼 `_enrich_effective_and_special`(실효 `calc_effective_far`+`detect_special_parcel`)로 일원화.) 무목업·완결 게이트·실시간 기록공유와 함께 적용.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-05-27 | 초기 구성 | 전체 | 하네스 신규 구축 |
| 2026-05-27 | Mock→Live 전환 | api-client.ts | 프론트엔드 기본 모드를 Live로 변경 |
| 2026-05-27 | Tier1 WorkspaceClient 7개 신규 | Legal, Construction, ESG, SiteAnalysis, Permit, CostAnalytics, InvestmentAnalytics | ModulePlaceholder→실제 API 연동 |
| 2026-05-27 | Tier2 WorkspaceClient 6개 신규 | Permits, Regulations, Tenant, Safety, MarketInsights, Approvals | 운영 모듈 프로덕션 전환 |
| 2026-05-27 | 백엔드 스텁 보강 | drone_iot, regulation_monitor, energy, alris_service | Fallback 구조화, 법규 RAG 20개 확장 |
| 2026-05-27 | 13개 페이지 업데이트 | page.tsx 13개 파일 | WorkspaceClient 연동 |
| 2026-05-27 | Stage1: 모세혈관 네트워크 | useProjectContextStore, ProjectLifecyclePipeline | 모듈간 데이터 자동 흐름, 10단계 라이프사이클 |
| 2026-05-27 | Stage2: CAD 고도화 | CadCompliancePanel, CadBimSidePanel, CadExportPanel, design-to-cad-converter | 법규검증, BIM연동, 도면내보내기 |
| 2026-05-27 | Stage3: 디자인 퀄리티 | tokens.css, globals.css, SidebarNav, AnimatedCounter, TiltCard, GridBackground | 색상통일, 타이포강화, 동적UI |
| 2026-05-27 | Stage4: 미구현 보완 | ReportPdfDownload, AiTokenUsage, WebhookMgmt, OnboardingWizard | 기획서 갭 해소 |
| 2026-05-27 | Critical 계산오류 수정 | tax(취득세/양도세/종부세), finance(복합이자), revenue(임대수입), legal(건폐율), energy(BEEC), vworld(좌표계) | 7건 Critical 버그 수정 |
| 2026-05-27 | 공공데이터 실시간참조 기반구조 | validator.py, public_data_registry.py, calculation_metadata.py, data_integrity router | 할루시네이션 방지, 데이터 신선도 검증, 계산 메타데이터 |
| 2026-05-27 | Innovation1: 유닛믹스 최적화 | unit_mix_optimizer.py, UnitMixOptimizerPanel.tsx | SLSQP 기반 수익극대화 평형배분 (ArkDesign 벤치마크) |
| 2026-05-27 | Innovation2: 대화형 시장분석 AI | conversational_market_ai.py, ConversationalMarketPanel.tsx | 자연어→실거래 조회→차트 생성 (Deepblocks ChatDB 벤치마크) |
| 2026-05-27 | Innovation3: 자동 용도지역 감지 | auto_zoning_service.py, AutoZoningBadge.tsx | 주소→PNU→용도지역→법규한도 자동매핑 |
| 2026-05-27 | Innovation4: 은행제출용 보고서 | bank_ready_report_service.py, BankReadyReportBuilder.tsx | PF대출 심사용 10섹션 통합보고서 (Feasibly 벤치마크) |
| 2026-05-27 | Innovation5: GRESB ESG 스코어링 | gresb_scoring_service.py, GresbScoreCard.tsx | GRESB 2025 기준 자동 ESG 점수 산출 |
| 2026-05-28 | 킬러기능: Top3 자동추천 파이프라인 | permit_validator.py, auto_recommend_top3(), AutoRecommendPanel.tsx, BusinessModelRefineModal.tsx | 주소입력→15모델시뮬→인허가검증→Top3추천→수정→완성 |
| 2026-05-31 | 나라장터(G2B) 6엔진 연동 | bid_analyzer, schemas, routers, 프론트 모달 | 입찰 추정가격→QTO/수지/용도지역/인허가/ESG/시장 정밀분석 + 적정투찰가 |
| 2026-06-14 | 멀티세션 협업체계 수립 | WORKTREES.md, coordination/, scripts/ | 세션 간 브랜치 충돌 방지(워크트리 격리)+조율 보드 |

## 검증 규율 (필수 — 반복 결함의 근본 하나를 막는다)

### ★결론 직전 한 문장 (이 절만은 외워라)

> **"내가 방금 잰 것이, 사용자가 실제로 쓰는 그것인가?"**

★**왜 규칙을 더 늘리지 않고 한 문장으로 압축했나**: 아래 표와 20개 항목, 그리고 볼트의
동일 주제 문서 4개(711줄)가 **이미 있는데도** 2026-08-17 한 세션에서 같은 근본으로
**10종이 재발했다**(그 문서를 인용하면서 저지른 것도 있다). 재발 지점을 세어 보니
**전부 "결론을 내리는 순간"** 이었다 — 규율은 세션 초반에 읽히고 오류는 중반 결론에서 난다.
규칙이 많을수록 그 순간에 무엇을 볼지 모른다. 그래서 **하나만** 반사적으로 던진다.

그 10종 중 8종이 이 질문 하나에 걸린다. 실제 사례:

| 내가 잰 것 | 사용자가 쓰는 것 |
|---|---|
| 호스트 파일시스템의 소스 `grep` | **실행 중인 컨테이너 안** |
| 기본 SSH 키로 접속 실패 → "키 미등록" 결론 | 실제 키는 **`~/.oci.key`** |
| 트래픽 없는 compose 컨테이너 | **caddy 가 가리키는 실서비스** |
| `git worktree list` 의 **fetch 전** SHA | fetch 후 원격 |
| `pgrep -f safe-deploy` (**자기매칭**) | `safe[-]deploy` |
| 정규식이 **주석**을 긁음 | 실행 라인 |
| `exit 7` 을 **파일 전체**에서 탐색 | 가드 블록 안 |

★짝이 되는 한 줄(**대조군 없는 "0건"은 근거가 아니다**)은 아래 **§지켜야 할 것 8** 에 있다.
거기에는 강제 도구(`tests/_scan_guard.py`)까지 있으므로 **여기 중복해 적지 않는다** —
같은 규율을 두 곳에 두는 것이 바로 이 절이 경고하는 그 문제다.

---

이 저장소에서 같은 형태의 결함이 계속 새어 나갔다. 근본은 **하나**다:
**검증이 실제 대상을 태우지 않는다.** 아래는 실제로 뚫린 형태들이다.

| 형태 | 실제 사례 |
|---|---|
| 정의만 하고 **소비처 0** | TTL 함수는 잠갔는데 수집부가 안 씀(고정값으로 되돌려도 통과) |
| 스텁이 **실제 층 우회** | 파서 필드를 지워도 통과(테스트가 스텁 사용) |
| 픽스처가 **두 모집단을 안 가름** | 지분/일반이 같은 값이면 배선을 끊어도 결과가 같다 |
| 단언이 **원리적으로 위반 불가** | `mustContain` 이 `scope` 에 함의됨(공허한 참) |
| 소스 검사가 **주석·문자열에 뚫림** | 여러 줄 JSX 주석으로 렌더를 감싸면 배선 락이 초록 |
| **봉합이 새 결함을 만듦** | 순서 의존을 고치려던 승격이 "존재하지 않는 주소"를 합성 |

### 지켜야 할 것

1. **분기·필드·상수를 만들면 테스트는 같은 커밋에.** 나중은 오지 않는다.
2. **픽스처는 두 모집단을 갈라야 한다.** 필드가 두 집합의 차를 말하면, 그 둘이 실제로
   **다른 값**을 내야 한다. 차가 0인 픽스처는 잠금이 아니다.
3. **스텁이 검증 대상 층을 우회하는지 확인하라.** 우회한다면 그 층을 직접 태우는 테스트를
   따로 둔다(파서·직렬화·네트워크 경계).
4. **소스 검사는 주석·문자열을 배제하고** 실행되는 줄만 본다.
5. **변이를 직접 고르지 말고 `scripts/mutate_changed.py` 를 돌려라.**
   사람이 고른 변이는 사람이 못 본 층을 비껴간다 — 이 도구는 diff 에서 기계적으로 뽑는다.

       python3 scripts/mutate_changed.py --tests <테스트 경로>

   ★2026-08-07 — **부분 해소.** `#586` 이후 **러너는** 테스트 확장자로 고르고
   (`.ts/.tsx` → vitest), 변경 파일 수집도 `.ts/.tsx` 를 포함한다. 남은 한계는 **탐색**이다:
     · `_guess_tests` 는 `tests/test_*.py` 만 본다 → 프론트는 자동으로 짝을 못 찾는다
     · 그때는 `★테스트를 찾지 못했다` 를 찍고 **EXIT=2 로 시끄럽게 실패**한다(조용한 초록 아님)
     · 따라서 프론트는 **`--tests <vitest 경로>` 를 직접 줘야** 감사된다
   ★★2026-08-15 정정 — **이 한계는 프론트 전용이 아니었다.** 위 서술이 그렇게 읽혀서
   "백엔드는 자동탐색이 된다"고 오독하게 만들었다. 실제로는 서비스 모듈이 `_service` 접미를
   다는데 테스트 파일명은 그 접미를 떼는 이 저장소 규칙 때문에
   (`parcel_rights_survey_service.py` ↔ `test_parcel_rights_survey.py`)
   **`app/services/**` 상당수가 백엔드에서도 자동탐색 0건**이었다(실측 — #633 의 변경 파이썬
   파일 전부 MISS). 그래서 그 PR 의 "변이 CAUGHT ×4"는 **손수 고른 변이**였다.
   → `_guess_tests` 에 `_service` 접미 제거 폴백을 넣어 그 계열을 회복했다.
   ★그래도 **완전 탐색이 아니다** — 테스트명이 모듈명과 무관하면 여전히 MISS 다
   (실측: `registry_analysis_service`). **자동탐색이 비면 `--tests` 를 명시하라.**
   ★"도구를 돌렸다"가 프론트 보증이 되려면 **`--tests` 를 준 실행**이어야 한다. 그냥 돌려서
   EXIT=2 를 보고 "대상 없음"으로 넘기면 감사되지 않은 것이다 — 실제로 그 착각으로 손수
   변이 2개만 골랐다가 **설명 불가한 생존 3건**을 놓쳤다(#588 R1).
   이 한계를 모르면 "전수 감사했다"고 착각하게 된다 —
   실제로 백엔드만 감사하고 "생존 0"이라 선언한 적이 있다(2026-08-06).

   ★**생존 0이 목표가 아니다.** 이중 가드·도달 불가 방어라면 **그 사실을 코드에 적는다**
   (변이 점수 부풀리기 방지). **설명할 수 없는 생존**만 진짜 구멍이다.
6. **고친 자리의 형제·미러를 반드시 스윕한다**(백엔드만 고치고 프론트 미러를 놓치는 일이
   반복됐다). 위 "버그수정 기본정책"의 전역 전파방지와 같은 규율이다.
7. **재보지 않은 것을 단정하지 마라.** "비현실적/충분하다/가능하다"는 측정 없이는 추정이다.
   비용은 축이 여럿이다 — **속도만 재고 쿼터를 안 재면 틀린다**(실제로 판단이 두 번 뒤집혔다).
8. **대조군 없는 "0건"은 근거가 아니다.** 2026-08-16~17 한 세션에서 **위양성 7건**이 났고,
   **"위반 0"이라는 결과는 매번 같았다** — 갈라 준 것은 오직 대조군이었다. 둘은 정상 배포를
   실패로 찍을 뻔했고, 하나는 **1,200원을 헛쓸 뻔**했다.

   ★가장 자주 틀리는 상대는 코드가 아니라 **자기가 방금 쓴 텍스트**다. 7건 중 3건이 그랬다:
   `매도청구 가능` 이 **`매도청구 가능여부`**(다른 기능의 정당 라벨)를, `propai-v` 가
   **`propai-vitest`**(로그 파일명)를, 그리고 **주석에 적은 예시값**을 상수 선언으로 집었다.
   **주석에 예시를 적으면 그 예시가 다음 검사의 위양성이 된다.**

   그래서 도구로 강제한다 — `propai-platform/tests/_scan_guard.py`:

       assert_absent(text, pattern=…, positive_control=…, reason=…)

   `positive_control`·`reason` 이 **필수 키워드 인자**라 대조군 없는 호출이 문법적으로 불가능하다.
   **대조군 파괴는 `ScannerDeadError`, 진짜 위반은 `AssertionError`** — 다른 예외로 던진다
   (뭉치면 "검사기가 죽었다"가 "깨끗하다"로 읽힌다). 주석은 `code_lines()` 로 걷어내고 검사한다.

   손으로 조회할 때의 최소 규율: **패턴이 아니라 선언을 본다**(`grep '^const X ='`),
   **경계를 준다**(`가능(?!여부)`), **기대값도 코드에서 파생시킨다**(계획서 문장에서 추론하면
   그 자체가 위양성이 된다 — 실제로 정상 배포를 실패로 찍을 뻔했다).
9. **`cmd | head`·`| tail` 은 파이프 끝의 종료코드를 준다.** 실패 경로를 확인하면서
   `EXIT=0` 을 읽고 "가드가 안 죽는다"고 결론낼 뻔했다(실제로는 1). **파일로 받고 `$?` 를 읽거나
   `PIPESTATUS` 를 써라.** 이 저장소는 같은 함정으로 `pytest | tail` 이 exit 0 인데 실제로는
   37 수집오류였던 전례가 있다.

## 멀티세션 협업 (필수 — 여러 Claude 세션이 동시 작업)

이 저장소는 **여러 Claude 세션이 동시에** 개발한다. 충돌(특히 같은 워크트리에서 브랜치 전환→HEAD 충돌→엉뚱한 브랜치 커밋)을 막기 위해 **세션 시작 시 반드시**:

1. **공유 보드를 읽는다**: `scripts/coord.sh status` (보드는 우리 저장소의 공유 git 디렉토리 `.git/coordination/BOARD.md` — 모든 워크트리가 같은 한 부를 보고 이 저장소에만 스코프). 누가 어느 브랜치·영역을 작업 중인지 파악.
2. **브랜치당 전용 워크트리에서만 작업한다.** 공유 메인(`Development_AI/`)에서 feature 브랜치 `git checkout` 금지. 전용 워크트리 생성: `scripts/new-worktree.sh <branch>`. (git이 동일 브랜치 이중 checkout을 거부하므로, 한 번 전용 워크트리에 두면 충돌이 구조적으로 불가능.) 상세: `WORKTREES.md`.
3. **공유 파일(예: `main.py` 라우터 등록) 편집 전 claim**: `scripts/coord.sh claim <영역>` → 완료 후 `release`. 진행/완료는 `scripts/coord.sh note <내용>`.
4. **커밋 전 `git branch --show-current`로 자기 브랜치 확인.** main 직접 푸시 금지.
5. 명시적 인계는 `mcp__ccd_session_mgmt__send_message`(사용자 확인). 전체 규약: `coordination/PROTOCOL.md`.

## 회귀망·검증 규율 (필수 — 이 저장소가 반복해서 데인 오류 클래스)

버그를 고칠 때마다 **같은 실수가 반복됐다**. 아래는 실제로 여러 번 재발한 것만 추린 체크리스트다.
테스트·가드·계약을 쓰거나 고칠 때 **매번** 확인한다.

### A. 회귀망을 쓸 때 (가장 많이 재발)

1. **검사 대상이 실제로 DOM/실행 경로에 있는가.** "검사는 있는데 대상이 없어" 통과하는 공허한
   그린이 4회 이상 났다(팝오버가 닫혀 있음 · 테스트 스텁이 슬롯을 버림 · 상세 패널을 안 엶 ·
   조회 완료 상태를 안 만듦). **조건부 렌더 요소는 그 상태를 만들어서** 검사하라.
2. **공허 진리 가드를 단언 *앞에* 둔다.** "위반 0"이 참이 되는 이유가 "대상이 0개"이면 무의미하다.
   개수 하한·대상 존재를 먼저 단언한다.
3. **소스 grep 대신 렌더 결과를 본다.** 소스 검사는 **주석 처리 + 임포트 유지** 변이에 뚫린다
   (2회 실증). 렌더가 불가하면 `propai-platform/apps/web/lib/source-invariant.ts` 의 주석 스트립을 경유한다.
4. **목록형이 아니라 전수/파생형으로 쓴다.** 사람이 센 목록이 곧 상한이 된다(목록 5 vs 실제 11).
   대상을 코드에서 **파생**시키면 새 항목이 자동으로 감시망에 들어온다.
5. **새 계약 상수를 만들었으면 그 상수에 결속시킨다.** `> 600` 같은 대역만 보면 상수가 장식이
   되고 커밋이 선언한 불변식이 무잠금으로 남는다 → `toBe(상수)`.
6. **가드의 위양성도 결함이다.** 하한을 *넘는* 등가 표기(`size-12`·`min-h-36`)를 위반으로 신고하면
   정상 코드를 막는다. 허용 표기를 전부 열거하라(2회 재발).

### B. 변이 검증을 할 때

7. **커밋 먼저 → 변이 → 원복.** 미커밋 상태 `git checkout --` 는 **내 편집을 날리고**, 그 뒤 얻은
   변이 결과는 오염된다(실제로 SURVIVED 를 CAUGHT 로 읽을 뻔했다).
8. **"생존" 보고 전에 주입 성공을 확인한다.** `grep -c` 는 동명의 다른 코드를 세므로 **해당 라인**을
   직접 본다(주입 실패 2회를 이걸로 적발).
9. **새 케이스를 추가했으면 변이로 CAUGHT 를 확인하고 넘어간다.** 케이스를 추가한 것만으로는
   그 케이스가 무엇을 보는지 알 수 없다.

### C. 주석·문서를 쓸 때

10. **주석에 쓴 근거도 검증 대상이다.** "본문 전체 위" · "가장 작았다" · "hover 가 끊긴다" 같은
    단정이 실측과 어긋난 사례가 다수다. 다음 사람이 그 문장을 근거로 판단한다.
11. **면역을 거짓 주장하지 마라.** "주석에 속지 않도록 찾는다"고 써 놓고 정규식에 주석 인식이
    없었다. 코드가 그 면역을 갖고 있는지 확인하고 쓴다.
12. **"무잠금(고쳤으나 미검증)"과 "미수정"을 섞어 쓰지 마라.** 후자를 전자로 표기하면 리뷰어가
    이미 안전하다고 오독한다.
13. **부채는 `it.todo` 로 초록 안에 보이게** 남긴다(커밋 메시지에만 적으면 드러나지 않는다).

### D. 판단·범위

14. **"위험해서 미룬다"의 위험을 실측하라.** 미룸의 근거가 **호출되지 않는 코드 경로**였던 적이 있다.
15. **리뷰가 "범위가 틀렸다"고 하면 주석으로 범위를 사후 확정하지 마라** — 지적 무력화다.
16. **PR 이 선언한 원칙을 그 PR 안의 신규 코드에 스스로 적용했는지** 확인한다(고친다고 선언한
    결함 클래스를 같은 PR 의 새 부분에서 재발시킨 적이 있다).
17. **전역 값(층위·계약·공용 상수)을 바꿨으면 수정 후 전역 스윕은 절차다** — 그 스윕이 "내가 방금
    만든 회귀"를 잡는다.
18. **z 결함은 좌표가 아니라 페인트 순서로 판정한다**(`rect` 교차가 아니라 `elementFromPoint`).
19. **경계를 걸면 양방향으로 걸어라.** 상한만 걸었더니 하한이 0으로 붕괴해 **프로덕션에서 대화
    영역이 0px** 이 됐다(`max-h` 만 있고 `min-h` 가 없었다). 상한·하한, 최소·최대, 시작·종료는
    한 쌍이다 — 한쪽만 거는 순간 반대쪽이 무제한이 된다.
20. **처방을 적용한 범위 = 결함이 사는 범위인지 확인하라.** "판정을 파서에게 넘겼다"고 선언해
    놓고 **같은 함수의 다른 절반**(줄 주석)은 손수 정규식으로 남겨, 그 절반으로 배선 락 2개가
    관통됐다. 매체·언어·강제 지점·**적용 범위**를 각각 대조한다.

### E. 배포

21. **여러 PR 로 나눈 계약 변경은 중간 상태가 회귀인지 확인하고 묶어서 배포**한다.
22. **sw(캐시) 범프는 머지와 경합한다.** 범프가 기능 PR 보다 먼저 머지되면 그 기능은 캐시로 가려진다
    — 배포 전 `<범프커밋>..origin/main` 에 프론트 변경이 남았는지 실측한다.

### F. 워크트리·산출물 (2026-08-06 추가 — 실제로 어겼다)

23. **편집한 워크트리와 커밋하는 워크트리가 같은지 확인하라.** 공유 메인(`Development_AI/`)에서
    파일을 고치고 다른 워크트리에서 커밋하면 **그 변경은 PR 에 들어가지 않는다** — 지침 56줄을
    공유 메인에 미커밋으로 방치한 사고가 실제로 났다(머지해도 전달 안 됨 + 다른 세션의
    checkout/stash 로 소실 위험 + 테스트가 인용한 문서가 브랜치에 없는 매달린 참조).
    **커밋 후 `git show --stat` 으로 선언한 산출물이 실제로 들어갔는지 확인**한다.
24. **커밋 메시지가 "했다"고 선언한 것을 그 커밋이 실제로 담았는지 확인하라.** 선언과 산출물이
    갈리면 리뷰어·다음 세션이 이미 된 것으로 오독한다. ★"소스에서 그 수치를 걷어냈다"고 썼는데
    **테스트 파일에는 남아 있던** 사고가 실제로 났다 — 선언은 `git grep` 으로 전수 확인한다.
25. **git 만이 아니다 — 내가 실행한 모든 외부 변경 명령은 산출물로 재확인하라.** `gh pr edit` 가
    **조용히 실패**(Projects GraphQL 오류)해 PR 본문이 안 바뀐 채 "수정했다"고 보고할 뻔했다.
    `gh`·배포 스크립트·파일 쓰기 전부 해당 — 종료코드가 0이어도 **결과물을 다시 읽는다**.

### G. 조회·기록 (2026-08-17 추가 — 한 세션에서 13회 실측됐다)

A~F 를 다 지켜도 **재는 행위 자체가 틀리면** 아무 소용이 없다. 아래는 2026-08-16~17 한 세션에서
**실제로 센 횟수**다: 잘못된 조회 4회 · 성공신호 위장 6회 · 기록 부패 3회.

> ★**§검증 규율 8·9 와의 관계**(중복 제거 — 다른 세션이 같은 날 먼저 넣었다):
> **8 번은 위양성**(있지도 않은 위반을 신고)을, **9 번은 파이프 종료코드**를 다룬다. 아래 26·27 은
> 그 **반대편**만 적는다 — 26 은 **위음성**(있는데 못 찾음: 대상·범위가 틀림), 27 은 파이프 **밖**의
> 무성 실패다. 겹치는 내용은 8·9 를 보라. *(두 세션이 같은 날 독립적으로 같은 교훈에 도달했다 —
> 그 자체가 이 결함 클래스의 빈도를 말한다.)*

26. **"0건"이 위음성일 때 — 패턴이 아니라 *대상과 범위* 가 틀렸다.** (위양성은 §검증 규율 8)
    실측 3종: ①**존재하지 않는 대상**을 조회(`propai.4t8t.net` — 실제는 `4t8t.net`)해 "사이트 죽음"으로
    읽을 뻔 ②**라우트만** 훑고 "전 구간 미배선" 결론 직전 ③패턴이 마크다운 **볼드 마커를 가로질러** 0건.
    → **반드시 있어야 할 것을 같은 방법으로 조회해(대조군) 조회기 생존부터 증명한다.**
    ★★**"라우트 0건"은 "진입점 0건"이 아니다.** 진입점은 라우트·celery·CLI·워커·스크립트다.
    한 층만 보고 "미배선"이라 부르면 살아 있는 코드를 죽었다고 보고하게 된다.

27. **파이프 밖에도 무성 실패가 있다 — "실패도 이 필터에 걸리는가"를 먼저 물어라.**
    (파이프 종료코드는 §검증 규율 9) 실측 4종: `gh pr edit` 이 Projects GraphQL 로 **무성 실패**
    (→ `gh api -X PATCH` + 사후 확인) · `coord.sh note` 인자의 **백틱이 호출 셸에서 치환**돼 문구가
    소실(종료코드 0·"noted." 정상 출력 → 변수에 담아 전달하고 쓴 뒤 `tail` 로 재확인) ·
    `printf '%s|%s' a b c` 는 포맷을 **재사용**해 한 줄이 두 줄로 쪼개진다 ·
    ★**내가 새로 짠 감시 스크립트**가 빈 `conclusion` 을 "완료"로 세어 **거짓 완료**를 냈다 —
    **검증 도구를 만드는 순간 그 도구 자체가 이 함정의 새 서식지가 된다.**

28. **휘발성 상태를 기록에 값으로 적지 마라 — 재측정 방법을 적어라.**
    라이브 배포·캐시명·HEAD·PR 상태는 **분 단위로** 바뀐다. 실측: `"158 에 v506 미배포"` 를 6자리에
    현재 사실처럼 적었다가 뒤집혔고, **그것을 고치는 정정문이 42분 만에 또 썩었고**, 정정 PR 은
    **머지되기도 전에** 거짓이 됐다(3중 재감염). **정정문이 새 부패의 운반체가 된다.**

    ```bash
    curl -s https://4t8t.net/sw.js | grep -m1 CACHE_NAME   # 끝의 sha = 배포된 커밋
    curl -s https://api.4t8t.net/health
    curl -s -o /dev/null -w '%{http_code}\n' https://4t8t.net/zzz-nope   # 대조군(404)
    ```

    ★**모든 값을 지우라는 뜻이 아니다.** 불변 사실(머지 해시·측정 기준 커밋)은 값이 정답이다.
    **휘발성만** 재측정으로 돌린다 — 구분하지 않으면 문서가 쓸모없어진다.

29. **"이 기능이 없다"고 결론 내리기 전에 형제를 훑어라.** 실측 2회: `PUT /admin/config` 에 감사가
    없다고 판단했는데 **같은 라우터의 `set-tier` 는 이미 `audit_admin_action` 으로 감사 중**이었다
    (없던 게 아니라 **불일치**) · `apply_config` 의 요율 위생도 **옳은 패턴이 바로 옆 루프에** 있었다.
    → **없는 것을 새로 만드는 것과, 있는 것을 안 쓴 것은 처방이 다르다.** 형제를 먼저 보면
    내 구현이 표준 통로의 **부분집합**이 되는 것도 막는다(실제로 그럴 뻔했다).

30. **커밋·주석에 쓴 "…한다"는 동작 주장은 그 자체가 검증 대상이다.**
    *"어느 키가 왜 거부됐는지 남긴다(무언 실패 금지)"* 라고 써 놓고 그것을 지키는 단언이 없어,
    해당 줄을 지워도 초록이었다 — **변이 도구가 그 자리를 정확히 짚었다**(생존 5건이 전부 그 라벨).
    → 주장을 썼으면 **그 주장에 변이를 넣어라.** ★단 **산문(경고 문구 자체)까지 단언하지는 마라** —
    계약이 아니라 표현이라, 다듬을 때마다 깨지는 취약한 락이 된다. 그때는 **점수용 단언 대신
    "왜 이 생존은 구멍이 아닌지"를 코드에 적는다**(도구가 그렇게 지시한다).

31. **목록을 뭉뚱그리면 그 안의 급한 항목이 같이 묻힌다.** 인계서의 **별도 「미확정」 절**(실제 돈이
    걸린 25배 과금 괴리)을 *"부채는 착수 권하지 않음"* 으로 **묶어서** 넘겼다. 절이 다르면 판단도
    다르게 하라 — 특히 **돈·보안·사용자 가시성**이 걸린 항목은 목록 밖으로 꺼낸다.
