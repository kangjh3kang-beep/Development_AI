[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "propai-schemas"
version = "0.1.0"
description = "PropAI 공유 Pydantic 스키마 패키지"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0",
]

[tool.hatch.build.targets.wheel]
packages = ["."]
