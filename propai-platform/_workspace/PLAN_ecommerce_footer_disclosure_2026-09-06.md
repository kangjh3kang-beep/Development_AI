# 전자상거래법 표기를 **모든 화면 하단**에 (2026-09-06)

세션 development-ai-e4 · sid=e739d2a9 · 브랜치 `fix/ecommerce-footer-disclosure`

## 0. 옵시디언 조회 결과 (§0)

대조군 «PropAI» 497파일(조회기 생존). «전자상거래»·«통신판매업»·«푸터» 관련 선행 기록 **없음**.
같은 클래스의 앞선 결함: **있다** — [[2026-09-06_구현이_없는_게_아니라_왜_안되는지_말하지_않았다]]
(같은 날 · 같은 인증 도메인 · **고지 결함** 클래스).

## 1. 전제 표 — 확인 *방법* + **실제 측정값**

| # | 전제 | 확인 방법 | 결과(실측 2026-09-06) |
|---|---|---|---|
| P1 | 네이버 검수가 반려됐다 | 개발자센터 검수상태 화면 | ★**승인거부 · 2026.05.13 · 반려 2건** |
| P2 | 반려 사유 ②가 전자상거래법 표기 | 화면 원문 | *「모든 서비스 환경(PC/모바일 **화면 하단 푸터**)에 [대표자명, 사업자등록번호, 통신판매업 신고번호]가 확인되도록」* |
| P3 | 초기 화면에 3종이 있나 | 라이브 `curl /ko` | ✘ **0 · 0 · 0** |
| P4 | 로그인 화면은 | 라이브 `curl /ko/login` | ✘ **0 · 0 · 0** |
| P5 | legal 페이지는 | 라이브 `curl /ko/legal/terms` | △ 대표자 1 · 사업자번호 1 · **통신판매업 0** |
| P6 | 통신판매업 신고번호가 코드에 있나 | 저장소 전수 grep(대조군 «로그인» 70파일) | ✘ **0파일** — 아예 없었다 |
| P7 | 값은 어디에 있나 | `grep -E '[0-9]{3}-[0-9]{2}-[0-9]{5}'` | 대시보드 푸터 + legal 2페이지 = **세 곳에 중복** |
| P8 | 전역 레이아웃에 푸터가 있나 | `[locale]/layout.tsx` | ✘ **0건**(대시보드 라우트에만 있었다) |
| P9 | 네이버 콜백은 별도 비밀번호를 요구하나 | 콜백 클라이언트 grep | ◎ **요구 없음** — 그 반려 조건은 해당 없음 |
| P10 | 신고번호 값 | **사용자 제공** | `제2026-경기광주-1245호` |

★**결론: 값이 없어서가 아니라 「검수자가 보는 화면」에 없었다.** 그리고 통신판매업 신고번호는 실제로 없었다.

## 2. 변경 내용과 회귀가 아닌 근거

1. `components/layout/SiteFooter.tsx` 신설 — **사업자 식별정보 단일 출처**(`BUSINESS_INFO`)
2. `app/[locale]/layout.tsx` 에 배선 → **초기 화면·로그인 포함 모든 locale 라우트**
3. `DashboardChromeGate` 의 **중복 푸터 제거** — 값이 두 곳에 있으면 한쪽만 고쳐진다(§29)
4. legal 2페이지에 **통신판매업 신고번호 추가**

**회귀가 아닌 근거**: 대시보드는 종전에도 푸터가 있었고 **같은 내용이 전역 푸터로 대체**된다
(값은 오히려 늘어난다 — 통신판매업·약관 링크 추가). 다른 라우트는 **없던 것이 생기는** 순증이다.

## 3. ★검증하지 못한 것

- **배포 후 라이브 확인 미실시** — 이 PR 은 아직 머지 전이다. 배포되면
  `/ko` · `/ko/login` 에서 3종이 실제로 보이는지 **재측정해야 한다**(반증조건: 하나라도 0이면 실패).
- **모바일 화면 실측 미실시**(사유 = **장치 부재**). 네이버는 *「PC/모바일」* 을 모두 요구하는데
  반응형 렌더를 실제 모바일에서 확인하지 않았다. jsdom 렌더 락은 뷰포트를 안 본다.
- **네이버 재검수 통과 여부는 당연히 미측정** — 반려 사유 ①(Callback URL)이 남아 있고
  그것은 **사용자 권한**이다. 이 PR 은 **사유 ②만** 닫는다.
- `(fieldapp)` 등 별도 레이아웃을 쓰는 라우트가 전역 푸터를 받는지 **미측정**.

## 4. 되돌리기 경로

`git revert` 한 번. 대시보드 푸터 제거분도 같은 커밋에 있어 함께 복원된다.

## 5. 잠금

`components/layout/__tests__/ecommerce-footer-disclosure.test.ts` (재료 5건)
- 3종이 비어 있지 않다 + **형식**까지(`\d{3}-\d{2}-\d{5}` · `제\d{4}-.+-\d+호`)
- 전역 레이아웃이 **렌더에 넣었는가**(임포트만으로는 배선이 아니다)
- ★**값이 한 곳에만** 있는가(중복 부활 방지) — 하한 단언으로 「walk 가 0건 읽어서 통과」 배제
- legal 2페이지 전수

`components/layout/__tests__/site-footer-render.test.tsx` (행위 3건)
- ★**실제로 그려지는가** — 재료 락만 있었을 때 **푸터 JSX 줄 삭제 변이가 SURVIVED** 했다.
  검수자는 소스가 아니라 **화면**을 본다.

**변이 10/10 CAUGHT** — 값 비우기 · 전역 배선 제거 · 푸터 줄 삭제(재시도) · 대시보드 중복 부활 ·
legal 제거 · 형식 파괴 · 라벨만 남기고 값 제거 · `data-testid` 제거(락 회피 시도) 등.
