# 인계 — 사통맵 UX 캠페인 (2026-09-04 · development-ai-23 → 다음 세션)

★**값을 승계하지 마십시오.** 아래 「휘발성」 표시가 붙은 것은 **명령으로 다시 재십시오**.

## 0. 이 캠페인이 무엇이었나

사용자 신고 하나에서 시작했다 — *"사통팔땅 멀티지도에 선택필지가 표시되는데 가독성이
떨어진다. 기본은 나타나되 **끌 수 있게** 해 달라."* 조사하니 **네 겹**이었다.

| # | PR | 무엇 | 상태(★재측정) |
|---|---|---|---|
| 1 | `#954` | 「선택 필지」 토글이 **UI 에만 있고 배선 0건** | 머지 `39ed70f5` · **라이브** |
| 2 | `#959` | 락이 **모양**을 잠가 서식만 바꿔도 깨짐 → **관계**로 | 머지 `0bd0b89e` |
| 3 | `#960` | 선택 라벨만 **버짓 면제** → 206필지가 지도를 덮음 | 머지 `6a39fb11` · **라이브** |
| 4 | `#965` | 레이어 **컨트롤** 영속(끈 것이 새로고침하면 되돌아옴) | 머지 `12da5720` · **미배포** |
| 5 | `#966` | 레이어를 **켰는지 자체**도 영속(신고의 남은 절반) | **OPEN · 필수 4/4 통과 · 머지 대기** |

### 재측정 명령 (휘발성 — 값을 옮기지 말 것)

```bash
# 배포된 커밋
curl -s https://4t8t.net/sw.js | grep -m1 '^const CACHE_NAME'
# PR 상태
gh pr view 966 --json state,mergeStateStatus,baseRefName,mergeCommit
# 머지 판정(★브랜치 sha 아니라 머지커밋으로 — 스쿼시라 브랜치 sha 는 main 조상이 아니다)
git merge-base --is-ancestor <mergeCommit> origin/main && echo 포함
```

### #960 배포 확증 프로브 (★대조군 먼저)

```bash
curl -s https://4t8t.net/ko/precheck | grep -oE '/_next/static/chunks/[A-Za-z0-9._/-]+\.js' | sort -u > /tmp/c.txt
# ★초기 청크 안의 webpack 청크맵에서 참조 청크를 **파생 수집**해야 동적 임포트 영역에 닿는다
for c in $(cat /tmp/c.txt); do curl -s "https://4t8t.net$c" -o /tmp/k.js
  grep -oE '"static/chunks/[A-Za-z0-9._/-]+\.js"' /tmp/k.js | tr -d '"'; done | sort -u | sed 's|^|/_next/|' >> /tmp/c.txt
for c in $(sort -u /tmp/c.txt); do curl -s "https://4t8t.net$c" -o /tmp/r.js
  grep -q '"hover-only"' /tmp/r.js && echo "대조군 $c"     # ★0 이면 프로브 사망 — 판정 금지
  grep -q '"rollup"'     /tmp/r.js && echo "★#960 표식 $c"; done
```

---

## 1. ★미해결 — 실거래 「아파트만 안 보인다」 신고

사용자가 스크린샷 3장과 함께 신고: *"실거래가 아파트는 클릭이 안 되고 금액표시배너도
뜨지 않는다. 다른 모든 표시는 정상이고 클릭하면 팝업이 뜬다."*

### 관측 (내가 실제로 잰 것 — 근거 있음)

| 잰 것 | 결과 |
|---|---|
| 백엔드 `POST /api/v1/zoning/nearby-map` | `apt_trade` **4건 전부 좌표 보유**(`located`) |
| 좌표 보유 수 | apt **4** · land **0**(국토부 지번 마스킹 = 원천한계) · commercial **2** |
| 화면 배너 「실거래 6곳」 | **4+0+2 = 6 과 정확히 일치** → 아파트 4개는 **이미 마커로 생성됨** |
| 순수 함수 `resolveMarketRenderPlan` 에 라이브 페이로드 투입 | `apt groups=4` · z≥15 에서 **라벨 4개 배정** |
| 배포본 청크 | `market:` · `만/평` · `지오코딩 전 사전컷` **전부 실재** |
| 화면의 두 금액 배너 | **정확히 `commercial_trade` 2건**(창현리 736-1 839만/평 · 마석우리 92-4 1,213만/평) |
| `fitBounds` | **`maxZoom: 15`** · **fit-key 당 1회**만 |
| 범례 문구 | `실거래 ${marketCount}곳` — **생성 마커 수**만, 화면 내 개수는 **말하지 않음** |

아파트 4곳(실측 좌표):

    신명스카이뷰그린        37.65592, 127.30240  평당 1,645만  5건
    중흥S클래스             37.65822, 127.30318  평당 1,415만  4건
    마석힐즈파크푸르지오    37.65467, 127.29805  평당 1,871만  9건
    그랜드힐(2차)           37.65002, 127.31876  평당 1,132만  1건

사용자 클릭 지점(스크린샷): **37.652535, 127.303105** — 상업 2건만 그 근처.

### ★추론 (미측정 · 반증조건 포함)

> **아파트 마커는 생성되지만 현재 화면 밖이다.**
> 지적도는 **z17 이상**에서만 나오는데(화면 배너가 그렇게 적는다) fit 은 **z15 까지만** 당긴다.
> 지적을 보려고 확대하면 아파트가 시야를 벗어난다. 상업 2건은 선택 필지 옆이라 남는다.

★**반증**: 사용자가 지도를 **z15 로 축소**했을 때 위 4개가 나타나면 참, 안 나타나면 거짓.
  거짓이면 z-index·페인트 순서를 **브라우저에서** 계측해야 한다(§D18 — `elementFromPoint`).

### 처방 후보 (사용자 확인 후 착수)

1. ★**범례에 「화면 밖 N곳」 병기** — 이 저장소의 «무음 절단 금지» 규율에 정확히 부합.
   범례가 «6곳» 이라 말하는데 화면엔 2개면, *"아파트가 안 나온다"* 로 읽히는 게 당연하다.
2. 유형을 새로 켜면 그 유형이 보이도록 fit
3. 화면 밖 마커 방향 표시

★**이건 «없다» 가 아니라 «있는데 안 보인다» 이고, 사용자에게는 같은 모양이다.**

### 좌표

    백엔드   apps/api/app/services/land_intelligence/nearby_map_service.py
             apps/api/routers/auto_zoning.py  (POST /nearby-map)
    프론트   apps/web/components/map/SatongMultiMap.tsx  (market 이펙트 · fitBounds · 범례)
             apps/web/lib/market/comparable-sample.ts    (selectMappableGroups)
    선행기록 볼트 `2026-08-21_두_손실을_섞어_읽으면_원천한계로_체념한다`
             ★그때는 **단독·토지·상업이 마스킹**돼 안 나왔다. 지금은 **반대**(아파트만)다 —
               같은 배너를 읽는 두 결함이 **정반대 방향**일 수 있다.

---

## 2. 남긴 부채 (`it.todo` 로 초록 안에 있음)

`apps/web/store/__tests__/useSatongMapPrefsStore.persist.test.ts`

- `resetEnabledLayers` / `resetControlsByLayer` **UI 미노출**(액션은 스토어에 있음)
- `selected` ↔ `selected-parcel` **어휘 통합 시 저장분 마이그레이션**(`migrate` 는 지금 통과)

그리고 **락으로 고정된 의도적 절충**(★회귀로 읽지 말 것):

- **새 레이어가 추가돼도 기존 사용자에게 자동으로 안 켜진다** — `enabledLayersCustomized` 가
  참인 사용자만 저장분을 쓴다. 기본값과 합치면 **사용자가 끈 레이어가 되살아나기** 때문.
- **206필지는 어느 줌에서도 개별 라벨이 안 나온다**(206 > 96). 식별은 필지 클릭.
- **13~64필지 z17 은 무변화** — `#960` 이 인용한 선행 실측은 «12필지가 이미 파일업» 인데
  임계는 그 2~5배다. 임계를 낮추는 것은 **별건**.
- `isRenderableSatongMapLayer` 가드는 **여전히 영속 층 밖**(선행 결함 · `merge` 에서 원소만 좁힘)

---

## 3. ★이 캠페인에서 반복해서 값을 낸 규율 (다음 세션이 그대로 쓰면 된다)

적대 리뷰 **4회 · 매번 MAJOR**. 분류하면 셋인데 **전부 「내가 옳다고 적은 것」에서 나왔다**:

| 유형 | 예 |
|---|---|
| **내가 「사실」로 적은 문장이 반증됨** | «다음 계정이 이전 선호를 못 본다» · «jsdom 에선 값으로 못 가른다» · «여기 담긴 것은 사용자가 켠 것» |
| **내 봉합이 새로 만든 회귀** | identity 를 항상 새로 · 위양성 대조군 제거 |
| **락의 축이 결함보다 위** | 이름 vs 값 · 스토어 vs 필드 · 마운트 vs 시간축 |

실제로 작동한 절차:

1. ★**대조군을 본판정보다 먼저 찍는다.** 오늘 조회기 오류로 **거짓 경보 직전까지 세 번** 갔고
   (`grep -E` 괄호 · `grep -F` 의 `\|` · 패턴 불일치) **매번 갈라 준 것은 대조군 하나**였다.
2. ★**락 단독 실행**으로 판정한다 — 전체 CAUGHT 는 **형제 락**이 잡은 것일 수 있다.
3. ★**변이는 「내가 고른 것」에만 참**이다. 독립 리뷰가 고른 것으로 다시 잰다.
4. ★**기준선은 「이전에 잰 값」이 아니라 「같은 base·같은 범위에서 잰 값」**이다.
5. ★**로컬 초록 ≠ CI 초록** — 같은 명령이 아니라 **같은 게이트**를 돌린다
   (`pnpm lint --format json -o …` → `scripts/ci/lint_ratchet.py`, ★**파이프 없이** 파일로 받고 `$?`).
6. ★**「미측정」을 적는 것과 「범위를 맞게」 적는 것은 다르다.** 좁게 적힌 미측정은 넓은 결함을 숨긴다.
7. ★**AA(add/add) 는 «양쪽이 만들었다» 이지 «내 것이 더 크다» 가 아니다.** 세고 나서 고른다.
8. ★**PR 메타 변경은 `gh api -X PATCH`** — `gh pr edit --base` 가 **rc=0 인데 안 바뀌는** 것을
   동료가 실측했다(Projects GraphQL). 종료코드가 거짓말한다.

---

## 4. 협업 상태

- 통합자 레인: `development-ai-3a`(배포·큐). `#966` 신호를 이미 보냈고 **`#965` 와 한 주기로
  묶으면 배포 1회를 아낀다**고 전달했다. ★**세션 이름은 재사용된다** — 보내기 직전 `ListAgents`
  로 다시 확인할 것.
- 보드: `scripts/coord.sh status`. ★`status` 는 **[NOTE] 를 안 보여 준다**(`coord.sh:39` 가
  CLAIM/RELEASE 만 grep) — 공유가 목적이면 **CLAIM 본문이나 PR 코멘트**에 실어라.
- claim: `satong-enabled-layers-persist` 가 내 것으로 걸려 있다. 이어받으면 **release 하고 다시 claim**.

## 5. 워크트리

    Development_AI_selmark   fix/satong-selection-label-toggle   (#954 · 머지됨 — 정리 가능)
    Development_AI_rollup    fix/satong-selection-label-density  (#960 · 머지됨 — 정리 가능)
    Development_AI_persist   fix/satong-layer-controls-persist   (#965 · 머지됨 — 정리 가능)
    Development_AI_layers    fix/satong-enabled-layers-persist   (#966 · **작업 중**)

★★**정리 전에 재라 — 단, 손으로 재지 마라**(2026-09-05 정정 · development-ai-d8):

    scripts/worktree_safe_to_remove.sh <워크트리경로>    # 0=안전 1=제거금지 2=판정불가

★**이 줄은 원래 「고유 커밋 0 · 작업트리↔인덱스 차이 0」이라고 적혀 있었고, 그 조건은 틀렸다.**
이 저장소는 **현재 기본이 스쿼시 머지**라 머지된 브랜치도 `origin/main..HEAD` 가 **비지 않는다**
★**「영원히」는 거짓이다**(2026-09-05 적대 리뷰가 반증): main 이력에 부모 2개인 **진짜 머지커밋이 224개**
있고(예: PR #255) 그 브랜치 tip 은 origin/main 의 **조상이라 범위가 빈다**. 조건은 「현재 기본값이
스쿼시일 때」이고, 조건 없는 단정은 참일 때도 검증 불가다(§27-c).
(실측: `#966` MERGED 인데 14건 · `#975` MERGED 인데 2건). 그 조건을 읽은 사람은 「못 지운다」로
읽거나 **조건을 무시하고 지운다** — 후자가 유실 사고다.
★**틀린 축 둘을 더 걸렀다**(같은 실측에서 · 다시 쓰지 마라):
  · `git diff origin/main HEAD` — **판별력 0**. 머지된 대조군도 똑같이 대량 deletions 를 낸다.
    그건 「미머지」가 아니라 **「main 보다 뒤처짐」**을 재는 것이다.
  · `tip != PR headRefOid` — **방향이 둘**이다(뒤에 커밋이 있다 / 뒤처졌다). 한쪽만 읽으면
    「미머지 2건」으로 **오보**한다. 실측 양방향: 로컬 고유 **0** · 뒤처짐 **7·4**.
◎ 올바른 축은 셋이다 — PR `MERGED` · `rev-list --count <PRhead>..HEAD` **== 0** ·
  `status --porcelain` 비어 있음. 위 스크립트가 그 셋을 태우고, **못 재면 「안전」이 아니라
  판정 불가(exit 2)**를 낸다.
★적대 리뷰 서브에이전트에게 격리 워크트리를 줄 때는 **끝나면 `git checkout --detach`** 를
  프롬프트에 넣어라 — 안 그러면 브랜치를 붙들어 기준선 측정이 `exit 128` 로 실패한다(실제로 났다).
