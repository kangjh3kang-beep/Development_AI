# 결제 안내 문구의 마크다운 제거 — 계획서 (2026-09-06)

> **발단**: `#1005` 배포 직후 **실제 브라우저로 화면을 렌더**해 보니 안내문에
> `**같아야**` 처럼 별표가 그대로 찍혔다. 소스·API 응답·번들 포함까지 확인했는데도
> **그려진 픽셀**은 다른 명제였다.

---

## 0. 옵시디언 조회 결과 (계획 게이트 §0)

★**조회 실패**. `/mnt/d` 전 경로 `ENODEV`(대조군 `/mnt/c` 31항목 정상 · `mount` 상
`D: … 9p` 는 살아 있으므로 **전송 단절**이며 처방은 `wsl --shutdown`).
따라서 볼트의 기각 이력·전례는 **미측정**이고, 「없다」고 말하지 않는다.

대신 저장소 안에서 같은 네 항목을 확인했다:

| 찾을 것 | 결과 |
|---|---|
| 이미 기각된 접근 | **없음**(평문 문구 표기 규칙에 관한 기록 0건) |
| 같은 클래스의 앞선 결함 | **있음** — `insight_types.py` 주석: 백엔드가 조립한 산문에 **영문 enum 이 그대로** 실려 화면에 떴던 사고. **같은 형태**(개발자용 표기가 사용자 화면으로 샘) |
| 미결·부채 | **없음** |
| 이전 판단의 근거 | 이 저장소의 평문 강조 관용구는 `「」`(예: `관리자 > API 키 > 「결제(PG)」`) — 기존 문구에서 실측 |

---

## 1. 전제 표 — 확인 방법과 **실측값**

| # | 전제 | 확인 방법 | **결과** |
|---|---|---|---|
| P1 | 별표가 실제로 화면에 찍힌다 | `agent-browser` 로 라이브 렌더 후 DOM 스냅샷 | 88줄: `이름과 **같아야** 확인이 빠릅니다` — **찍힌다** |
| P2 | 이 문구가 평문으로 렌더된다 | `CoinsClient.tsx` `<p>{bankConfig.guide}</p>` | 마크다운 파서를 거치지 않는다 |
| P3 | 관리자 진단도 같은 위험 | `routers/billing.py:861` `warnings.append(f"★{diag['message']} → {diag['action']}")` + `PaymentAdminPanel.tsx:226` 이 `warnings` 를 문자열로 렌더 | **같다** |
| P4 | 몇 건인가(파생형) | 응답으로 나가는 문자열만 AST/구간 조회 | **8건** — `billing.py` guide 1 · `KEY_DIAGNOSIS_COPY` 7 |
| P5 | `bank_transfer.diagnosis()` 는 깨끗한가 | 같은 조회 | **깨끗**(별표는 독스트링에만) — 대조군 역할 |
| P6 | 파일 전체 grep 은 왜 안 되나 | `billing.py` 전체 리터럴 스캔 | **11건**이 잡히는데 대부분 **독스트링**이다 → 위양성. 처방 범위 = 결함 범위 |
| P7 | 대체 표기 관용구 | 기존 문구 실측 | `「」` 를 이미 쓴다 |

---

## 2. 변경 내용과 회귀가 아닌 근거

`**X**` → `「X」` **8건**. 값·분기·계약은 **하나도 바뀌지 않는다** — 사람이 읽는 문구의
표기만 바뀐다. `KEY_DIAGNOSIS_COPY` 의 **키(code)·severity 는 불변**이므로 그것을 읽는
기존 잠금(코드 판정·심각도 단언)은 전부 그대로 통과한다(실측: 161 passed).

---

## 3. ★검증하지 못한 것

1. **이 수정 자체의 라이브 렌더는 미측정** — 배포 후에 확인해야 한다(현재 라이브는
   별표가 보이는 판이다).
2. **결제 도메인만 훑었다** — 다른 화면의 평문 문구에 같은 습관이 있는지는 **미측정**.
   범위를 넓히면 이 PR 이 검증할 수 없는 곳까지 건드리게 되므로 의도적으로 남긴다.
3. **`「」` 가 모든 폰트·좁은 화면에서 잘 보이는지** 미측정(기존 문구가 이미 쓰므로
   새 위험은 아니다).

---

## 4. 되돌리기 경로

revert 한 커밋. 값·계약 변경이 없어 데이터·API 스키마 영향 **0**.

---

## 5. 잠금 (155 → 161)

| # | 무엇을 잠그나 |
|---|---|
| C1 | `KEY_DIAGNOSIS_COPY` **표 전체**를 파생형으로 훑는다(새 항목이 자동 편입) |
| C2 | `bank_transfer.diagnosis()` 를 **네 상태 전부**에서 검사 — 정상만 보면 오류 문구가 샌다 |
| C3 | 화면 직행 문구는 **AST 로 그 함수의 실행 리터럴만**(독스트링 제외 — P6) |
| C4 | 각 검사에 **대조군** — 검사기가 별표를 실제로 잡는가 · 수집이 비지 않았는가 |

★C4 가 없으면 「0건」이 **부재인지 검사기 사망인지** 구별되지 않는다.
