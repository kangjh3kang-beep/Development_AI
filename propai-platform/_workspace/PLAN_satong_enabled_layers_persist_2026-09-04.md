# 계획 — 레이어 **활성 상태**도 영속 (2026-09-04)

★사용자 신고의 **남은 절반**. `#965` 가 레이어 **컨트롤**을 영속했는데, 레이어를 **켰는지 자체**
(`enabledLayers`)는 여전히 `useState` 라 새로고침하면 초기화된다 —
**같은 툴바의 절반만 영속되는 비대칭**이다. `#965` 에 `it.todo` 로 남겼고 이어가라는 지시를 받았다.

## 0. 옵시디언 조회 결과 (§계획게이트 0)

조회기 생존: 사통맵 문서 **69건** · 음성 대조군 **0**.

| 찾을 것 | 결과 |
|---|---|
| 이미 기각된 접근 | ★**없음** — «레이어 활성을 영속에서 제외한다» 는 결정 **0건**(관련 1건은 레일 UX) |
| 같은 클래스의 앞선 결함 | ★**있다** — `#965` 2차 리뷰 **MAJOR-1**: merge 가 **항상 새 객체**를 만들어 **전 사용자**에게 identity 교체를 일으켰다. **이 작업이 정확히 같은 축**이다 |
| 미결·부채 | `#965` 의 `it.todo` 셋 중 첫째가 이 항목 |
| 이전 판단의 근거 | 코드에도 «의도적으로 영속 안 함» 주석 **없음**(단순 미구현) |

## 1. 전제 표 — 확인 방법과 **실제 측정값**

| # | 전제 | 확인 방법 | **결과(실측)** |
|---|---|---|---|
| P1 | 현재 형태 | `SatongMapShell.tsx:836` | `useState<Set<SatongMapLayerId>>(() => new Set(["cadastre"]))` |
| P2 | 소비 형태 | 전수 | **9곳이 `.has()`** — Set 이 필요하다 |
| P3 | 갱신처 | 전수 | **2곳**(`toggleLayerEnabled` · `handleLayerControlClick`) |
| P4 | 계약 ① | `toggleLayerEnabled` | **`cadastre` 는 못 끈다** |
| P5 | ★계약 ② | `handleLayerControlClick` | **이미 켜져 있으면 같은 참조 반환** — 주석: *"무조건 new Set 을 만들면 … 오버레이·POI effect 가 전량 파괴·재생성된다(**깜빡임의 근원**)"* |
| P6 | `mapLayerState` 가 쓰는 형태 | `:1393` | `Array.from(enabledLayers)` — **매번 새 배열**을 만든다 |
| P7 | 직렬화 가능성 | — | `Set` 은 `JSON.stringify` 로 `{}` 가 된다 → **배열로 저장해야** 한다 |
| P8 | 겹침 | 열린 PR | `#965` 와 **같은 파일** → 그 위에 **스택**(세 번째 충돌 회피) |

## 2. 변경 내용과 **회귀가 아닌 근거**

스토어에 `enabledLayerIds: SatongMapLayerId[]` 를 두고, 컴포넌트는 **`useMemo` 로 Set 을 파생**한다.

    스토어   enabledLayerIds: ["cadastre"]  (기본)
             toggleLayerEnabled(id)   — cadastre 보호 + **변화 없으면 같은 배열 참조**
             ensureLayerEnabled(id)   — 이미 있으면 **같은 배열 참조**(P5 이식)
    컴포넌트 const ids = useSatongMapPrefs(s => s.enabledLayerIds)   // 셀렉터
             const enabledLayers = useMemo(() => new Set(ids), [ids])

- **회귀 아님**: 저장분이 없으면 기본이 `["cadastre"]` 로 종전과 동일.
- ★**P5 계약을 배열로 이식**한다 — 변화가 없으면 **같은 참조**를 돌려주므로 `useMemo` 가
  재계산하지 않고 `mapLayerState` identity 도 안 바뀐다.
- ★**오히려 개선**: `mapLayerState` 가 `Array.from(...)` 대신 **스토어 배열을 직접** 쓴다
  (P6 — 지금은 memo 가 돌 때마다 새 배열이 생긴다).

## 3. ★검증하지 못한 것 / 미리 정할 것

- **브라우저 눈 확인** — 배포 후에만.
- ★★**`#965` MAJOR-1 과 같은 축이다.** 그때 나는 «저장분이 없어도 새 객체» 를 만들어
  **전 사용자**에게 identity 교체를 일으켰다. 여기서도 같은 실수를 할 수 있으므로
  **두 모집단으로 잠근다**(변화 없음 → `toBe` · 변화 있음 → `not.toBe`).
- ★**새 레이어가 추가되면 저장분에 없다** — `merge` 가 레이어 단위가 아니라 **배열 통째**라,
  기본으로 켜져야 할 새 레이어가 기존 사용자에게 **안 켜진다.** 이건 `controlsByLayer` 와
  **성질이 다르다**(거기선 «켜짐 목록» 을 합쳤지만, 여기서는 «켠 것» 이 사용자 의사다).
  → **의도적으로 저장분을 그대로 존중**한다. 그 판단을 여기 적고 락으로 고정한다.
- **되돌리기**: `resetControlsByLayer` 와 같은 형태로 `resetEnabledLayers` 를 둔다(UI 노출은 범위 밖).

## 4. 되돌리기 경로

스토어 읽기를 `useState(() => new Set(["cadastre"]))` 로 되돌리면 종전 동작.

## 5. 잠금

| 축 | 락 |
|---|---|
| **영속** | 껐다 → 재수화 → **꺼진 채로** · **대칭**(켠 것도 켜진 채로) |
| **기본값** | 저장분 없으면 `["cadastre"]`(첫 방문 = 종전과 동일) |
| ★**identity** | 변화 없는 토글 → **같은 참조**(`toBe`) · 변화 있는 토글 → **다른 참조** |
| **계약 ①** | `cadastre` 는 꺼지지 않는다 |
| **계약 ②** | 이미 켜진 레이어에 `ensure` → 같은 참조 |
| **배선** | 셸이 **셀렉터로** 읽고 `useState` 가 **0건** |
| ★**새 레이어** | 저장분을 **그대로 존중**한다(새 레이어가 자동으로 안 켜진다 — 의도) |
