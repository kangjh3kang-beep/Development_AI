# 변이 도구의 rc 신뢰 판정이 **거꾸로** 걸려 있었다 — 층을 가른다

- 2026-09-02 · 세션 `development-ai-3c [8cabd7]` · 브랜치 `fix/mutate-manual-shell-wrapper-rc`

## 0. 조회 결과 (계획 게이트 §0)

| 찾을 것 | 결과 |
|---|---|
| **이미 기각된 접근** | **있음** — *"보이면 시끄럽게 경고한다"* 는 `#924` 계획서에서 **이미 기각**됐다(*"경고는 산문이고 판정이 산출물"*). 그 처방(판정 불가 + exit 12)은 **유지**한다. |
| **같은 클래스의 앞선 결함** | `CLAUDE.md` §검증 규율 9 · `PLAN_mutate_manual_pipe_verdict_2026-08-27.md`(P1~P6). ★그 계획서가 **위양성을 이미 알고**(P4) `MUTATE_ALLOW_PIPE` 탈출구를 뒀다 — 내 첫 서술 *"위양성만 낸다"* 는 **과했다**. |
| **미결·부채** | `PLAN_probe_run_mode_dead_2026-08-27.md` 가 *"전수 vitest 가 변이 없이 rc=1"* 을 **범위 밖**으로 남겼다(별건). |
| **락이 이미 있나** | ★**둘 다 있다** — `apps/api/tests/test_manual_mutation_guard.py`(8건) · `propai-platform/tests/test_mutate_manual_pipe_verdict.py`(7건). **테스트 루트가 둘**이다. |

★**출처**: 동료 `development-ai-8f` 가 자기 사고(`bash -c "…; tail"` → 거짓 SURVIVED)로 발견해 보드에
넘겼고, 그쪽이 *"큐가 우선이라 안 잡는다"* 고 해 내가 잡았다. **재현은 직접** 했다.

## 1. 전제 표 — 측정 방법과 **실제 값**

| # | 전제 | 방법 | 결과 |
|---|---|---|---|
| P1 | `;`·`&&` 가 미탐지다 | 도구의 실제 `case` 식을 그대로 실행 | **참** |
| P2 | `\|\|` 도 미탐지다(동료 진단) | 같은 방법 | ★**거짓** — `\|\|` 는 문자 `\|` 두 개라 **우연히 잡힌다** |
| P3 | 직접 argv 의 `\|` 가 차단된다 | 격리 저장소에서 도구 실행 | **참**(`grep -qE 'a\|b'` → 판정 불가) |
| P4 | `bash -c "…; tail"` 이 거짓 SURVIVED 를 낸다 | 같은 곳 · 그 안에서 **실제 1 failed** | **참** — `SURVIVED` 발행 |
| P5 | 면제가 **탐지를 통째로 끈다** | `case *"pipefail"*` 직독 + 실행 | **참**(`pipefail` 이 문자열로만 있어도 면제) |
| P6 | 래퍼면 **다 위험**인가 | ★기존 락 두 파일의 호출 형태 전수 | ★**거짓** — 기존 락이 **모든 호출을 `bash -c` 로 감싼다**. 단일 단순 명령은 rc 가 정확하다 |
| P7 | 기존 호출부에 위험 형태가 있나 | 저장소 전수(`bash -c\|sh -c`) | 문서·락 안의 **예시 1건**뿐(실사용 0) |

★**P6 는 내 첫 설계를 죽였다** — *"셸 래퍼면 판정 불가"* 로 만들었더니 **다른 테스트 루트의
락 4건이 깨졌다.** `apps/api/tests/` 만 돌리고 초록으로 읽은 것이 그 전 단계다(테스트 루트가 둘).

## 2. 변경 내용 — **형태에서 층으로**

    ✘ 종전:  인자열 **전체**에서 문자 `|` 하나  → 두 층(셸 스크립트 / 리터럴 argv)을 섞어 판정
    ◎ 현재:  ① 셸 래퍼가 아니면 → **신뢰**(execvp 직접 실행이라 rc 는 그 명령의 것)
             ② 래퍼면 **`-c` 뒤 스크립트**를 본다
                · 접두 `set -… pipefail;` 를 **걷어내고**
                · 나머지에 `;`·`&&`·`||` → **판정 불가**
                · 나머지에 `|` 이고 pipefail 이 **없으면** → 판정 불가
             ③ 탈출구 `MUTATE_ALLOW_SHELL="사유"`(옛 `MUTATE_ALLOW_PIPE` 도 계속 받는다)

★**같은 `;` 를 위치로 가른다**: `set -o pipefail; cmd | tail` 은 **안전**(설정 분리),
  `cmd; tail` 은 **위험**. 문자 하나로 못 하던 것을 **구조**로 한다.

**회귀가 아닌 근거**: 두 락 루트 **19 passed**(기존 15 + 신규 4). 기존 호출부 위양성 0(P7).

## 3. ★검증하지 못한 것

1. **`eval`·백틱·`$(...)` 안의 명령 연결**은 안 본다. 스크립트 문자열을 **파싱하지 않고 부분문자열**로 보므로, 따옴표 안의 `;`(예: `pytest -k 'a;b'`)는 **위양성**이 될 수 있다 — 실사용 0건이라 그대로 뒀다.
2. **`-c` 를 다른 위치·형태로 주는 호출**(`bash -lc`, `--command=`)은 못 찾는다.
3. **`RC_WHY` 문구가 판정 줄에 실리는지**는 락이 안 본다(사유 없는 판정 금지를 잠그지 않았다).
4. **다른 셸**(`fish`·`pwsh`)은 목록에 없다.
5. 동료의 `#944` 등 **다른 세션의 과거 변이 판정**이 이 결함으로 오염됐는지 **재감사하지 않았다**.

## 4. 되돌리기

`scripts/mutate_manual.sh` 의 탐지 블록을 되돌리면 종전 동작(락 신규 4건이 빨개진다).

## 5. 잠금

| 지킬 것 | 검사 |
|---|---|
| `;`·`&&` 래퍼가 **판정을 안 낸다** | `test_셸_래퍼면_판정을_발행하지_않는다`(파이프 회귀 + `;` + `&&`) |
| **단일 명령 래퍼는 신뢰**(위양성 금지) | `test_단일_단순_명령_래퍼는_신뢰한다` |
| **직접 argv 리터럴 `\|`** 는 위험이 아니다 | `test_직접_argv_의_리터럴_파이프는_위험이_아니다` + **거짓 CAUGHT 가드** |
| `pipefail` 접두를 **걷어내고** 판정 | `test_pipefail_접두는_걷어내고_판정한다`(두 모집단) |
| 탈출구가 **사유를 남긴다** | `test_셸_래퍼_예외는_사유를_남기고_통과시킨다` |
| 기존 계약(7건) | `propai-platform/tests/test_mutate_manual_pipe_verdict.py` **무변경 통과** |

### 변이 — 기준선 rc=0 확인 후 · **두 락 루트 동시**

    N1 ★';' 를 위험에서 뺌(원 결함 부활)          CAUGHT
    N2 ★래퍼면 무조건 차단(내 첫 거친 설계)        CAUGHT   ← 위양성 축
    N3 ★pipefail 접두 제거 안 함(위양성 부활)      CAUGHT
    N4 pipefail 이 파이프를 안 고치는 것으로        CAUGHT
    N5 예외를 사유 없이 허용                       CAUGHT

★**판정어는 줄 시작 앵커로** 본다 — 부분문자열이면 판정 불가 **설명문 안의**
  *"CAUGHT 가 SURVIVED 로 보고된다"* 를 집는다(이 작업 중 실제로 한 번 빨개졌다).
