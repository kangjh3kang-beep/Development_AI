# PropAI 통합자 인계 — 2026-08-27 23:18Z (`development-ai-93 [f0bf22]` → 후임)

> ★**이 문서의 모든 값은 낡았다고 가정하고 재측정하라.** 값을 적은 곳마다 **재는 명령**을 함께 뒀다.
> ★**"그 세션에 물어보라"는 없다** — 오늘 내가 그 당사자가 됐다. 세션이 재시작돼 내 이름이
>   `development-ai-b8 [a814d0]` → `development-ai-93 [f0bf22]` 로 바뀌었고, 함께 일하던
>   **ae·62·9c·f8 이 전부 사라졌다.** **보드에 적은 것만 살아남았다.**

---

## §0 착수 즉시 (순서대로)

```bash
cd /home/kangjh3kang/My_Projects/Development_AI
git rev-list --left-right --count HEAD...origin/main   # ★원격만>0 이면 낡은 트리 — 먼저 ff
bash propai-platform/scripts/monitor/integrator_dashboard.sh   # 0=이상없음 2=위반 3=검사기사망
scripts/coord.sh status | tail -80                     # ★자르지 마라
```
**★exit 3 을 0 으로 읽으면 "안 재 봤다"가 "깨끗하다"가 된다.**
★계기판이 **배포 전환 중**에는 exit 3 을 낸다(전환 중 컨테이너 교체). **결함이 아니다** —
전환이 끝난 뒤 재실행해서 갈라라. 오늘 그것으로 한 번 오진했다.

추가로: ① `ListAgents` 로 **생존 피어 확인** ② 전역지침 「인수인계 공유 의무」대로 **보드에 요약**
③ `obsidian-brain` 으로 주제어 조회(오늘 그 조회가 중복 PR 을 두 번 막았다).

---

## §1 ★★가장 급한 것 — 프로덕션이 **결함본**을 돌고 있다

`#918`(선언 범위 **cost/소방**)이 **선언 밖의 성장루프 파일 5개를 함께 삭제**했다.
`#917` 의 적대 리뷰 봉합분이 main 에서 사라졌고, **내가 그 상태를 88주기로 배포했다.**

```bash
# 지금도 결함본인지 재라 (봉합본=23/2 · 결함본=12/0)
git show origin/main:propai-platform/apps/api/tests/test_effector_firing_observability.py | grep -cE '^(async )?def test_'
git show origin/main:propai-platform/apps/api/app/services/growth/effector_firing.py | grep -cF 'any('
# 라이브(실행 중 컨테이너)
ssh -i ~/.oci.key ubuntu@168.110.125.89 'PORTS=$(grep -vE "^[[:space:]]*#" $HOME/caddy/Caddyfile | grep -oE "reverse_proxy[[:space:]]+localhost:80[0-9]+" | grep -oE "80[0-9]+" | sort -u); sudo docker exec propai-api-$PORTS python -c "import inspect; from app.services.growth import effector_firing as ef; print(inspect.getsource(ef).count(\"any(\"))"'
```
**23:18Z 실측: main 12/0 · 라이브 0 = 둘 다 결함본.**

**→ `PR #921` 이 복원본이다. 머지되면 즉시 168 api 재배포.**
복원 내용은 `45c9235a` 에 온전하다: `git show 45c9235a -- <경로>`.
★`#918` 의 **소방 교정은 정상 반영분**이라 건드리지 않았다(형제 락 36 passed 무회귀 확인).

---

## §2 내 PR **7건** — 전부 필수 4/4 pass · 전부 BEHIND · **auto-merge 껐다**

```bash
for N in 897 901 905 907 910 914 921; do gh pr checks $N | grep -viE 'cloudflare|workers'; done
```

| PR | 무엇 |
|---|---|
| **#921** | ★위 §1 복원. **가장 급함** |
| **#897** | 에스컬레이션이 **구조적으로 발화 불가**(실행수에 캡이 천장을 씌워 임계 5 에 못 닿음). 상수 불변·**세는 대상만** 변경. 독립 리뷰 MAJOR 4건 반영 완료 |
| **#901** | 진단 사유가 **마지막 층에서 사라짐** 3건(narrative 가 `feedback down None%` 출력 · 라우터가 문자열 워터마크를 `null` 로 위장 · 파라미터가 헬퍼를 가림) |
| **#905** | 프로메테우스 경보 규칙 **3건이 발화 불가**(메트릭이 `/metrics` 에 0건). **탐지만** 기계화(`xfail strict`) |
| **#907** | `#886` 의 **안 쓸린 형제** — L1 이 인사이트를 안 닫음(SQL 이 `id` 를 아예 select 안 함) |
| **#910** | 계기판이 **지연 버스트를 못 봤다**(하루 최소 5회 · 최대 **69,503ms**). 사후 판정 절 추가 |
| **#914** | 치유 후보 조회가 처리 못 하는 타입까지 `LIMIT 200` 슬롯 잠식. ★선언↔분기 **AST 정합** 락 |

★**왜 AM 을 껐나 — 오늘 두 번 값을 했다**: ①독립 리뷰가 `#897` 에서 MAJOR 4건(자기승인했으면
**의미가 틀린 신호가 critical 로** 나갔다) ②`#917` 이 보여 준 것 — **적대 리뷰를 AM 켠 뒤에
돌리면 경주가 되고 진다**(38초 차로 결함본 머지).
★다만 **7건이 쌓인 것도 사실**이다. 보드에 리뷰 요청을 올려 뒀다. **자기승인은 하지 마라.**

---

## §3 배포 — 이 세션이 75~88주기를 했다

```bash
# 168 api  ★배포는 ~/deploy.sh (safe-deploy 금지)
ssh -i ~/.oci.key ubuntu@168.110.125.89 'bash ~/deploy.sh'
# 158 web  ★그 스크립트는 158 호스트 전용이다(로컬에서 돌리면 cd 에서 죽는다)
ssh -i ~/.oci.key ubuntu@158.179.174.207 'bash ~/Development_AI/propai-platform/scripts/safe-deploy.sh web main'
```
★**IP 를 기억에서 쓰지 마라** — 저장소에서 파생하라(`grep -oE '1[0-9]{2}(\.[0-9]{1,3}){3}' propai-platform/scripts/monitor/integrator_dashboard.sh`).
★**`cmd; echo` 를 붙이지 마라** — 하네스가 **마지막 명령의 종료코드**를 보고한다. 내가 그것으로
`safe-deploy` 실패를 **exit 0 으로 오보**할 뻔했다. 로그는 **파일로 받아라**(`tail` 로 자르면 앞부분이 사라진다).

### ★★배포 확증은 **세 층**이다 — 섞지 마라

| 층 | 무엇으로 | 함정 |
|---|---|---|
| ① **배포** | 빌드ID(커밋을 직접 인코딩) | `curl -s https://4t8t.net/sw.js \| grep -m1 '^const CACHE_NAME'` ★**줄시작 앵커 필수** |
| ② **적재** | 파생 마커 **양방향**(배포 전 0 · 후 N) | ★**마커가 없는 PR 이 있다**(새 문자열 무생성) → 그때는 ①로만 판정하고 **②③을 미확증이라 적어라** |
| ③ **동작** | ★**실행 중인 그것에게 직접 묻기** | `docker exec <api> python -c "import inspect; ..."` |

★**②를 ③으로 읽지 마라** — `#898` 이 그 값을 치렀다(번들에 문자열은 **있었고** 런타임 전역은 `undefined`).
★**③도 「어느 버전인가」는 말해 주지 않는다** — `#917` 이 그것이다(심볼은 있는데 결함본).
★**마커를 고를 때 「배포 전 0인가」를 저장소에서 먼저 확인하라.** 나는 후보 셋이 전부 부적격인 것을
보고도 하나를 골랐고, 그 기준선은 비교 자체가 불가능했다.

---

## §4 ★기각된 가설 (재생성 금지 — 값을 치렀다)

| 가설 | 판정 |
|---|---|
| *"판정률 100%는 하한(`FALLBACK_MIN_CALLS`)을 내리면 된다"* | **기각** — `threshold_relax` 가 **프로덕션 HTTP 타임아웃을 곱한다**(볼트에 사고 기록) |
| *"`coverage_pct` 를 싣자"* | **기각** — `total>0` 에서 상수이고 `state` 와 완전 중복 |
| *"C 를 독립 baseline 으로(7일 트레일링)"* | **기각** — 30일 replay 에서 judged **510/510 동일**(0% 개선) |
| *"생산자 3중 실행이 경보를 지운다"* | **기각(내 오류)** — 같은 `(key,window)` 2회 발행 **0건**. 진짜는 **baseline 자기참조**(`baseline_p95 == p95_ms` 200/200) |
| *"#913 의 소방 제거는 이중계상 교정"* | **기각(내 오류)** — 소방시설공사업법 §21② 는 **분리 도급 의무**(위반 시 벌칙). `#918` 이 복원 |
| *"#880 cleanup 은 03:12 **UTC**"* | **거짓** — `celery_app.py:61` 이 `timezone="Asia/Seoul"` 이라 **03:12 KST = 18:12 UTC**. 그대로 03:16Z 에 재면 **없는 결함을 신고**하게 된다 |

---

## §5 확증됐으나 **미조치** (소유자 없음 — 집으면 보드에 남겨라)

1. **레거시 `latency_regression/info`** — `NON_ACTIONABLE` 이 `frozenset({"latency_baseline"})` 로
   **타입만** 봐서, 비조치성을 `severity` 에 담은 레거시 행이 그물을 통과해 **조치대상으로 집계**된다.
   23:xxZ 실측: open 1898 · 표본 500 중 `latency_regression` 287 · **그중 info 159**.
   → 처방 후보 ①필터를 계약(`insight_type_for_latency`)에서 **파생**시켜 넓힌다(데이터 무변경) ②백필.
   **①이 옳다고 보지만 ②는 내 단독 판단 밖이라 남긴다.**
2. **`stale_reanalysis` 생산 0건** — 카탈로그·분기·효과기가 다 있는데 **전 역사상 발행 0**.
3. **baseline 자기참조** — 점진 회귀 **구조적 탐지 불가**. 반감기 **1배치**(볼트의 "≈2일"은 반증됨).
4. **지연 버스트 원인** — 하루 최소 5회 · 최대 **69,503ms** · 여러 라우트 **동시**(공통 경로 시사).
   **Supabase 콘솔 권한이 없어 원인 미측정.** `PR #910` 이 탐지만 넣는다.
5. **감시 스택 가동 여부 미측정** — 프로메테우스·Alertmanager 가 실제로 떠 있는지 안 쟀다
   (`#905` 의 3건은 떠 있든 아니든 발화 불가라 결론 무관하나, **나머지 8개 규칙의 유효성은 거기 달렸다**).
6. **`#918` 이 왜 남의 파일을 담았는지 원인 미확인**(소유 세션 부재). 내 추정은 `reset --soft` 류.

---

## §6 ★이 세션이 값을 치르고 배운 것 (같은 자리를 두 번 밟지 마라)

- **인용은 검증이 아니다.** `#913` 배포 때 *"반쪽 이관인가"* 는 3층까지 봤는데
  **"제거가 옳은가"는 안 쟀고** PR 본문의 근거를 그대로 인용했다. 법령을 봤으면 갈렸다.
- **숫자를 잰 것이 기전을 잰 것은 아니다.** `57.8%` 를 정확히 재고 **기전은 그럴듯한 것으로 채웠다.**
- **대조군이 살아 있는데 마커만 죽으면, 마커를 의심하기 전에 대상을 의심하라.**
  ★그날 마커를 네 번 틀려 *"이상하면 내 마커를 의심"* 하는 습관이 생겼고, **그 습관이 진짜 회귀를
  덮을 뻔했다.** 최근에 자주 틀린 쪽을 자동으로 범인으로 지목하면 **결백한 회차를 놓친다.**
- **삭제 규모를 세어라.** `#918` 은 파일 목록이 그럴듯했고 **추가 352 / 삭제 512** 로만 드러났다.
- **판정은 파서로.** 소스 문자열 검사는 **그 수정을 설명하는 내 주석**에 걸린다(오늘 실측).
- **린트는 커밋 직전에.** 구현 후엔 돌렸는데 테스트 파일을 쓴 뒤 안 돌려 CI 가 빨갰다.
  ★체크 이름이 `Backend (pytest)` 여도 **실제로 죽은 단계는 `Ruff (lint)`** 일 수 있다 →
  `gh api repos/<slug>/actions/jobs/<id> --jq '.steps[]'` 로 단계별 conclusion 을 봐라.
- **같이 떨어지는 지표를 전부 세어라.** *"감소는 교정"* 을 쓸 때 하나만 설명하면 **다른 하나가
  회귀로 오독**된다(`qty_pct` 를 빠뜨렸다가 동료가 짚어 줬다).

---

## §7 협업 — 오늘 **양방향**으로 작동했다

동료가 내 오류 3건을 잡았고(기전 반증 · 정정의 재정정 · 배포 노트 누락),
나는 동료 오류 3건을 잡았다(exit-3 오진 · ruff 핀 전제 · `#894` 가 pytest 가 아니라 lint 에서 죽은 것).
★**자기승인이 아니었던 이유가 그것이다.**

★**직접 메시지는 공유가 아니다.** 오늘 그 값을 치렀다 — 긴급 노트에 *"소유 세션이 판단해 주십시오"*
라고 적은 **직후** 그 세션들이 없다는 것을 알았다. **보드·PR 본문·커밋 메시지에 적어라.**

라이브 검증 계정: `admin@4t8t.net` / `admin1234`
★토큰 만료는 **401 이 아니라 빈 값/0건**으로 올 수 있다. **0건을 보면 재로그인부터.**
★`%{http_code}` 만 재지 마라 — `login=200` 을 4회 보고 정상으로 읽었는데 **본문에 `access_token` 이 없었다.**
