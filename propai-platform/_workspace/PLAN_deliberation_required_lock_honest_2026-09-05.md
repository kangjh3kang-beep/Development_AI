# 부채 표지가 **자기 상수**라 부채가 해소돼도 침묵했다 (2026-09-05)

> **작성: `development-ai-c8` · sid=0a08a179 · 2026-09-05 · 절대형 서명**
> 대상: `propai-platform/tests/test_deliberation_ci_required_shape.py`
> 선행: `#964`(구조 전환 · 작성 `development-ai-8f`) · 등록 실행(오늘 · 이 세션)

## 0. 옵시디언 조회 결과

| 찾을 것 | 결과 |
|---|---|
| **이미 기각된 접근** | ★**있다.** `2026-09-04_인계용_명령은_아무도_실행해본_적이_없다.md` §2 — *"`strict=True` 는 XPASS 를 실패로 보고하므로, 등록하는 순간 필수 체크 `Backend (pytest)` 가 저장소 전역에서 빨개지고, 그 xfail 을 지우는 PR 자신이 그 빨강에 막히는 **교착**이 난다."* → **「선언대로 작동시키기」는 기각된 접근이다.** |
| **같은 클래스의 앞선 결함** | `feedback_a_lock_that_asserts_its_own_constant` — *"자기 상수를 단언하는 락은 정반대 주장을 통과시킨다"*. 이번 건은 그 변종(상수라 **현실을 보지 않는다**). |
| **미결·부채** | 같은 문서 Next Actions: *"부채 표지를 **머지를 막지 않는 층**으로 옮기거나, **상수 대신 실제 조회**로 바꾸기"* → **이 PR 이 그것이다.** |
| **이전 판단의 근거** | 같은 문서: 이 API 는 **치환**(PATCH)이고 기존 4종을 함께 보내지 않으면 그 보호가 **사라진다**. 즉 «등록이 조용히 사라지는» 실패모드가 실재한다 — 그것이 이 락의 존재 이유다. |

## 1. 전제 표 (전부 **실측** · 2026-09-05)

| 전제 | 확인 방법 | 결과 |
|---|---|---|
| 등록이 실제로 끝났나 | `gh api .../branches/main/protection --jq '.required_status_checks.contexts[]'` | ★**끝났다 · 5종** — `Backend (pytest)` · `Detect changes` · `Frontend (next build)` · `Frontend (type-check + lint + test)` · **`Deliberation Engine (pytest)`** |
| 그런데 표지가 뒤집혔나 | `pytest tests/test_deliberation_ci_required_shape.py -q -rxX` | ★**아니다.** `6 passed, 1 xfailed` — 사유 문구는 여전히 *"**아직 없다**"* 라고 말한다(**초록 안의 거짓**) |
| 왜 안 뒤집히나 | 파일 `:120` 원문 | `registered = False` — **상수는 현실을 보지 않는다** |
| 선언대로 고치면 안 되나 | 볼트 §2 + 이 파일의 위치 | ★**안 된다.** 이 파일은 필수 `Backend (pytest)` 가 태우는 층이다 → XPASS(=실패)가 **전역 차단** |
| skip 이 required 를 충족하나 | `#986`(오늘) · 이후 `#990` | ★**충족한다 — 이 저장소에서 실측됐다.** 잡레벨 skip(`Deliberation Engine (pytest)` = `skipping`)인 채로 머지됨. 볼트의 *"발화한 적 없는 경로"* 는 **오늘 발화했다** |
| 잡 이름이 컨텍스트 문자열인가 | `deliberation-ci.yml` 파싱 | `engine-tests.name == "Deliberation Engine (pytest)"` — 보호규칙 컨텍스트와 **바이트 동일** |

## 2. 변경 내용

`registered = False` 를 **실제 조회**로 바꾸고 `xfail` 을 없앤다.

1. `_live_required_contexts()` — `gh api` 로 브랜치보호를 **실측**한다.
   ★**못 재면 `None` 을 돌려준다. 상수로 대체하지 않는다**(그 상수가 곧 오늘의 거짓이 된다).
2. `test_심의검사가_필수로_등록돼_있다` — 잴 수 있으면 **판정**하고, 못 재면
   **`pytest.skip` 으로 「판정 불가」를 사유째 밝힌다.**
   ★**`xfail(strict=True)` 를 쓰지 않는다** — 그 형태가 교착의 원인이었다(§0).
3. **신규 락** `test_워크플로_잡이름이_등록된_컨텍스트와_같다` — 잡 이름을 바꾸면
   그 컨텍스트가 **영원히 `Expected`** 로 남아 **큐 전체가 막힌다.** 오프라인에서 잠근다.
   ★기대값은 워크플로에서 파생하지 **않는다** — 그러면 자기참조라 이름을 바꿔도 통과한다.
   외부 시스템(보호규칙)의 문자열을 **독립 리터럴**로 못 박고, 워크플로가 그것을 **생산하는지**를 본다.

**회귀가 아닌 근거**: 기존 6개 락은 그대로다(워크플로 구조 축). 바뀌는 것은
**부채 표지 하나**이고, 그 표지는 지금 **거짓을 말하는 중**이다.

## 3. ★검증하지 못한 것

- **CI 안에서 이 조회가 성공하는지 미측정.** `GITHUB_TOKEN` 은 보통 브랜치보호 읽기 권한이
  없으므로 CI 에서는 **skip 으로 떨어질 가능성이 높다**(추론 · 반증 조건: CI 로그에
  `SKIPPED` 대신 통과/실패가 찍히면 이 추론이 틀린 것). 그래도 **거짓을 말하는 상수보다 낫다** —
  판정 불가를 **판정 불가라고 말한다**.
- **보호규칙이 사라지는 사건을 실제로 재현하지 않았다**(비가역 설정 변경이라 만들지 않는다).
  대신 조회 결과를 **가짜 목록으로 바꾸는 변이**로 락이 발화함을 확인한다.

## 4. 되돌리기 경로

단일 커밋 revert. 런타임 코드 변경 **0** — 테스트 파일 하나.

## 5. 잠금

| 이 변경이 지키는 것 | 무엇이 지키나 |
|---|---|
| 등록이 **치환 PATCH 로 조용히 사라지는 것** | `test_심의검사가_필수로_등록돼_있다`(잴 수 있을 때) |
| 잡 이름 변경으로 **큐 전체가 `Expected` 에 막히는 것** | `test_워크플로_잡이름이_등록된_컨텍스트와_같다`(오프라인) |
| **못 재는 것을 잰 척하는 것** | 조회 실패 시 `skip` + 사유. ★상수 금지를 주석에 근거째로 적는다 |

## 6. 변이

| 변이 | 기대 |
|---|---|
| 워크플로 잡 `name` 을 다른 문자열로 | CAUGHT |
| `_live_required_contexts` 가 항상 `None` (=상시 skip) | ★**SURVIVED 가 정상** — 판정 불가는 실패가 아니다. 그래서 **이름 락을 따로 둔 것**이다 |
| 조회 결과에서 심의 컨텍스트만 제거 | CAUGHT (잴 수 있는 환경에서) |
