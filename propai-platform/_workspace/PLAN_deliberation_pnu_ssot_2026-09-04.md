# 심의 서비스 PNU 판정 — 길이 7벌을 정본으로, **검증 안 되는 입구**를 막는다 (2026-09-04)

> **작성: `development-ai-8f` · 2026-09-04**

## 0. 옵시디언 조회 결과

| 찾을 것 | 결과 |
|---|---|
| 이미 기각된 접근 | **없음** |
| 같은 클래스의 앞선 결함 | **있다.** `wiki/errors/2026-09-02_저장소가_이미_막던_함정에_내가_빠졌다` — 파이썬 `\d` 가 **유니코드 십진수를 포함**해 전각 19자가 통과했다. 정본은 `^[0-9]{19}$` 이고 **공백을 벗기지 않는다**(판정한 문자열 ≠ 소비하는 문자열이면 판정이 아무것도 보증하지 않는다) |
| 미결·부채 | ★**있다.** `wiki/dev-tasks/2026-09-02_백엔드_PNU_유효성_SSOT_배선과_수집수_대조` §미측정: *"**길이만 6벌**은 현재 관측 오염값(6자·26자)에 **안 걸린다 — 잠재**다"* |
| 이전 판단의 근거 | 위 「잠재」 라벨. ★**그 라벨이 옳았다** — 아래 §1 이 그것을 확인한다 |

## 1. 전제 표 (전부 **실측** · 2026-09-04 `origin/main`)

| 전제 | 확인 방법 | 결과 |
|---|---|---|
| 길이 기반 PNU 판정이 몇 벌인가 | `git grep -nE` 파생(테스트 제외) | ★**7벌** — 인계 라벨의 「4곳」도, 볼트의 「6벌」도 아니다 |
| 어디에 사는가 | 위 결과의 경로 | ★**전부 `services/deliberation-review/`** — 별도 서브서비스 |
| 그 경로가 살아 있는가 | 프로덕션 `docker ps` | ★**`deliberation-engine` 실행 중** — dead path 아니다 |
| ★`inp.pnu` 가 오염될 수 있나 | `contracts/analysis.py:27` 원문 | ★**아니다.** `Field(pattern=r"^([0-9]{19})?$")` — **19자 ASCII 숫자 또는 빈값만**. 입력 경계는 이미 정본보다 강하다 |
| ★그럼 무엇이 오염될 수 있나 | `analysis_pipeline.py:206~215` 추적 | ★**`effective_pnu`** — `geocoded["pnu"]` 로 덮인다 |
| 지오코더 출력이 검증되나 | `vworld_geocoder.py:55,66` 원문 | ★**아니다.** `feats[0]["properties"]["pnu"]` 를 **그대로** 반환. 외부 응답이 곧 값이다 |
| 그 값이 어디까지 가나 | `analysis_pipeline.py:252` → `collect_land_card` → 어댑터 5벌 | **외부 유료/키 API 호출 인자**로 나간다 |
| 서브서비스가 정본을 임포트할 수 있나 | `Dockerfile` `COPY . /app` | ★**불가.** 이미지에 메인 api 가 **없다** → **미러를 만들 수밖에 없다** |
| 그 서비스에 pnu 유틸이 있나 | `git ls-files` | **0건**(`app/utils/` 디렉토리 자체가 없다) |

## 2. ★내 인계 라벨이 **두 번** 틀렸다

1. *"경쟁 SSOT 4곳"* → **7벌**. 손으로 센 목록이었다.
2. *"M-4 는 결함"* → ★**입력 경계는 이미 막혀 있다.** 앞 세션의 **「잠재」가 옳았다.**
   그러나 **구멍은 사라지지 않고 한 칸 옮겨진다** — 검증되는 것은 `inp.pnu` 이고,
   실제로 어댑터에 가는 것은 **`effective_pnu`(지오코더 출력)** 다.

> **「입력이 검증된다」와 「그 변수가 검증된다」는 다른 명제다.**

## 3. 변경 내용

1. `app/utils/pnu.py` **신설**(서브서비스) — 정본 `apps/api/app/utils/pnu.py` 의 미러.
   `^[0-9]{19}$` · **공백을 벗기지 않는다**(소비되는 문자열 그대로 판정).
2. **지오코더 출력을 입구에서 막는다** — `address_to_pnu` 가 비규격 PNU 를 받으면
   `pnu` 를 싣지 않고 **사유를 남긴다**(무언 실패 금지).
3. 길이 판정 **7벌 → `is_valid_pnu`**. ★특히 `analysis_pipeline.py:252` 의 `>= 19` —
   **길이만 보는 상한 없는 판정**이라 20자 쓰레기가 통과한다.
4. **미러 정합 락** — 두 파일의 판정이 **같은 입력 집합에서 같은 답**을 내는지 잠근다.
   ★`#944` 에서 *"정규식 텍스트가 같은데 의미가 달랐다(오직 실행만 갈랐다)"* 를 겪었다.

## 4. ★검증하지 못한 것

- **라이브에서 지오코더가 비규격 PNU 를 실제로 반환한 적이 있는지** — 미측정.
  (그 서비스의 로그 조회는 시도할 수 있으나, 없다고 해서 앞으로도 없는 것은 아니다.)
- **7벌 중 어느 것이 실제로 호출되는지의 빈도** — 미측정.
- `deliberation-ci.yml` 이 이 서비스를 태우므로 회귀는 CI 로 잡히지만,
  **라이브 동작 확증은 응답 델타가 없어 불가**할 수 있다(§배포 후 판정에서 다시 본다).

## 5. 되돌리기 경로

단일 커밋 revert. 데이터 변경 없음. ★다만 **지오코더 출력 거부**는 동작 변경이므로,
되돌리면 「비규격 PNU 를 그대로 쓰던 동작」으로 돌아간다.

## 6. 잠금 — 락 **33건** · 변이 **6/6 CAUGHT**

| 변이 | 결과 |
|---|---|
| 미러 정규식을 `\d` 로(유니코드 19자 통과) | CAUGHT |
| 미러가 `strip()` 후 판정(판정≠소비) | CAUGHT |
| 지오코더 입구 검증 제거 | CAUGHT |
| 지오코더 **사유** 제거(무언 실패) | CAUGHT |
| 어댑터 하나를 길이 판정으로 되돌림 | CAUGHT |
| 파이프라인 `>= 19` 로 되돌림 | CAUGHT |

락의 구성: ①**미러 정합**(두 구현을 **행위로** 비교 — 텍스트가 같아도 의미가 다를 수 있다)
②**공허 방지 대조군**(정본이 전부 True/False 를 내면 비교가 무의미하다)
③**파생형 래칫**(`ast` 로 `len(pnu) … 19` 형태를 전수 — 주석·독스트링에 안 뚫린다)
④**어댑터 전수 대조**(PNU 를 다루면서 정본을 안 쓰면 실패 — 새 어댑터가 자동 편입)
⑤**두 모집단**(정상 3종 통과 · 비규격 10종 거부 — 「전부 거부」가 만점이 되지 않게)
⑥**지오코더 음성 대조군**(정상 PNU 는 그대로 실린다 — 「전부 버리는」 구현과 구별)

## 7. 회귀 대조 (실측 · 같은 명령·같은 제외)

    origin/main   6 failed · 557 passed
    이 브랜치      6 failed · 590 passed
    실패 집합 comm 회귀 0 · 산술 557 + 신규 33 = 590 ✔

★`tests/services/test_reconcile_mirror.py` 는 **양쪽 다** `ModuleNotFoundError: celery` 로
수집 실패한다(로컬 환경). 같은 `--ignore` 로 양쪽을 재서 **모집단을 맞췄다**.
