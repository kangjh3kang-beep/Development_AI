# 수지분석 매출·지출 산정 — **없는 것을 만들기 전에, 있는 것이 왜 안 불리는지 먼저**

**요구(사용자, 2026-09-04)**
- 매출: ①분양사례조사·분양가조사로 **표준가** 산정 ②실거래 + **건축연한 가중치**로
  현실성 있는 **실효 분양가** + **직접입력**
- 지출: 토지비(실거래+가중치+직접입력) · 공사비(평당공사비 + **건축물·층수 가중치** + 직접입력)
  · 그 밖 항목 **표준 반영** + **항목별 직접입력**

---

## 0. 옵시디언 조회 결과 (계획 게이트 §0)

| 찾을 것 | 결과 |
|---|---|
| **이미 기각된 접근** | ★**셋 있다**(초판은 둘만 적었다 — 적대 검증이 셋째를 찾았고, **그것이 사용자 요구 ①을 정면으로 제약한다**). ③**분양사례를 산정 앵커로 쓰지 마라** — `suggest.py:266-270` 원문: *"★**용인 신봉동 과대표시 사고 교훈: 주변 분양가를 산정 앵커로 쓰면 럭셔리 분양가에 2배 과대된다.**"* → 청약홈은 **참고·교차검증 전용**이고 실거래 앵커는 유지한다. ★①②는 아래. ①**「금액 ÷ 면적」으로 평당가를 만들지 마라** — 라이브 71건 실측에서 원천이 **이미 평당단가로 정한 값을 역산**하는 것이고 **없는 가격차를 만든다**(같은 날 면적이 다른 3건이 소수 둘째 자리까지 동일 단가). ②**하드코딩→법규 실시간조회**는 **절반만 가능** — 표준건축비는 법제처 `admrul` + HWPX 추출 **성공**, 상하수도 단가는 **원천(조례)에 값이 없다**(별표 198개 중 텍스트 0). 커밋 `a4bc6902` *"HWP 파싱은 하지 않는다"* · `14e6abe9` *"전국폴백 날조값 제거"* |
| **같은 클래스의 앞선 결함** | 「미조회를 유효값으로 표현하면 결함」 · 「기준이 갈리면 숫자가 아니라 말이 틀린다」 · 「한 화면이 두 기준으로 말한다」 |
| **미결·부채** | `2026-08-26 수지분석 자동화 구축계획` 의 갭 6개. ★**재측정 결과 P0 둘(B1·B2)은 이미 해소**됐다(아래 P-확인). 남은 것은 **B6 검산층·파라미터 해소층·B5 지역테이블** |
| **이전 판단의 근거** | 위 구축계획의 4층 구조(파라미터 해소 / 법규 적용 / 계산 / 검산)와 *"새 트리거 기계가 필요 없다 — 이미 있고 수지만 공용 통로를 안 쓴다"* |

★**신선도 재확인(승계하지 않고 다시 쟀다)**

| 볼트 서술 | 재측정(2026-09-04) |
|---|---|
| `feasibility/` **19모듈** | **26모듈** — 늘었다 |
| B1 오케스트레이션이 공시지가·시도명 미전송 | **해소** — `lib/feasibility-seed.ts:siteDerivedFeasibilityFields` + `node-body-builders.ts:288` `sido_name` 전송. ★처방 이름이 계획서의 `buildFeasibilitySeed` 와 **달라서**, 이름으로 찾았으면 «미해소» 로 오판했다 |
| B2 `feasibility` 가 provenance 밖 | **해소** — `useProjectContextStore.ts:371` 에 `"feasibility"` 편입 |
| `feat/legacy-ledger` 미머지 | 브랜치는 **미머지지만 파일은 main 에 있다**(스쿼시 머지) · 소비처 배선 확인(`rough_scenario_report.py:657` · `LegacyLedgerTable.tsx`) |

---

## 1. 전제 표 — **측정값**(예상값 아님)

### A. 매출(분양가)

| # | 전제 | 확인 방법 | 실제 값 |
|---|---|---|---|
| A1 | 수지 **시드**의 분양가 출처 | `v2_feasibility.py:676` 함수 특정 | `baseline_feasibility()` → `get_regional_sale_price_per_pyeong()` = **하드코딩 지역 테이블**. `sources` 라벨 `"지역 시세 테이블(시장표준)"` |
| A2 | 실거래 기반 경로 존재 | `rough_feasibility_orchestrator.py:145-167` | **있다** — `_trade_per_pyeong(sigungu5, dong, prop_type)`. 우선순위 주석: `suggest_base_price` → MOLIT 직접 → 지역테이블(«추정·비실거래» 명시) |
| A3 | 그 경로가 **시드에 쓰이나** | 두 파일 교차 | ✘ **안 쓰인다.** 시드는 A1, rough 는 A2 — **같은 현장이 경로에 따라 다른 분양가** |
| A4 | `_trade_per_pyeong` 의 집계·가중치 | `suggest.py:77-78` | 8개월 창 · 동/시군구 **median** · **가중치 없음**(거리·시점·면적 decay 0건) |
| A5 | **건축연한 가중치** | pricing 4파일 + `regional_pricing.py` | **0건**. ★양성 대조군: 같은 파일 `평당` **23건** → 조회기 생존 |
| A6 | 분양사례(청약홈) | `presale_service.py` + 소비처 | **있다.** 소비처 = `market_report` · `quick_sales_survey` · `presale_monitor` — **수지 0건** |
| A7 | ★**비교사례·시점보정 엔진** | `app/services/market_precision/` | ★★**이미 있다** — `ComparableSet` · `TimeAdjustment`(R-ONE 지수 · `OBSERVED`/`UNKNOWN` 정직 마커 · **외삽 금지**) · `absorption` · `price_suggestion`. **소비처 = `sales/actions.py` 뿐, 수지 0건** |
| A8 | 분양가 **직접입력** | `_request_to_input:127` ↔ `ModuleInputForm.tsx` | ✅ **배선됨** — `avg_sale_price_per_pyeong` ↔ 「평당 분양가」 |
| A9 | 동·호 가중치 | `SalesPriceWeight` | **있다**(FLOOR/LINE/ASPECT/GROUP/CUSTOM) — 단 **분양 모듈 전용**, 표준가 산정 축이 아니다 |

### B. 지출

| # | 전제 | 확인 방법 | 실제 값 |
|---|---|---|---|
| B1 | 토지비 산식 | `land_cost_engine.py:21-44` | 공시지가 × `price_multiplier`(기본 **1.0**). 시드는 **1.1** 고정(`v2_feasibility.py:690`) |
| B2 | 토지비 **실거래** | 같은 파일 | ✘ **미사용** |
| B3 | 공시지가 미상 시 | `v2_feasibility.py:686-689` | **`1_500_000` 원/㎡ 로 채운다**(라벨 «표준 폴백(공시지가 미상)» + `confidence_penalty`) — ★**「모름」이 값을 입는다** |
| B4 | 토지비 **직접입력** | 프론트 필드 전수 | △ 「**시가반영배율**」만. **총액 override 없음** |
| B5 | 공사비 단가 | `unit_price_repository.py:_DIRECT_SQM_FALLBACK` | ₩/㎡ **7유형**(apartment 240만 … warehouse 120만). SSOT 일원화됨 |
| B6 | 공사비 **가중치** | `construction_cost_engine.py:29-143` | **있다** — 구조계수(RC/SRC) · **지하 +30%** · 조경 1.5% |
| B7 | 공사비 **직접입력** | `alternatives_engine.py:ALLOWED_OVERRIDE_KEYS` | ✘ 5키 전부 **물량축**(building_type·gfa·floor×2·structure). **총액 override 없음** |
| B8 | 그 밖 지출 **표준요율** | `construction_cost_engine.py:44-49` · `budget_template.py:49-76` | **있다** — 설계 4%·감리 3%·예비 5%·일반관리 3% + 분양대행·광고·보증·신탁 등 **14항목** |
| B9 | 그 밖 **직접입력** | 같은 파일 | △ **비율 파라미터만**. 항목별 **금액 override 없음** |
| B10 | 부담금 | `project_charges.py` 등 | **16종 코드화** + 3상태 게이트(미조회/없음/있음). 미조회면 **0** + `degraded_notes`. **수동 보충 경로 없음** |
| B11 | ★소방 | `unit_price_repository.py:80-88` | **법정 분리도급으로 명문화됨**(소방시설공사업법 §21②). ★조사 에이전트가 «미구현» 이라 보고했으나 **틀렸다** — 원문을 직접 봐서 갈랐다 |
| B12 | 프론트 수지 입력 필드 | `ModuleInputForm.tsx` 전수 | **11개**: 연면적·대지면적·중도금대출·시가반영배율·평당 분양가·평균 전용면적·총 세대수·지역 2·프로젝트명·본PF. → **공사비·기타 지출 입력 0개** |

### C. 성장루프(사용자 요구로 가동)

| 전제 | 확인 방법 | 실제 값 |
|---|---|---|
| 이 도메인 인사이트 존재 | 라이브 `/growth/insights?sort=created_at&limit=200` | **총 200건 · 이 주제 0건**. 종류: `latency_baseline` 182 · `latency_regression` 6 · `error_cluster` 6 · `recurring_verify_error` 4 · `fallback_rate` 2 |
| 조회기 생존 | **양성 대조군** `severity`/`insight`/`id` | **각 200/200** → 조회기 정상 |
| 해석 | — | 성장루프가 **운영지표만** 본다. **도메인 정확도 축이 아예 없다** — 그 자체가 발견이며 §5 에 반영 |

---

### D. ★계획서를 쓴 뒤 **라이브로 재서** 두 가지가 바뀌었다 (2026-09-04)

§3 이 *"괴리 크기를 재지 않았다"* 고 적은 것을 **잤다.** `/api/v1/external/transactions/apt`
(MOLIT 실거래, `deal_ym=202607`, `is_cancelled` 제외).

#### D-1 하드코딩 테이블이 **실거래보다 58~135% 낮다**

| 지역 | 하드코딩(만원/평) | 실거래 중위 | 괴리 | n |
|---|---|---|---|---|
| 강남구 | 5,500 | **12,907** | **+135%** | 159 |
| 강북구 | 2,000 | **3,159** | +58% | 131 |
| 노원구 | 2,200 | **3,618** | +64% | 720 |

★**과대해석 금지**: 이것은 **아파트 매매 시세**이지 **신축 분양가가 아니다**(분양가상한제·
위치·연식이 다르다). 그러므로 *"분양가가 2.4배여야 한다"* 로 읽으면 안 된다.
**읽을 수 있는 것은 방향과 자릿수**뿐이다 — 테이블이 **낡은 정도가 배수 단위**라는 것.

★**볼트의 「÷ 함정」은 이 데이터셋엔 적용되지 않는다** — 면적이 평의 정수배에 붙는 행이
**1/159 · 0/131 · 25/720** 이다(그 경고가 나온 데이터는 71/71 이었다).
**교훈을 범위 밖까지 끌어 쓰지 않는다.**

#### D-2 ★★**연식 효과가 단조가 아니다** — 단순 감가계수는 **반증됐다**

`build_year` **결측 0** (재료는 전건 존재). 연식대별 평당 중위(신축 대비):

| 연식 | 노원구 | 강남구 |
|---|---|---|
| 0~5년 | 1.00 (n=19) | 1.00 (n=8) |
| 6~10 | 0.82 (n=17) | 0.85 (n=13) |
| 11~20 | 0.60 (n=14) | 0.79 (n=31) |
| 21~30 | 0.62 (n=222) | 0.56 (n=43) |
| **31년+** | **0.78** (n=448) | **0.91** (n=64) |

★**31년+ 에서 도로 올라간다** — **재건축 기대 프리미엄**이다.
**단조 감가계수를 넣었으면 「구축일수록 싸다」로 틀렸을 것이고, 그 구간이 바로
개발사업의 주 대상이다.** 가장 중요한 자리에서 가장 크게 틀린다.

★**교란 미제거**: 코호트마다 평균 전용면적이 다르다(노원 신축 74.4㎡ ↔ 구축 63.0㎡ ·
강남 81.3 ↔ 89.6). 위 배율은 **연식만의 효과가 아니다.** 표본도 얇다(0~5년 n=8·19).

**→ P2 를 다시 쓴다**(아래 §2 P2 참조): «감가계수» 가 아니라 **지역별 경험 곡선**이거나,
확보 못 하면 **`UNKNOWN` + 미보정**이다. **단조 가정을 코드에 넣지 않는다.**

---

### E. ★★적대 검증이 **전제 셋을 반증했다** — 전부 내가 재측정해 확인했다 (REJECT → 개정)

계획서는 *"측정값만 적었다"* 고 주장했다. **그 주장이 거짓이었다.** 세 축이 계획의 P0·P2·P3 을
지탱하고 있었고 셋 다 틀렸다.

#### E-1 (초판 B7·B9 폐기) — **내가 다른 기능의 객체를 쟀다**

초판: *"공사비 직접입력 ✘ — `alternatives_engine.ALLOWED_OVERRIDE_KEYS` 5키는 전부 물량축"*.

`ALLOWED_OVERRIDE_KEYS` 는 **대안설계 비교 엔진**의 것이고 **수지 `/calculate` 경로가 아니다.**
실제 통로는 `ModuleInput.params` 이고 **엔드투엔드로 이미 살아 있다**(재측정):

    web/store/use-feasibility-v2-store.ts   construction_cost_override_won 생산
    app/routers/v2_feasibility.py:193       inp.params["construction_cost_override_won"] = …
    modules/common/cost_blocks.py:50        override = inp.params.get(…)
                                            → source: "cost_analysis_override"

`params.get("…")` 파생 계수 = **28키**. 초판이 «신규» 라 적은 것 중 **이미 있는 것**:
`construction_cost_override_won`(총액) · `unit_cost_per_sqm`(㎡당) · `marketing_cost_won` ·
`management_cost_won` · `reserve_cost_won` · `compensation_won`.

> ★**§검증 규율의 그 한 문장에 정확히 걸렸다** — *"내가 방금 잰 것이, 사용자가 실제로 쓰는
> 그것인가?"* 나는 **이름이 비슷한 다른 화이트리스트**를 재고 «없다» 고 썼다.

#### E-2 (초판 A5 폐기) — 건축연한 축은 **2종 실재**하고, P2 는 **+15% 이중계상**을 만들 뻔했다

초판: *"건축연한 가중치 0건 — 이것만이 진짜 신규"*. **거짓이다.**

| 실재하는 축 | 좌표 | 성질 |
|---|---|---|
| **신축 프리미엄** `_PREMIUM = {conservative 1.05, base **1.15**, aggressive 1.25}` | `suggest.py:35` | `rough_…:184` 에서 `med × 전용률 × premium` 으로 **이미 곱해진다** — 실거래(기존 재고) → **신축 기준 환산** |
| **잔가율** (1 − 경과/내용연수, 하한 20%) | `desk_appraisal_service.py:53-65` | 감정평가 **원가법**의 건물 잔존가치 |

★**P2 가 `ComparableSet` 에서 «신축 기준으로 환산» 하면 1.15 위에 또 곱해진다 = 분양가 +15%
과대.** 수지 산출물이라 **돈이 걸린 축**이다.

★★**계획서가 스스로 모순이었다**: A5 는 «0건», 같은 문서 P2 두 번째 불릿은
*"`desk_appraisal_service._building_value` 의 기존 감가 로직(**이미 있다** — 형제를 먼저 본다)"*.
**한 문서가 두 답을 말했고, 낙관적인 쪽(0건·진짜 신규)이 커밋 헤드라인에 갔다.**

**→ P2 는 「신설」이 아니라 「`_PREMIUM` 평면 상수를 사례 연식의 함수로 승격」**이다.
그리고 D-2 실측(단조 아님)이 그 함수의 **모양**을 제약한다.

#### E-3 (초판 §5 P0 락 폐기) — **축 모집단이 2인데 실제는 5**다

`get_regional_sale_price_per_pyeong` 소비처(파생 · 테스트 제외):

    app/routers/v2_feasibility.py            (시드)
    app/routers/project_dashboard.py         expected_revenue 산정
    app/services/feasibility/feasibility_service_v2.py
    app/services/feasibility/market_revaluation_service.py
    app/services/feasibility/regional_pricing.py   (정의)
    + services/pipeline/project_pipeline.py:2151 (market_revaluation 경유 · 간접)

**2경로만 수렴시키면 나머지가 여전히 다른 값을 낸다 — 그런데 락은 초록이다.**
★그리고 `is` 동일성은 **원리적으로 공허**하다: 두 모듈이 같은 심볼을 import 만 해도 참이고,
시드가 `if …: shared() else: get_regional_…()` 로 분기하면 **락 초록·값 갈림**이 성립한다.

#### E-4 그 밖에 재측정으로 갈린 것

| 초판 | 재측정 |
|---|---|
| B8 «그 밖 지출 표준요율 14항목 있음» | ✘ `budget_template` 은 **UI 프리셋**이고 원문이 *"★무목업 — 금액 없이 라벨·산정근거만"*. 계산 경로의 실제 기타경비는 `cost_blocks.py:148-151` 의 **`(토지+공사) × 7%` 단일 덩어리**(`auto_estimated`) |
| §3·§5 «표본 하한이 없다 → 신규 과제» | ✘ **이미 있다** — `rough_…:69 _MIN_TRADE_SAMPLES = 5`, 미달 시 `None` → 지역테이블 폴백 + 라벨. P0 이 리졸버를 추출하면 **딸려 온다**. 신규 상수를 또 만들면 **이중 SSOT** |
| §4 «B3 처방은 시드 함수 한 곳의 분기 추가 = 1커밋» | ✘ `app/schemas/feasibility_v2.py:17·91·140` 이 `official_price_per_sqm: float = 0`(**Optional 아님**). `None` 으로 바꾸면 `cost_blocks.py:169` · `v2_feasibility.py:695` 가 `TypeError`. **스키마 3곳 + 세금엔진 + 자기자본 기본값**을 함께 고쳐야 한다 |
| B3 무성 형제 | ★**있다** — `feasibility_service_v2.py:678` `official_price_per_sqm or 1_500_000` 에 **라벨도 `confidence_penalty` 도 없다**(라우터판보다 나쁘다). 소스 스캔이 라우터만 보면 **초록인 채 형제가 산다** |
| B12 «프론트 입력 11개» | ✘ 목록형 저계수 — 파생하면 **19키**(`official_price_per_sqm`·`sale_ratio` 포함). «공사비·기타 입력 0개» 결론의 기반이 약해진다 |
| C «인사이트 총 200건» | ✘ **200 은 총계가 아니라 `limit`** 이다(`limit=500` → 500건, 거기서 `prompt_candidate` 2건이 새로 나타난다). ★**결론은 살았다** — 500건 전수에서 `분양·수지·pricing·공사비` 각 0. **틀린 것은 근거의 표기이지 결론이 아니다** |
| A4 좌표 | ✘ `suggest.py:77-78` 은 `_cost_validation`. 집계는 **`:183-262`**. 내용(8개월·median·가중치 없음)은 옳고 **좌표만 오귀속**. 단 `:246-251` 에 `_PP_MIN/_PP_MAX` **이상치 제외**가 있어 «가공 0건» 은 아니다 |

#### E-5 ★★**놓친 형제 — 가장 강한 것을 SSOT 후보에서 뺐다**

`app/services/feasibility/market_revaluation_service.py` = **F1 다중출처 신뢰도 가중 블렌딩 엔진**
(`regional` w0.35 + `molit_real` w0.65 *"가장 강한 시장신호"* + `avm` MLflow 등록모델).

★독스트링이 **`확장 예정: 청약홈 분양가, 시세지수 추세`** 라고 적는다 —
**사용자 요구 ①(분양사례)과 시점보정이 이 엔진의 선언된 로드맵이다.**
`project_pipeline.py:2151` 이 이미 «있으면 우선» 으로 소비한다.

★초판 P0 은 이 엔진을 **한 번도 언급하지 않고** rough 의 3단 우선순위를 SSOT 로 추출하겠다고
했다. 볼트에 기록된 **가장 비싼 실패**(*"형제 중 가장 약한 것을 SSOT 로 골랐다"*)와 같은 자리다.

> ★**이 계획의 최대 강점은 «없는 것을 만들기 전에 있는 것을 본다» 였고,
> 반려의 전부는 그 원칙을 계획서 자신에게 적용하지 않은 것이다.**

---

### F. ★계획이 **「착수 전 필수」라 지정한 측정**을 했다 — 두 경로가 실제로 갈린다 (라이브)

주소 **`서울특별시 노원구 상계동 771`** 로 두 엔드포인트를 나란히 태웠다(2026-09-04).

| 경로 | 분양가 | 근거(응답 원문) | 소요 |
|---|---|---|---|
| `POST /api/v2/feasibility/baseline` (시드) | **2,200만원/평** | `"지역 시세 테이블(시장표준)"` | 즉시 |
| `POST /api/v2/feasibility/rough-scenario` | **2,945만원/평** | `"주변 실거래(MOLIT) 동 중앙값 3,428만원/평(전용, 표본 2,230건·최근 8개월) × 전용률 0.747 × 신축 프리미엄 1.15 → 공급 평당가"` | **7초** |

**괴리 = ×1.34 (+34%)** — 같은 주소, 같은 날, 다른 엔드포인트.

★**A3 이 확증됐다.** 초판이 «추론» 으로 적었던 것이 **라이브 관측**이 됐다.

★**E-2 도 라이브로 확증**: 응답 근거 문자열에 **`신축 프리미엄 1.15`** 가 그대로 찍힌다.
P2 가 `ComparableSet` 에서 또 환산했으면 **이 자리 위에 곱해진다.**

★**부수 관측 셋**
- 표본 **2,230건** — `_MIN_TRADE_SAMPLES=5` 를 크게 넘는다(이 주소에선 하한이 구속하지 않는다)
- rough 응답이 `legacy_ledger.coverage.basis_pct = **100.0**`(23항목 전건 근거) — 볼트가
  «브랜치 `feat/legacy-ledger`» 라 적은 원장이 **라이브에서 돌고 있다**
- 소요 **7초**(성장루프 경고의 p95 13.8초보다 낮다 — 단 **표본 1**이라 분포를 말하지 않는다)

★**미측정으로 남는 것**: 이 ×1.34 가 **다른 주소에서도 성립하는지**(N=1). 그리고
**어느 쪽이 옳은지는 이 측정으로 정해지지 않는다** — 갈린다는 사실만 확증됐다.

---

## 2. 변경 내용 — **개정판**(적대 검증 반영)

> ★**개정의 요지**: 초판은 요구 6개 중 «4개가 이미 있다» 고 했다. 재측정하니 **더 많이 있다.**
> 실제 미충족은 **둘로 좁혀진다** — ①**5경로가 서로 다른 분양가를 낸다** ②**있는 것이 UI 에 안 보인다**.
> 새로 만들 것은 **연식 함수 하나**뿐이고, 그것도 신설이 아니라 **평면 상수의 승격**이다.

### ★「완료」의 측정 가능한 정의 (전역 지침 §완결 가능한 구현계획)

| 단계 | 완료 지표 | 도달 가능한가 |
|---|---|---|
| P0 | **동일 주소 N=20 에서 5경로 분양가의 상대표준편차 = 0** (라벨까지 동일) | ◎ 결정론적 — 같은 함수를 부르면 0 |
| P0 | 실거래 표본 `n ≥ 5` 인 주소에서 **지역테이블 미사용 비율 = 100%** | ◎ |
| P1 | 수지 응답의 `market_precision.comparables` **사례 수 ≥ 1 이고 `time_adjustment.status` 가 실린 비율** | △ R-ONE 키 없으면 `UNKNOWN` — **그때도 status 는 실려야** 한다(그것이 지표) |
| P2 | 연식 코호트 5구간에서 **보정계수가 D-2 실측의 부호와 일치**(31년+ ≥ 21~30) | ◎ |
| P2 | 연식 결측 사례의 **미보정 비율 = 100%** | ◎ |
| P3 | `params` 28키 중 **UI 노출 키 / 전체** — 현재값을 먼저 재고 그 수를 올린다 | ◎ 파생 계수 |
| — | ★**`judged`(판정)와 `coverage`(판정했거나 왜 못 하는지 말함)를 둘 다 싣는다.** 하나만 보이면 오독된다 | — |

---

### P0 — 분양가 리졸버 **단일화** (요구 ①②·토지비)

**문제(개정)**: **5경로**(+파이프라인 간접 1)가 각자 분양가를 만든다. 초판이 «2경로» 라 한 것은
목록형 저계수였다.

**★SSOT 후보를 값으로 판정한다 — 이름으로 고르지 않는다**

| 후보 | 성질 | 초판 판정 |
|---|---|---|
| `rough_…_resolve_sale_price_per_pyeong` | 3단 우선순위 + `_MIN_TRADE_SAMPLES=5` + degraded 라벨 | 초판이 고름 |
| ★`market_revaluation_service` | **다중출처 신뢰도 가중 블렌딩**(regional 0.35 + molit_real 0.65 + avm) · 독스트링에 **청약홈 확장 예정** | **초판이 누락** |

**착수 전 필수**: 두 리졸버를 **같은 주소 N개로 나란히 태워** 값·라벨·지연을 표로 만든다.
**그 표 없이 고르지 않는다.** (볼트: *"형제 중 가장 약한 것을 SSOT 로 골랐다"* 가 가장 비싼 실패였다)

**토지비도 같은 축** — ★초판이 **가중치를 빠뜨렸다**(사용자 요구는 «실거래+**가중치**+직접입력»):
- 실거래 median(있으면) → 공시지가(폴백) → 라벨
- **가중치 축**: 지목·용도지역·면적대·시점. ★**출처 없는 계수를 만들지 않는다** —
  확보 못 한 축은 **적용하지 않고 그 사실을 라벨에 남긴다**
- `1_500_000` 폴백 **제거** — ★단 **§4 참조: 1커밋이 아니다**

### P1 — `market_precision` 을 수지에 **플래그로** 켠다 (요구 ①)

★초판은 «새 배선» 이라 했으나 재측정 결과 **기존 opt-in 플래그를 켜는 일**이다:
`suggest_base_price(..., collect_cases=True)` + `assemble_market_precision_from_db(...)`
(*"편의 진입점(신규 호출부용)"* 이라고 원문이 적는다). **재구현 0.**

★★**분양사례는 앵커로 승격하지 않는다**(§0 기각 ③ — 용인 신봉동 2배 과대).
청약홈은 **교차검증 표면**이고 앵커는 실거래로 유지한다. **이것을 락으로 잠근다.**

### P2 — `_PREMIUM` **평면 상수를 연식 함수로 승격** (요구 ②)

★«신설» 이 아니다(E-2). 지금은 `_PREMIUM["base"] = 1.15` 가 **모든 사례에 평면으로** 곱해진다.

- 승격: 사례별 `build_year` → 신축 환산 배율. **자리는 그대로**(`rough_…:184`)
- ★★**배타 락**: 승격한 자리와 `ComparableSet` 보정 자리가 **동시에 곱해지지 않는다**
  (그렇지 않으면 **+15% 이중계상** — 돈이 걸린 축)
- ★**단조 가정 금지**(D-2 실측 반증) · **교란 제거 못 하면 쓰지 않는다**
- ★계수 출처 후보에서 **`desk_appraisal` 잔가율은 기본값으로 쓰지 않는다** —
  그것은 **원가법의 건물 잔존가치**이고 여기는 **거래사례비교법**이다. 기준이 다르다
  (볼트: *"기준이 갈리면 숫자가 아니라 말이 틀린다"*)

### P3 — **있는 것을 UI 로 꺼낸다**(신설 아님) (요구: 항목별 직접입력)

★초판의 «override 신설» 은 **이미 있는 계약의 재구현**이었다(E-1).

**재정의**: `params` **28키**를 파생으로 세고, 그중 **UI 미노출 키**를 찾아 노출한다.
현재 프론트 입력은 **19키** — 차집합이 이 단계의 실제 범위다.

★★**`{value, source}` 재형태화를 하지 않는다** — `cost_blocks.py:95` 가
`marketing + management + reserve` 로 **더하므로** dict 를 넣으면 `TypeError` → 500.
필요하면 **병행 키**(`params_source`)로 **가산**한다(기존 계약 무변경).

★기타경비는 **7% 단일 덩어리**다(E-4). 항목별로 쪼개려면 **`compute_other_cost` 의 계약을
바꾸는 별건**이고, 그 전에 **7% 가 어디서 왔는지**부터 재야 한다(현재 근거 미상).

★부담금 수동 보충은 **`surveyed` 를 승격시키지 않는다**(3상태 게이트의 뜻).

---

## 3. ★검증하지 못한 것 (개정)

- **두 SSOT 후보의 값 차이 미측정** — P0 의 전제. **DB·MOLIT 키가 필요**하다
- **P0 의 지연·쿼터 비용 미측정.** ★라이브 성장루프가 이미 경고한다:
  `/api/v2/feasibility/rough-scenario p95 **13,846ms**`(이전 baseline 8,869ms, 표본 48).
  그 13.8초 경로의 리졸버를 **빠른 시드**(`baseline`, `enforce_llm_quota` 걸린 대화형 진입점)에
  이식하는 것이다. **지연 예산을 먼저 정해야 한다**
- **연식 계수의 교란(면적 구성) 미제거** — 제거 못 하면 P2 를 **적용하지 않는다**
- **하드코딩↔실거래 괴리의 「분양가 축」 미측정** — D-1 은 **매매 시세**다
- **7% 소프트비의 근거 미상**
- **`market_precision` 이 수지 입력 형태와 맞는지 미확인** — P1 이 P2 의 선행조건이므로
  **미확인 위에 두 단계가 서 있다.** P1 착수 전에 형태부터 맞춰 본다
- **`limit=1000 → 0`** 이 서버 상한인지 오류인지 **미측정**(그 자체가 무성 실패 후보)

## 4. 되돌리기 (개정)

| 단계 | 되돌리기 | ★초판이 틀린 곳 |
|---|---|---|
| P0 (리졸버 단일화) | 분기 추가 — 1커밋 | — |
| P0 (`1_500_000` 제거) | ★**1커밋 아님** — 스키마 3곳 타입(`float = 0` → Optional) + `cost_blocks.py:169` + `v2_feasibility.py:695` 자기자본 + **무성 형제 `feasibility_service_v2.py:678`** 를 함께 | 초판은 «시드 함수 한 곳» 이라 적었다 |
| P1 | 플래그 off — 1커밋 | 초판에 **없었다** |
| P2 | 연식 함수를 상수 1.15 로 되돌림 — 1커밋 | 초판에 **없었다** |
| P3 | 스키마 추가만(옵션) | — |

★**중간 상태 회귀 판정**(전역 §E21): **P0 만 배포된 상태는 5경로 중 일부만 수렴한 상태**다.
배포 전에 «부분 수렴이 현행보다 나쁘지 않은가» 를 값으로 확인한다.

## 5. 잠금 — 각 단계와 **같은 커밋에**, 그리고 **변이로 CAUGHT 를 확인**

| 단계 | 락 | 왜 이 축인가 |
|---|---|---|
| P0 | ★**파생형 소비처 집합**(`grep -l` 로 뽑은 전 경로) × **행위 단언**(같은 주소 → 같은 값·같은 라벨). **`is` 동일성을 쓰지 않는다** | E-3: import 만 해도 `is` 는 참이고 분기하면 값이 갈린다 = 공허 |
| P0 | 실거래 있음 → 실거래값 ↔ 없음 → 지역테이블 **라벨까지** (두 모집단) | «같은 현장 다른 값» 이 결함이므로 축은 일치 |
| P0 | `1_500_000` **부재** + 미상 시 라벨. ★**형제 포함 전수**(라우터 + `feasibility_service_v2`) | 소스 스캔이 한쪽만 보면 초록인 채 형제가 산다 |
| P0 | 표본 하한은 **`_MIN_TRADE_SAMPLES` 를 참조**한다(새 상수 금지) | 이중 SSOT 방지 |
| P1 | `market_precision` 이 **사례 수 ≥ 1 · `time_adjustment.status` 실림**(존재 아님) | 존재 검사는 빈 껍데기를 통과시킨다 |
| P1 | ★**청약홈이 앵커로 승격되지 않는다** — 분양사례만 있고 실거래가 없을 때 **가격이 그것으로 결정되지 않음**을 단언 | §0 기각 ③(용인 신봉동 2배) |
| P2 | ★**배타**: 연식 보정과 `_PREMIUM` 이 **동시에 곱해지지 않는다**(같은 사례에서 배율 1회) | +15% 이중계상 = 돈 |
| P2 | **단조 금지**: 31년+ 가 21~30 보다 낮은 계수표를 **거부** | D-2 실측 반증 |
| P2 | 연식 결측 → **`UNKNOWN` + 미보정**(음성 대조군: 연식 있으면 보정됨) | 방향 있는 결함엔 방향 있는 단언 |
| P3 | override 가 **결과를 바꾼다**(키 존재 아님) + 재실행 후 **남는다** + ★음성 대조군: 자동값은 갱신된다 | 「필드만 있고 소비처 0」 |
| P3 | ★**`params` 형태 불변**: 기존 6키가 **스칼라**임을 단언(dict 화 금지) | `cost_blocks.py:95` 합산이 `TypeError` → 500 |
| P3 | 부담금 수동 보충이 `surveyed` 를 **승격시키지 않는다** | 3상태 게이트의 뜻 |

### ★하지 않기로 한 것 — **변이로 잠근다**(산문으로 두지 않는다)

| 금지 | 변이 | 기대 |
|---|---|---|
| 상하수도 단가를 조례에서 긁는다 | 조례 조회 호출 추가 | CAUGHT |
| 평당가를 「금액÷면적」으로 만든다 | 나눗셈 산식 주입 | CAUGHT |
| 청약홈을 앵커로 쓴다 | 분양사례를 가격 결정에 배선 | CAUGHT |
| 연식 계수를 단조로 만든다 | 31년+ 계수를 21~30 아래로 | CAUGHT |
| `params` 값을 dict 로 바꾼다 | 스칼라 → `{value, source}` | CAUGHT |

★**변이를 넣어 CAUGHT 를 확인한 출력을 PR 본문에 싣는다.** 확인 없이 «잠갔다» 고 쓰지 않는다.
