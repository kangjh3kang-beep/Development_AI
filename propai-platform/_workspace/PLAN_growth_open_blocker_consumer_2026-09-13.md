# 계획 — 대시보드가 **「왜 아직 open 인가」를 읽는다**(소비처 0 해소)

- 세션 development-ai-bd · 절대형 서명 `sid=536becdd`
- 브랜치 `fix/growth-open-blocker-consumer` · base `b200f6c51`
- 레인: 보드 `growth-open-blocker-consumer` (web 축). ★`#1039`(`sid=68ed1a1d` · **MERGED**)가
  `.todo` 로 선언한 부채이며 **api 는 이미 착지**했다. 저자에게 직접 알렸다.

---

## §0 옵시디언 · 저장소 회상 (착수 전)

| 찾을 것 | 결과 |
|---|---|
| **이미 기각된 접근** | **있다** — 임계 자동완화(`threshold_relax`)로 open 을 줄이는 길은 이 저장소가 **기각**했다(무트래픽 스택이 프로덕션 임계를 바꾼 사고). 인사이트의 `open_blocker_reason` **자신이 그렇게 적고 있다**. ⇒ 이 PR 은 **수를 줄이지 않고 보이게만** 한다 |
| **같은 클래스의 앞선 결함** | `heal-log-blocked-consumer.todo.test.ts` — **같은 날 같은 이유**로 같은 형식을 쓴 형제 부채(아직 `todo`) |
| **미결·부채** | 이 PR 이 갚는 그 `.todo` 두 항목 |
| **이전 판단의 근거** | `#1039` 리뷰 MAJOR-3: *"`HEAL_UNHANDLED_REASONS` 에 사유가 있는데 프로덕션 소비처 0"* 을 고치면서 **소비처 0 을 한 층 위로 옮겼다** |

---

## §1 전제 표 — *확인 방법* 과 **실제 측정값**

측정 트리: `DAI_gblk` · base `b200f6c51` · 라이브 조회 2026-09-13T00:4xZ(admin)

| # | 전제 | 확인 방법 | **측정값** |
|---|---|---|---|
| P1 | 대시보드가 지금 blocker 를 렌더하는가 | `grep -n open_blocker GrowthDashboard.tsx` | **주석 2줄 + 타입 2줄뿐 · 렌더 0** |
| P2 | 라이브 `open` 모집단 | `status=open` + offset 페이지네이션 | **선언 `total`=707 · 수집 707행 · 고유 705** (2026-09-13T00:54:5xZ) ⇒ ★**전수가 아니다**(아래 P2-a) |
| P2-a | ★그 조회의 함정 **셋** | 실측(불변식 두 개를 같이 찍음) | ①`limit` 상한 **500** — 한 번 조회는 **절단**(처음 **173** 으로 오측정) ②`created_at` 내림차순 표에 **새 행이 위로 들어와** offset 이 밀린다 ⇒ **같은 행을 두 번** 집고(`고유 705 ≤ 수집 707` ①통과) **맨 위 새 2행은 아무도 못 준다**(`고유 705 ≠ total 707` ★②**FAIL**) ③**`total` 자체가 시점값**이라 계속 자란다 |
| P2-b | ★★**중복과 누락은 같은 사건의 양면**이다 | 위 ②의 기전 | **dedupe 만으로는 전수가 되지 않는다.** ①만 찍으면 «깨끗해 보이는 불완전»이 통과한다. ⇒ 전수가 필요하면 **①②를 둘 다** 만족할 때까지 재조회하거나 **커서**로 넘긴다. 이 정정은 `sid=68ed1a1d` 가 **자기 조언을 스스로 뒤집어** 준 것이고, 나는 **직접 재서 확인**했다(②FAIL 재현) |
| P3 | 그중 구조적으로 못 닫는 것 | 블로커 조합 집계(고유 705 기준 · ★전수 아님) | `window_expired` 포함 **693 / 705 (98.3%)** · 창 안 **12**. ★비율은 2행 누락에 **실질적으로 둔감**하지만, 절대값을 인용할 땐 **`total`·측정 시각·불변식 결과**를 같이 적는다 |
| P4 | 사람이 봐야 할 것이 묻혀 있는가 | `severity=critical&status=open` | **10건이 19~48일 전** (feasibility 폴백률 100% · `/tiles/vworld/wms` 503 **시간당 2,349건** …) |
| P5 | `#1039` 계약이 라이브에서 지켜지는가 | `open` 인데 `open_blockers=None` 인 건수 | **0건** ◎ |
| P6 | **`[]`(차단 사유 없음)가 라이브에 있는가** | 블로커 조합 집계 | **0건** ⇒ 라이브만으로 검증하면 **공허**하다. 합성 픽스처 필수. ★**이 결론은 P2-a 의 절단·누락에 영향받지 않는다** — **0 은 절단으로 생기는 값이 아니기 때문**이다(분모가 705든 707이든 같다) |
| P6-a | 왜 0건인가 | `sid=68ed1a1d` 실측(★그분 측정 · 내 관측 아님) | `growth_analysis.axes` 가 `fal 0/0 lat 1/9 pay 0/1 qua 0/0` — **치유 대상 타입의 입력이 0**(LLM 호출 147시간째 0건 · `/ai/status` available) ⇒ **사건 미발생**이지 배선 결함이 아니다 |
| P7 | 필드가 **항상** 실리는가 | API 스키마 원문 | `open_blockers: list[str] \| None = None` ⇒ 현행 api 는 항상 실음. **단 web 은 api 와 따로 배포**되므로 구판 api·mock 에서는 **부재(`undefined`)가 도달 가능** |

---

## §2 변경 내용 · **회귀가 아닌 근거**

★**상태는 셋이 아니라 넷이다.** `.todo` 는 「빈 배열 / null」 둘만 말하는데, 타입이
`open_blockers?: string[] | null` 이라 **부재(`undefined`)** 가 따로 있다(P7 로 도달 가능).

| 값 | 뜻 | 화면 |
|---|---|---|
| `undefined` | **API 가 말하지 않았다**(구판 api · mock) | **「판정 정보 없음」** — ★「차단 없음」으로 그리면 **모르는 것을 안다고 주장**하는 것이다 |
| `null` | 열려 있지 않음(판정 대상 아님) | **아무것도 안 그린다** |
| `[]` | 후보 술어를 **전부 통과** | **「차단 사유 없음」** ★★«치유 대기/예정»은 **쓰지 않는다** — `open_blockers()` 가 **안 재는 것이 셋**(`LIMIT 200` 절단·메타가드·닫기 실패)이라 화면이 **완료를 약속**하게 된다(필드 저자 `sid=68ed1a1d` 의 명시 제약) |
| `[...]` | 막혀 있음 | **코드 라벨 + `open_blocker_reason` 전문** |

- C1 카드에 blocker 블록 추가(`recommended_action` 아래). **네 상태를 각각 다른 출력**으로.
★어휘는 필드 저자와 합의했다: **「차단 사유 없음」**. 다른 말을 고르더라도
**«치유를 약속하지 않는다»는 제약**은 지킨다 — 그 이유를 여기 적어 다음 사람이 안 되밟게 한다.

- C2 코드→한국어 라벨 맵(`type_not_handled`·`window_expired`·`action_not_healable` …).
  ★**모르는 코드는 코드 그대로** 보여 준다(목록이 상한이 되지 않게).
- C3 `open_blocker_reason` 을 **자르지 않고** 보여 준다 — 그 문장이 이 PR 의 존재 이유다.

**회귀가 아닌 근거**: 추가만 한다. 기존 렌더·`ack`·집계(`actionable_counts`)·API 호출 **불변**.

---

## §3 ★검증하지 못한 것

1. **`[]` 상태의 라이브 확증** — 라이브에 0건이라(P6) **합성으로만** 검증한다.
   이유는 「장치 부재」가 아니라 **「사건 미발생」**이다(P6-a — ★그 근거는 `sid=68ed1a1d` 의
   측정이고 **내가 재지 않았다**. 남의 측정을 내 관측으로 적지 않는다).
2. **이 변경이 운영자의 행동을 바꾸는가** — 미측정. 이 PR 은 **보이게** 할 뿐이고, 707건을
   줄이지 않는다. 줄이는 길(임계 완화)은 **기각된 길**이다(§0).
3. **형제 부채**(`heal-log-blocked-consumer`)는 **이 PR 범위 밖**이다 — 같은 형식으로 남아 있다.
4. `undefined` 가 **실제 프로덕션에서** 나온 적이 있는지 미측정(도달 경로만 확인).

---

## §4 되돌리기 경로

`git revert <머지커밋>` 한 번. **web 단독 · api 무변경 · DB 없음.**
배포는 web 축이 필요하다(런타임 델타 > 0).

---

## §5 잠금 — 이 변경을 지키는 검사

| 잠글 것 | 검사 |
|---|---|
| 네 상태가 **각각 다른 출력** | 같은 실행에서 네 픽스처를 렌더해 **출력이 서로 다름**을 단언(한 칸으로 뭉치면 실패) |
| `undefined` 를 **「차단 없음」으로 읽지 않는다** | `undefined` 렌더에 「차단 없음」 문구가 **없음**을 단언 + 대조군(`[]` 에는 **있음**) |
| `null` 은 **아무것도 안 그린다** | blocker 블록 부재 단언 + 대조군(다른 셋은 존재) |
| 사유 문장이 **잘리지 않는다** | 긴 `open_blocker_reason` 전문이 DOM 에 있음을 단언 |
| 모르는 코드가 **사라지지 않는다** | 미등록 코드가 **코드 그대로** 나오는지(목록형 상한 방지) |
| ★공허 방지 | 각 단언 앞에 **대상이 실제로 렌더됐는지** 선단언 |

★락은 **소스 grep 이 아니라 렌더 결과**를 본다(주석 처리 변이에 뚫리지 않게).

## §6 변이 감사 (9조 §2 — `base:` 줄과 함께)

★**이번엔 기계가 골랐다** — 오늘 머지된 `#1038` 덕분에 프론트도 `--tests` 만 주면 감사된다.

    $ python scripts/mutate_changed.py --base origin/main \
        --tests propai-platform/apps/web/components/settings/__tests__/GrowthDashboard.openBlockers.test.tsx --max 300
      base: origin/main → b200f6c51 (공통 조상 · 남의 커밋 제외)

| 판 | `::AUDIT=` |
|---|---|
| 1차 | `generated=15 judged=15 caught=4 survived=11 undecided=0 skipped=0 truncated=0 scoped_out_files=0` |
| **락 추가 후** | `generated=15 judged=15 caught=9 **survived=6** …` |

### ★기계가 잡은 진짜 구멍 — `BLOCKER_LABELS` 가 **장식**이었다

맵에서 한 줄을 지워도(또는 문구를 바꿔도) **초록**이었다. 내 락은 *미등록 코드*만 보고
**등록된 코드가 사람 말로 나오는지**는 안 봤다 — 지우면 화면이 **원시 코드**로 떨어지는데
아무도 안 물었다. ⇒ **두 모집단**으로 잠갔다(등록=코드와 다름 / 미등록=코드 그대로).
재판정 **338·339·340 전부 `kill`**.
★손으로 골랐으면 못 봤다. 나는 *미등록* 쪽만 생각하고 있었다.

### 설명 가능한 생존 **6건** (점수 부풀리기 방지 — 코드에 적었다)

전부 **표현**이다: `className` 문자열 변경 4(365·379·388·397) · `className` 줄삭제 1(397) ·
`key={code}` 줄삭제 1(395). `key` 는 React 조정 힌트라 렌더 결과가 같고 `className` 은 스타일이다.
**계약이 아니라** 단언하면 다듬을 때마다 깨지는 취약한 락이 된다.
★그 판단을 **컴포넌트 안에 주석으로** 남겼다(다음 사람이 «왜 안 잠갔나» 를 되묻지 않게).
★★한국어 문구 자체(「판정 정보 없음」 등)도 **일부러 단언하지 않았다** — 잠근 것은
**구별**이다(네 출력이 서로 다름 + `undefined` 에 「차단 사유 없음」이 **없음**). 문구는 표현이다.

### 회귀 — **통과 수**로 대조

    $ cd propai-platform/apps/web && npx vitest run components/settings/__tests__

| | passed | todo | 합계 |
|---|---|---|---|
| base `b200f6c51` | 139 | 9 | 148 |
| 이 브랜치 | **150** | **7** | 157 |

**+11 passed · −2 todo** = 추가한 락 **11** · 상환한 `.todo` **2** 와 정확히 일치.
`tsc`: 기준선과 **오류 집합 동일**(5건 전부 `leaflet` 미설치 — 환경). `eslint` rc=**0**.

★**첫 회귀 실행에서 1건 실패가 한 번** 났고 **이후 3회 재현되지 않았다**. 어느 테스트였는지는
출력을 남기지 않아 **미측정**이다 — 「flaky」라고 부를 근거가 아직 없다.
