# 계획 — 보류 **사유가 응답에 실린 적이 없다** (2026-08-26)

## 0. 옵시디언 조회 결과

`AI-Sessions/conversations/2026-08-26_PropAI_화면감사완결_보류계약_실거래3층_인계.md` 조회.
**관련 기록 있음** — §4 가 `#838`(관리자 대사 보류)을 대기 항목으로, §6 이 *"락이 뚫린 세 번"* 을
기록한다. 그 §6 의 세 항목이 전부 **"순수 함수만 잠그고 배선을 안 잠갔다"** 인데,
**이 결함이 정확히 그 네 번째**다 — 그리고 이번엔 **프로덕션까지 나갔다.**

## 1. 전제 — 확인 방법과 실측값

라이브 `propai-v002789-3f46fa47` · admin 테넌트 · 2026-08-26 배포 직후.

| # | 전제 | 방법 | 실측 |
|---|---|---|---|
| P1 | 보류 수는 정상 노출 | `GET /api/v1/sales/projection/accounting-rollup` | `reconcile_withheld_count` = **11** · 항등식 11+0+2=13 ✔ |
| P2 | **사유는 부재** | 같은 응답의 `sites[].reconciliation` 키 전수 | `balanced_absent`·`balanced_basis` **둘 다 없음** |
| P3 | 11곳 전부 사유 갈래를 탄다 | 전수 표(판별 입력값) | `schedule_present=False · scheduled_total=0 · revenue_signed=0` × **11/11** |
| P4 | 배포본에 `#838` 코드가 **있다**(H2 기각) | 컨테이너 원문 `grep -c "withheld("` | **1** (line 348) · `tally_reconciliation` 1 |
| P5 | 원인은 **조립부** | `console.py` 원문 | `"balanced": reconcile_ok` 만 복사 · 사유 키 미복사 |
| P6 | 저장소 **자기 계약**이 위반이라 말한다 | `validate_withheld_pair(라이브형상,"balanced")` | 위반 **2건**(무언 보류) · **대조군**(사유 실린 형상) 위반 **0** |

★P4 를 넣은 이유: 처음 컨테이너를 `/app/app/...` 로 조회해 **"파일 없음"** 을 얻었다.
실제 경로는 `/app/apps/api/app/...` 였다 — **부재가 아니라 내 경로가 틀린 것**이었다(§26).
`find` 대조군(18,177개 `.py`)으로 조회기 생존을 먼저 증명하고 다시 쟀다.

## 2. 경쟁 가설과 기각 (propose → dispose)

| 가설 | 처분 |
|---|---|
| H1 조립부가 키를 손으로 골라 버린다 | **채택** — 원문에서 확인(P5) |
| H2 배포본에 `#838` 이 없다 | **기각** — 컨테이너 원문에 실재(P4) |
| H3 `_reconcile` 이 사유 갈래를 안 탄다 | **기각** — 11/11 이 그 갈래(P3) |

## 3. 처방 — 판정·값 불변, 승계만

`reconciliation_public_fields(rec)` **순수 함수**를 꺼내고 조립부가 그것을 쓴다.
`_absent` 는 **값이 `None` 일 때만** 싣는다(값이 있는데 사유가 남으면 **거짓 보류**).

## 4. 회귀가 아닌 근거

`reconcile_withheld_count`(11)는 **이미 정상**이라 건드리지 않았다 — 세는 경로와 사유 경로는 별개다.
정합·불일치 갈래는 사유 키를 **애초에 만들지 않으므로** 출력이 종전과 **바이트 동일**하다
(특이도 테스트 2건이 이를 단언). 관련 3스위트 37 passed · ruff CI 핀 0.16.3 통과.

## 5. 잠금 — 변이 4종 전부 CAUGHT

| 변이 | 결과 |
|---|---|
| R1 조립을 **손으로 고르기로 원복**(결함 그 자체) | **CAUGHT** |
| R2 `_absent` 만 조용히 누락(부분 회귀) | **CAUGHT** |
| R3 `is None` 가드 제거(무조건 사유 부착) | **CAUGHT**(아래 참조) |
| R4 **생산자**에 새 판정키 추가 → 미승계 | **CAUGHT**(파생형 락) |

★**R3 은 처음에 SURVIVED 였고, 그것이 옳은 신호였다.** 실제 `_reconcile` 은 비보류 갈래에서
사유 키를 **아예 만들지 않아** `if v:` 와 **이중 가드**라, 실입력만으로는 그 가드를 지워도 초록이다.
가드가 **무엇을 막는지** 잠그려면 **생산자가 어긋난 합성 입력**이 필요했다 — 그 테스트를 추가하니 CAUGHT.
*생존을 "구멍"으로도 "정상"으로도 성급히 부르지 않고 **왜 생존했는지**를 먼저 봤다.*

★**R4 는 처음에 내 변이 설계가 틀렸다** — **소비자**를 변이시켜 놓고 생산자 변이라 불렀다.
생산자를 고치니 CAUGHT. **변이가 생존하면 코드보다 변이를 먼저 의심하라.**

## 6. ★검증하지 못한 것 (정직)

- **라이브 재확인은 배포 후에만 가능하다.** 지금 라이브는 여전히 사유 없는 형상을 낸다.
- **`balanced=False`(불일치) 갈래는 라이브에 한 건도 없다**(13곳 중 0) — 단위 픽스처로만 확인.
- **다른 소비처 미측정** — `reconciliation` 을 읽는 다른 화면·엔드포인트가 있는지 전수로 세지 않았다.
- **조립부가 여전히 키를 손으로 고른다.** 이 PR 은 판정 키만 파생형 락으로 덮었다 —
  `discrepancies`·`tolerance` 같은 다른 키가 늘어나면 **같은 방식으로 사라질 수 있다**
  (동료 세션 SESSION-G 의 지적. 그 축은 이 PR 범위 밖으로 남긴다).

## 7. 되돌리기

단일 커밋 revert. 스키마·저장 무변경. 되돌리면 응답이 `#838` 직후 상태(사유 없는 보류)로 복귀한다.
