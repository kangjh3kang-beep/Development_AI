# 콜백 화면이 **내부 API 경로를 사용자에게 보여준다** (2026-09-06)

세션 development-ai-e4 · sid=e739d2a9 · 브랜치 `fix/social-login-error-disclosure`(#1000 에 얹음)

## 0. 옵시디언 조회 결과 (§0)

★**조회 불가** — `/mnt/d` 마운트가 `ENODEV` 다(사유 = **장치 부재**). 지어내지 않는다.
대신 **이 세션에서 내가 오늘 작성한 기록**을 근거로 쓴다:
`2026-09-06_구현이_없는_게_아니라_왜_안되는지_말하지_않았다.md` — 같은 도메인의 **고지 결함** 기록.
★이번 건은 그 **형제**다: 앞 건은 *실패를 안 말한 것*, 이번 건은 *성공을 개발자 언어로 말한 것*.

## 1. 전제 표 — 확인 *방법* + **실제 측정값**

| # | 전제 | 확인 방법 | 결과(실측 2026-09-06) |
|---|---|---|---|
| P1 | 콜백 화면이 구조적으로 필요한가 | OAuth 규약 + 라이브 왕복 | ◎ **필요하다.** `redirect_uri` 는 브라우저가 착지하는 실제 페이지여야 하고, 실제로 `?code=…&state=…` 를 받아 교환한다 |
| P2 | 사용자가 이 화면에 머무는가 | 소스 | ✘ **아니다.** 성공 시 `setTimeout(() => router.replace('/'+locale), 800)` — **0.8초 뒤 자동 이동**. 버튼은 자동 이동 실패 시 **탈출구** |
| P3 | 내부 API 경로가 노출되는가 | 3사 전수 grep | ★**네이버 1 · 구글 1 · 카카오 0** — *「인가 코드를 실제 \`/auth/naver/callback\` API에 전달해…」* |
| P4 | 형제가 갈려 있는가(§29) | 라벨 키 계수 | ★**카카오 28개 · 네이버 8 · 구글 8** — 카카오만 사용자 언어로 정돈돼 있다 |
| P5 | 카카오는 뭐라고 하나 | 소스 | `로그인하는 중` / `카카오 계정으로 안전하게 로그인하고 있어요` / `로그인되었습니다` |
| P6 | 이게 검수에 걸리는가 | 네이버 검수 요구 | ★**걸린다.** 요구 4장 중 **4번(완료 화면)** 이 정확히 이 화면이다 — 심사자에게 **내부 API 경로가 찍힌 화면**을 제출하게 된다 |
| P7 | 라이브에서 실제로 도달하는가 | 사용자 실측 스크린샷 | ◎ `4t8t.net/ko/naver/callback?code=…` → **「네이버 인증이 완료되어 브라우저 세션을 저장했습니다」** |

★**결론: 화면은 필요하고, 문제는 「이 모습이어야 하냐」다.**

## 2. 변경 내용과 회귀가 아닌 근거

1. 네이버·구글 콜백 화면의 **사용자 대면 문구를 카카오 톤으로** 통일(3언어 전부)
2. **내부 API 경로·개발자 용어 제거** — `/auth/*/callback`, `code·state를 확인하세요` 등
3. ★**파생형 락 신설** — `*CallbackWorkspaceClient.tsx` 를 **디렉토리에서 파생**해 전수 검사한다.
   네 번째 공급자가 추가돼도 자동으로 감시망에 들어온다(§수집·판정 규율).

**회귀가 아닌 근거**: **문구만** 바꾼다. 상태 전이·자동 이동·에러 분기는 **건드리지 않는다**.
`#1000` 이 추가한 `providerError` 경로도 그대로다.

## 3. ★검증하지 못한 것

- **옵시디언 §0 조회 불가**(장치 부재) — 선행 결정이 있었는지 **미측정**
- **실제 사용자 인지 개선은 미측정** — 0.8초짜리 화면이라 대부분 못 본다.
  이 변경의 실효는 **검수 캡처**에서 가장 크고, 그건 사람이 판정한다
- **다국어 문구의 자연스러움 미검증**(en·zh-CN) — 원어민 검토 없음
- `#1001` 푸터가 이 화면에도 붙는지는 **배포 후 재측정** 필요

## 4. 되돌리기 경로

`git revert` 한 번. 문구 상수만 바뀌므로 로직 복원 위험이 없다.

## 5. 잠금

`apps/web/components/auth/__tests__/callback-copy-hygiene.test.ts`
- ★**공급자를 디렉토리에서 파생** — 목록형 금지
- **금지 패턴 전수**: 내부 API 경로(`/auth/…/callback`), `파라미터`, `code·state를 확인`, `API에 전달`
- ★**두 모집단**: 금지어가 없어야 하는 것 ↔ **있어야 하는 것**(사용자 문구가 실재하는지)
  — 한쪽만 보면 「문구를 전부 지운」 구현도 통과한다
- **하한 단언**으로 「대상 0개라 통과」를 배제
