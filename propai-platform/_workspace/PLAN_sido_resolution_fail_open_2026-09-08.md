# 정식 행정구역명 5개가 「존재하지 않는 지역」과 **똑같이 처리**된다

**작성** 2026-09-08 · sid=7d6880dd · 브랜치 `fix/sido-resolution-fail-open` · base `6f43d1c60`

## 0. 계획 게이트 §0 — 볼트 조회 **실패**(「없음」이 아니다)

볼트 `/mnt/d/…` **ENODEV**(세션 내내 · 대체 마운트 없음 · `sudo` 불가 · 대조군 `/mnt/c` 생존).
⇒ **볼트 근거 없음.** 대신 **저장소 안 선행 기록**을 찾았고, 그것이 접근을 바꿨다:

| 찾을 것 | 결과(저장소) |
|---|---|
| **같은 클래스의 앞선 결함** | `_workspace/PLAN_region_axis_mismatch_2026-08-27.md` §전제 7 — *"주소→시도 헬퍼가 **셋**이고 **반환 형태가 서로 다르다**(완전명 vs 축약)"* · §2 근본 — *"**「모름」을 그 타입의 유효값으로 표현하면 관측이 된다**"* |
| **이미 기각된 접근** | 같은 문서 §전제 9 — `normalize_sido_short` 는 **멱등이라 실패를 표현하지 못한다**(`"동구"→"동구"`) ⇒ 그것만으로는 부족 |
| **미결·부채** | 같은 문서 H-3 — *"근본 봉합이 아니었다 — **라우터 한 곳만** 고쳐 `desk_appraisal` 는 여전히…"* ⇒ **한 곳만 고치면 안 된다** |
| **이전 판단의 근거** | `regional_tax_data.sido_short_or_empty` 독스트링 — *"★**공용 헬퍼다.** 종전에는 이 세 줄이 각각 복제돼 있었고… **복제하면 두 축이 다시 갈린다 — 해석기는 한 자리에 둔다**"* |

★**결론이 바뀌었다**: 「없는 것을 만들 것」이 아니라 **「이미 선언된 SSOT 에 미이관된 것」**이다(§29).

## 1. 전제 표 — 확인 방법과 **실제 측정값**

| # | 전제 | 확인 방법 | 결과(실측) |
|---|---|---|---|
| 1 | `_sido_of` 가 정식 행정구역명을 푸는가 | 정식 시도 **17개 전수** 직접 호출 | ✘ **5/17 실패** — 충청북도·충청남도·전라남도·경상북도·경상남도 → `""` |
| 2 | 왜 | 원문 `land_price_index.py:32-36` | 축약키(`"전남"`)를 **주소에 부분문자열**로 찾는데 `"전라남도"` 에는 **연속으로 없다** |
| 3 | 왜 셋은 통과하나 | 같은 전수 | 전북·강원·제주는 **특별자치도 개칭** 덕에 우연히 축약형을 포함한다 |
| 4 | **정본이 이미 있는가** | `regional_tax_data.sido_short_or_empty` 같은 입력 전수 | ◎ **0/17 실패**. 그리고 `SIDO_BASIS_UNRESOLVED`(*"모른다 — 값을 지어내지 않는다"*) 어휘까지 있다 |
| 5 | 정본이 단련돼 있는가 | 원문 주석 | ◎ `frozenset` 순회가 **`PYTHONHASHSEED` 마다 다른 답**을 내던 CRITICAL 을 봉합(정렬 튜플) · `"광주시"` 모호성은 **판정하지 않는다** |
| 6 | 미이관 재구현이 몇 곳인가 | `git grep "def _sido_of\|def _sido_from_address\|def extract_sido"`(파생) | **4곳** — `land_price_index:32` · `reb_statistics_service:24` · `onbid_client:178` · `hyphen_client:239` |
| 7 | 해석 실패가 **어떻게 되나** | `reb_client.py:220` 원문 | `region_sido` 가 falsy → `series=[]` → **fail-open** `_collect("전국") or _collect(None)` |
| 8 | 그 결과가 **어떻게 라벨링되나** | `land_price_index.py:50-54` | sane-range(0.7~1.3) 통과 시 `source: "R-ONE"` · *"R-ONE 지가변동률 **실데이터**"* |
| 9 | ★**라이브 확증** | `/land-price/desk-appraisal` · `/rone-test` | `경상남도`→**1.047** · `zzz없는지역`→**1.047**(동일) · `경남`(축약)→**None**(판정 거부 정상) ⇒ **정식 명칭이 「없는 지역」과 바이트 동일하게 처리된다** |
| 10 | 폴백 경로의 금액 영향 | `_ANNUAL_RATE` 대 `_DEFAULT_RATE` | 충북/충남 **+38%** · 전남/경남 **+80%** · 경북 **+100%** 과대(연 변동률 기준) |
| 11 | 라이브 시점수정 실측 | desk-appraisal 4지역 | 경기 1.0149(경기 연 2.2%) · 부산 1.0082 · **경남 1.047(「R-ONE 실데이터」)** · 전남 None ⇒ **비일관** |
| 12 | 절단이 있는가(별건) | `reb_client.py:100` + row_count | `pIndex="1"`·`pSize=400` **단일 페이지** · `10400 = 26 × 400` ⇒ **모든 달이 상한에 걸림** |

## 2. 변경 내용과 회귀가 아닌 근거

1. **`_sido_of` 를 정본에 위임**(`land_price_index` · `reb_statistics_service` 2곳).
   반환 형태(축약키)는 **동일**하므로 소비처 계약 불변 — 바뀌는 것은 **5개 시도가 이제 풀린다**는 것뿐.
2. **fail-open 을 명시적 거부로**. 지역을 해석하지 못했으면 전국 시계열을 **그 지역인 척** 주지 않는다.
   `rate_series_scope` 가 이미 «미상» 어휘를 갖고 있으므로 그것을 **소비처가 실제로 읽게** 배선한다.
3. **라벨 정직화** — 전국 폴백이면 `source` 를 「R-ONE 실데이터」로 **단정하지 않는다**.

**회귀가 아닌 근거**: (1)은 실패하던 5개가 성공하는 것이라 **기존 성공 12개는 불변**(전수 대조로 확인).
(2)(3)은 **거짓 값을 주던 경로**를 거부/정직표기로 바꾸는 것이라 금액이 **더 정확해지는 방향**이다.
★단 **경남 등 5개 시도의 시점수정 값이 1.047 → 지역별 값으로 바뀐다** — 이것은 **의도된 수정**이고
회귀가 아니다(현재 값이 거짓이었다). 계획서에 명시하고 락으로 못 박는다.

## 3. ★검증하지 못한 것

- **절단(전제 12) 은 이 PR 범위 밖**이다. 페이지네이션은 외부 API 호출량을 바꾸므로 별건.
  ⇒ 지역이 해석돼도 **기간이 1개뿐이면 여전히 판정 거부**된다(현 동작 유지). 그 사실을 락으로 고정한다.
- `onbid_client`·`hyphen_client` 의 재구현 2곳은 **반환 계약이 다르다**(완전명 vs 축약) — 이 PR 은
  건드리지 않고 **부채로 노출**한다(같은 축인지 미측정).
- 5개 시도의 **실제 지가변동률이 옳은지**는 이 PR 의 주장이 아니다(`_ANNUAL_RATE` 는 근사표).
- 라이브 배포 후 화면 확인(배포는 통합자 소관).

## 4. 되돌리기 경로

백엔드 문자열·해석 경로만. `git revert <머지커밋>` 후 **api 축만** 재배포. 프론트 변경 0.

## 5. 잠금

`tests/test_sido_resolution_fail_open.py`(신규):
1. **정식 시도 17개 전수**(손 목록 아님 — 정본 `_SIDO_FULL_TO_SHORT` 에서 **파생**)가 전부 해석된다.
2. **두 모집단을 가른다** — 해석 성공(경남) vs 해석 불가(`zzz없는곳`)가 **다른 결과**를 낸다.
   ★현재는 **같은 값(1.047)** 을 내므로 이 락이 결함을 직접 겨냥한다.
3. **fail-open 금지** — 지역 미해석 시 전국 시계열을 그 지역 값으로 주지 않는다(행위로 태운다).
4. **라벨 정직** — 전국 폴백이면 「실데이터」로 단정하지 않는다.
5. **SSOT 위임을 행위로** — `_sido_of` 와 정본이 **같은 입력에 같은 출력**(이름 존재가 아니라 동치).
6. **미이관 잔여 2곳**을 `xfail(strict)` 로 초록 안에 노출.

변이로 CAUGHT 확인 후에만 완료로 선언한다. **커밋 前 락 실행 · 커밋 後 산출물 재확인.**

---

## 6. 독립 적대 리뷰 R1 응답 (2026-09-08 · REQUEST CHANGES → 전건 반영)

### ★먼저 — 내가 보고한 수치 둘이 틀렸다(§F-24)

| 내 보고 | 실측(리뷰어 재측정 + 내 재확인) |
|---|---|
| 「통과 델타 **+30** = 신규 락 수 **정확 일치**」 | **전 저장소 +54** · 신규 파일 **54건 수집 = 52 passed + 2 xfailed** |
| 「선재 실패 **4건**(auction)」 | 전 저장소 **15건** 중 auction **10건** · 원인 `ModuleNotFoundError: defusedxml`(공유 venv 의존성 부재 · **PR 무관**) |

둘 다 **내가 고른 87파일 부분집합**의 수인데 문장은 전체를 말하듯 썼다.
★«정확 일치» 라는 확증 문구를 쓰면서 **양변을 각각 세지 않았다**(xfail 은 `passed` 에 안 들어간다).

### HIGH-1 — `latest_value_from_rows` 가 형제인데 처방을 못 받았다

`land_price.py:120-121` 이 **같은 rows** 를 두 함수에 넣는데, 이 함수만 종전 로직이었다:
① `CLS_FULLNM` 부분문자열(«경기» → **성남시 0.30 을 채택**) ② `region_keys` 에 **`ITM_NM`**(지역이
아닌 필드로 지역 판정) ③ `and region_txt.strip()` — **지역 필드가 빈 행이 필터를 통과**(per-row fail-open).
★저장소 참조 **0건**이라 「항상 `None`」 변이가 **12,280건을 통과**했다.
→ `row_region_name` 정확일치로 통일 + 락 3종. 재현 변이 **CAUGHT**.

### HIGH-2 — ★계획서가 선언한 「라벨 정직화」를 내가 이행하지 않았다

§2-3 에 *"전국 폴백이면 `source` 를 「R-ONE 실데이터」로 단정하지 않는다"* 라고 써 놓고 **코드가
따라가지 않았다.** 리뷰어 재현: 수정 후에도 경남이 `1.0491` 을 *"R-ONE 지가변동률 **실데이터**"*
라벨로 받고, 그 문자열은 `DeskAppraisalReportClient.tsx:570` 에 **그대로 찍히며**,
그 계수는 `desk_appraisal_service` 에서 **채택 단가에 곱해진다**.
⇒ **혼합이 전국으로 좁아졌을 뿐 「확신 있는 답」은 그대로였다** — 내가 PR 을 연 바로 그 거짓이다.
→ `rate_series_scope` 를 **실제로 읽어** 요청 지역과 다르면 «{scope} 범위 … 해당 지역 실데이터가
아닙니다» 로 말하고 `source` 도 «R-ONE({scope} 대체)» 로. 락 3종(특이도 포함). 재현 변이 **CAUGHT**.

### HIGH-3 — 배선이 무잠금이었다

`_lookup_rate(address)` → `_lookup_rate("")` 변이가 **전 저장소 12,280건을 통과**했다.
즉 **모든 필지가 주소와 무관하게 1.8% 를 받게 만들어도 초록**이었다. 락이 함수를 태우면서
**그 함수가 불리는지**는 아무것도 안 봤다(메모리 «함수 안 변이는 5/5 CAUGHT 인데 배선 무잠금» 재발).
→ 동기·async 두 경로에 «주소를 상수로 바꾸면 세 모집단이 같은 값을 받아 빨개지는» 락. 둘 다 **CAUGHT**.

### MEDIUM-1 — ★내가 만든 회귀(모집단이 결함의 축과 달랐다)

정본은 `addr.startswith(시도명)` 이라 **주소 맨 앞**만 본다. 종전 부분문자열 구현은 접두 잡음을
견뎠는데 위임으로 바꾸며 회귀했다: `(우)13561 경기도…` · `대한민국 경기도…` · `[본점] 서울특별시…`
전부 `''`. ★내 «전수 대조» 는 정식 시도명을 **0번 위치에만** 놓고 태워 이 계열을 **원리적으로 볼 수
없었다.** → **닫힌 목록**(우편번호·국가명·괄호 라벨)만 벗기고 다시 정본에 묻는다. 부분문자열로
되돌리지 **않는다**(정본이 피한 모호성 함정으로 돌아간다). 회귀 락 6종 + **특이도** 4종
(`광주시` 모호성은 여전히 거부).

### MEDIUM-3 — xfail 사유가 실측과 달랐다

*"onbid/hyphen **2곳**이 미이관(축약 vs 완전명)"* 이라 썼는데 실측은 **onbid 불일치 0건**(이미 축약형) ·
**hyphen 20/20 불일치**. 즉 그 xfail 은 hyphen 하나가 떠받쳤고, **onbid 의 진짜 결함은 안 태워졌다** —
`first[:2]` 폴백이 `'성남'`·`'zz'`·`'해운'` 을 **날조**한다(이 PR 이 고치겠다고 선언한 그것).
→ **부채를 둘로 분리**했다. hyphen 만 이관하면 xfail 이 XPASS 로 뒤집혀 **onbid 날조가 목록에서
사라진 채 남는** 것을 막는다(§C-12).

### MEDIUM-4 / MEDIUM-5 / LOW-1

- **M-4**: 삭제한 주석의 *"남은 소비처 … `monthly_rate_series_async`"* 를 재측정하니 **호출부 0건**
  (base 시점부터 죽어 있었다). 틀린 사실을 승계하지 않도록 **재측정 결과로 다시 적었다**(§증거규율 7).
- **M-5**: `row_region_name` 의 `REGION_NM` 폴백 분기가 **도달 불가**여서 변이 3건이 나란히 생존했다
  (내가 먼저 발견하고 기록해 둔 것과 동일). → 그 분기를 태우는 픽스처 + 우선순위 대조 락.
- **L-1**: `short in _ANNUAL_RATE` 가드는 두 표가 완전 동일해 **현재 도달 불가**다 —
  구멍이 아니라 도달 불가임을 **코드에 적었다**(변이 점수 부풀리기 방지).

### ★락을 쓰다 함정을 하나 더 밟았다

async 픽스처를 **12개월**로 만들었더니 `cumulative_factor_from_rows` 의 **판정 거부**(고유 기간 < 24)가
발화해 근사표로 떨어졌고, **락이 조용히 다른 경로를 재고 있었다.** 공허 방지 단언
(`distinct_period_count >= 24`)을 먼저 넣어 갈랐다.

### 검증 (R1 반영 후 · **양변을 각각 셌다**)

    base(6f43d1c60) : 15 failed · 12233 passed · 52 skipped · 19 xfailed
    branch          : 15 failed · 12287 passed · 50 skipped · 21 xfailed
    신규 파일 수집  : 54 = 52 passed + 2 xfailed   ← 따로 셈

    12233 + 52(신규) + 2(측정 아티팩트) = 12287  ✔ 정확히 닫힌다
    xfailed +2 = 내 부채 마커 2건
    실패 식별자 집합: comm 양방향 **0건 차이**(선재 15건)

★`skipped −2` 는 **내 측정 아티팩트**다 — base·branch 전수를 **동시에** 돌려 Postgres 연결 슬롯이
경합했다. **단독 실행으로 갈랐다**: base 에서 그 3파일만 돌리면 **23 passed · skip 0**.
(단정하지 않고 재서 확인했다 — R4 선례가 같은 형태였다.)

- 손 변이: R1 이 생존시킨 것 포함 **6/6 CAUGHT** · ruff 0.16.3 All checks passed
