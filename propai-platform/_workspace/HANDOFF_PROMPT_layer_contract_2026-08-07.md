# 인계 프롬프트 — PropAI 층위 계약 이후 (2026-08-07 → 다음 세션)

아래를 그대로 다음 세션 첫 지시로 쓰면 된다.

---

## 0. 착수 전 필수 (순서대로)

1. **`CLAUDE.md` 의 "회귀망·검증 규율" 22개를 먼저 읽어라.** 이번 캠페인에서 **반복 재발한 오류**를
   지침으로 고정한 것이다. 테스트·가드·계약을 쓰거나 고칠 때 매번 확인한다.
2. `scripts/coord.sh status` — 다른 세션이 잡은 영역 확인
3. **옵시디언 인계 문서를 읽어라**:
   `/mnt/d/옵시디언기록/나의-모든-기록-최적화본/AI-Sessions/conversations/2026-08-07_PropAI_레이어계약_인수인계.md`
   결함 계보는 `AI-Sessions/wiki/errors/2026-08-0{5,6}_*.md` 4건.
4. `git fetch` 후 **origin/main 기준 전용 워크트리**를 파라. 공유 메인에서 feature checkout 금지.
   ★**편집한 워크트리와 커밋하는 워크트리가 같은지 확인하라** — 이번에 공유 메인에 지침 56줄을
   미커밋으로 방치해 PR 에 안 들어간 사고가 났다.
5. `git stash` 금지 — 공유 stash 에 다른 세션 소유 항목이 있다.

## 1. 지금 상태

- 모바일 IA(P0~P2) + **층위 계약** 폐루프 종결. 머지 6건(#563·#566·#570·#573·#575·#579),
  독립 적대검증 **8라운드 전건 봉합**.
- ★사용자 스크린샷 지적(지도 오버레이가 "분석 대상"을 덮음)은 **라이브에서 해소 확인**했다.
- **배포 대기 1건**: #579(sw v496 필요 — v495 이후 머지).

## 2. 즉시 할 일

### ① 배포 확인 후 라이브 재실측 3항
1. `/ko/design-studio` CAD 편집실 → **전체화면 토글 시 앱 헤더가 가리지 않는지**
2. 토지조서에서 **주소 有 + 편입토지 0건**일 때 지도셸이 **펼쳐져 있는지**
3. 레이어 봉합 무회귀 — `/ko` 레일 펼친 채 `scrollTop=900` 에서 "분석 대상" 띠의
   `elementFromPoint` 가 **ContextHeader** 를 반환하는지(팝오버면 회귀)

★배포 판정은 **버전 문자열이 아니라 렌더된 값**으로. 지난번 sw 상수만 바뀌고 번들은 종전이라
`z-600` 이 0개였던 사례가 있다.

### ② 후속 티켓 (우선순위순)

| # | 항목 | 비고 |
|---|---|---|
| 1 | 파생 감시가 **2단계 이상 임포트**를 못 본다 | 실재 1건: `MarketInsights → OrchestratorPanel → InputResolveModal(z-50)` |
| 2 | **비리터럴 className**(삼항·`cn()`) 백드롭 미검사 | 정규식 밖 |
| 3 | 모달 rung 을 **렌더 기반**으로(현재 소스 락) | 오픈 조건이 제각각이라 미전환 |
| 4 | 지도 **비공존** 화면 모달 z 산재 | 50/60/70/100/120/1000 — **일괄 승격은 하지 않기로 판정**(실측 근거 있음) |
| 5 | `/projects/[id]/cad`·`/bim` 전체화면 실효성 | **미확증** — framer-motion `transform` 이 `fixed` 컨테이닝 블록을 만들 수 있다 |

## 3. 이 저장소의 층위 계약 (정본: `apps/web/lib/satong-map-z.ts`)

```
지도 오버레이(≤500) < 본문 sticky(600) < 네비 플라이아웃(700) < 모달(800) < 앱 크롬(1000)
                                                            전체화면(9990)만 크롬 위
```

**층위를 건드릴 때 반드시 알 것**:
- 지도 래퍼는 **스태킹 컨텍스트를 만들지 않는다** → 오버레이가 루트에서 본문과 경쟁한다
- 지도 타일만 `.leaflet-container{isolation:isolate}` 로 격리돼 있어 **본체는 아래, 부속은 위**로
  가는 비대칭이 있다
- **z 를 올려도 소용없는 경우가 있다**(컨텍스트 안에 갇히면) — 컨텍스트 자체를 봐라
- 한 칸을 올리면 **위/아래 칸이 함께 검사**되도록 `__tests__/layer-ladder.contract.test.tsx` 가
  사다리 전체를 잠근다

## 4. 재생성 금지 — 이미 반증한 것

1. "지도 위에서만 경쟁한다 → 지도 컨트롤이 이기는 게 옳다" → **거짓**. 패자는 본문 정보였다
2. "근원 봉합은 풀스크린 회귀 위험" → **존재하지 않는 경로**(`mode:"css"` 라 네이티브 분기 도달 불가)
3. "래퍼에 `isolate`" → 오버레이는 갇히지만 **FAB(95)이 지도 컨트롤 위로** 올라와 서열이 뒤집힌다
4. "전역 모달 z 일괄 승격" → 실측 결과 실위험 조합이 적어 **회귀 위험 대비 이득이 낮다**
5. "앱 헤더가 지도 오버레이에 덮인다" → 앱 헤더는 z-1000 이라 **안전**(좌표 겹침을 z 우열로 오독한 것)

## 5. 작업 방식

실측 → `/insight-loop` 으로 계획 전제부터 반증 → 구현 → **커밋 먼저 → 변이 → 원복** →
게이트 → 독립 적대검증(자기승인 금지) → 봉합 → 머지 → sw 범프 → 배포 → 라이브 검증 → 옵시디언 기록.
한국어 보고, 무목업·완결게이트 준수.

★**검증보다 머지가 빠르면 회귀가 라이브로 나간다** — 이번에 실제로 그랬다(#561). 적대검증은
**PR 을 올리는 시점에 같이** 걸어라.
