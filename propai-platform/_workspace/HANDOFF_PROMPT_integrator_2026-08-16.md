# 인계 프롬프트 — 통합자 세션 (2026-08-16 → 다음 세션)

맨 아래 「인계 프롬프트」 절을 **그대로 다음 세션 첫 지시**로 쓰면 된다.

---

## 1. 인계 시점 실측 상태

> **★★배포 상태는 이 문서에 값으로 적지 않는다 — 아래 명령으로 재라.**
>
> ```bash
> curl -s https://4t8t.net/sw.js | grep -m1 '^const CACHE_NAME'   # 끝의 sha = 158 에 배포된 커밋
> curl -s https://api.4t8t.net/health                             # 168 백엔드
> curl -s -o /dev/null -w '%{http_code}\n' https://4t8t.net/zzz-nope   # 대조군(404 여야 정상)
> git log --oneline origin/main -1                                # 정본
> ```
>
> `CACHE_NAME` 끝의 sha 를 `origin/main` 과 대조하면 **무엇이 미배포인지 한 번에** 나온다.
>
> ★**줄 시작 앵커(`^const`)를 빼지 마라.** 이 문서는 종전에 앵커 없는 형태를 실어 날랐고,
> 그것을 그대로 쓴 세션이 **주석의 예시값**을 배포 상수로 읽어 *"서빙이 뒤로 갔다"* 는
> 유령을 만들었다(2026-08-17). `sw.js` 의 `propai-v` 문자열 셋 중 **둘이 주석**이다.
> 자세한 것은 `CLAUDE.md` §회귀망 G-28.
> (`#658` 이후 캐시명이 **빌드 파생** `propai-v<카운터>-<커밋sha>` 다 — 예전 `v50x` 표기는 없다.)
>
> ★★★**왜 값을 안 적나 — 이 자리에서 두 번 썩었다.**
> ① 17:50 에 *"v506 미배포"* 를 **현재 사실처럼** 적었다 → 몇 시간 뒤 타 세션 배포로 거짓이 됨.
> ② 그것을 고치는 커밋에 *"sw = propai-v506 ✅ 배포됨"* 이라고 **또 값을 적었다** → `#658` 배포로
> 그 표기 자체가 사라져 **머지되기도 전에** 거짓이 됨.
> **정정문이 새 부패를 실어 나르는 자리다.** 그래서 값을 빼고 재측정 방법만 남긴다.
>
> **#647 의 CAD 비율표기 수정은 사용자에게 도달했다**(아래 17:50 갱신이 "미도달"이라 한 것은
> 그 시점의 사실이었고 뒤집혔다 — **지우지 않고 남긴다**. 무엇이 **언제** 참이었는지가 자료다).
>
> ★★**다음 158 배포에는 1회성 함정이 있다**(보드 2026-08-17 00:00 노트): `#658` 이
> `safe-deploy.sh` 에 `APP_BUILD_ID` export 를 추가했는데 **이미 실행 중인 bash 는 구버전
> 내용을 돌리므로** 빈 값이 넘어가고 `Dockerfile.web` 이 fail-closed 로 빌드를 죽인다
> (프로덕션 무변경 = 안전 실패). **그냥 돌리면 한 번 실패한다** — 보드 노트를 먼저 읽어라.
>
> **라이브 표면은 정상이다** — 저장소에서 파생한 페이지 라우트 **57개 전수** 호출 결과
> 404·5xx **0건**(대조군 `/zzz-nope`=404 · `/ko`=200 으로 조회기 생존 확인).
>
> ★**이 절을 그대로 믿지 마라.** 커밋 해시·캐시명·배포 상태는 **분 단위로** 바뀐다 —
> 읽는 즉시 위 명령으로 재라.

<details><summary>2026-08-16 17:50 시점의 갱신(뒤집힘 — 기록으로 보존)</summary>

> ```
> origin/main   19741dde  fix(ci) 린터 버전 핀 (#655)
> 168 백엔드    healthy · v62.0.0
> 158 프론트    ★sw = propai-v505-charge-idempotency   ← main 은 v506 인데 **미배포**
> ```
>
> **양쪽 최신이 아니다.** 프론트에 **v506 이 안 나갔다**(#647 CAD 비율표기 수정이 미도달).
> ~~배포는 SSH 가 필요한데 그 세션은 키가 서버에 미인가라 실행하지 못했다~~
> ↑ **거짓이었다(2026-08-17 정정).** 실제로는 **키를 잘못 찾은 것**이다 —
> `~/.ssh/` 만 뒤지고 `Permission denied` 를 "미등록"으로 읽었다.
> **정답: `ssh -i ~/.oci.key ubuntu@<ip>`** 로 168·158 양쪽 다 접속된다
> (hostname `4t8tpropai-backend-a1` / `4t8t`). `~/.ssh/` 의 키는 전부 거부된다.
> ★같은 오판이 하루에 **두 세션**에서 났다 — `-i` 를 빠뜨리거나 다른 위치를 안 찾고
> "키 미등록"으로 결론내는 형태다. **"0건은 부재가 아니다"의 SSH 판** 이다.

</details>

### ~~미머지 PR 2건~~ → **전부 머지 완료** (2026-08-16)

| PR | 내용 | 착지 |
|---|---|---|
| **#650** | 심의 결과패널 3섹션 평면화 누락 | `6e5d445b` |
| **#647** | "전역 스윕"이 4/472 → 파생형 전환 + CAD 프로덕션 6건 | `ec843e99` |
| **#657** | sw v506 범프(타 세션 채번, 통합자 대행 머지) | `3019233d` |
| **#656** | 심의엔진 supply 미배선 파생형 락 + 인계서 P0 정정 | `e527b6e8` |

**남은 것은 배포 하나뿐이다.**

---

## 2. 착수 전 반드시 읽을 것

1. `scripts/coord.sh status` — 멀티세션 보드(배포 요청·claim 진행 중)
2. 옵시디언 `AI-Sessions/conversations/2026-08-16_통합자_9차원감사_봉합_인수인계.md`
3. 옵시디언 `AI-Sessions/wiki/errors/agent-lessons.md` **마지막 15항** — 이 세션 산출 교훈
4. `CLAUDE.md` §검증 규율 · §회귀망 규율

---

## 3. 남은 작업 (우선순위 순)

### ~~P0 — 보안: 심의엔진(8801) 재배포~~ → **부채로 강등**(2026-08-16 정정)

> **★이 항목은 보안 P0 가 아니다.** `origin/main` **6e5d445b** 기준 실측:
> `HITLQueue` 는 **진입점(app.main·app.tasks.\*)에서 도달하지 않는다.** 상류(파싱·추출)와
> 하류(미러쓰기)도 같이 미도달이라, **재배포해도 그 코드를 지나가는 요청이 없다** —
> 닫히는 공격 경로가 없다. 아래 "31일째 없다"는 **사실이나, 그것이 위험을 뜻하지 않는다.**
>
> 측정: 총 모듈 241 · 도달 159 · **supply 미도달 6/11**(`rule_extractor`·`hitl_queue`·
> `mirror_writer`·`hwp_parser`·`pdf_parser`·`tier2_site_harvester`). 대조군으로
> `main.py` 가 include 하는 라우트 8/8 도달을 먼저 확인해 분석기 생존을 증명했다.
>
> 이 사실은 `hitl_queue.py` 독스트링이 이미 적어 두었는데("배선 미완료") **코드 주석에만**
> 있어서 세션마다 다시 보안 결함으로 오독됐다. 그래서 **파생형 락으로 고정**했다 —
> 정본: `propai-platform/tests/unit/test_deliberation_supply_wiring.py` (PR #656).
>
> **뒤집힘 조건**: 그 락이 실패하며 `hitl_queue` 가 도달하게 됐다고 알리는 날. 그때가
> SoD 를 실제 승인 경로에 붙일 시점이고, 그 전에 설계 결함 둘을 먼저 풀어야 한다 —
> ①엔진 인증이 **단일 공유 Bearer 하나뿐**이라 승인자 신원을 헤더로 받으면 같은 토큰
> 보유자가 위조할 수 있다 ②`HITLQueue._tasks` 가 **인메모리 리스트**라 워커 다중화·재기동에
> 태스크가 소실된다(`HITLTaskModel` 테이블은 있는데 큐가 쓰지 않는다).
>
> 아래 원문은 **지우지 않고 남긴다** — 무엇을 근거로 P0 라 판단했는지가 다음 판단의 자료다.

**#420 SoD dual-control(승인자 신원 필수·자기승인 차단)이 31일째 프로덕션에 없다.**

- 컨테이너 이미지 `2026-07-15T23:31:17Z` · 8801 **공개 노출**·`/health` 200 = 살아서 서비스 중
- 대조 실험: 컨테이너에 `hitl_queue.py` 는 **있는데** `self_approval`·`approver_identity` **0건**
  → 파일 부재가 아니라 **옛 코드**
- 미도달 3커밋: `03fb373b`(#319) · `8dd2478c`(#326) · `85633acc`(**#420 SoD**)

**★위험도 실측 — 낮다:**
- **alembic 마이그레이션 0건**(3커밋 전수 확인) → 스키마 변경 없이 **재빌드+재기동만**
- compose 새 env **전부 기본값 보유**(`${SHEET_CLASSIFIER:-mock}` 등) → **additive 무회귀**
- → **시크릿 실값 없이 안전하게 재배포 가능**

**★정정 사항**: "형제 스윕 누락 사고"가 **아니다**. `git log -S "deliberation" -- infra/deploy-zero-downtime.sh`
= **0건** — 그 스크립트에 한 번도 있던 적이 없고 **애초에 별도 라이프사이클**이다.
전용 문서: `services/deliberation-review/docs/DEPLOYMENT_HANDOFF_2026-06-21.md`

검증: ①`/health` 200 ②컨테이너 안 `hitl_queue.py` 에 `self_approval|approver` **>0**(지금 0) ③심의 dispatch 무회귀

### ~~P1 — #647·#650 머지·배포~~ → **완결** (2026-08-17 00:10 재갱신)

> **머지 전부 착지**(불변 사실): #650 `6e5d445b` · #647 `ec843e99` · sw 범프 `3019233d` ·
> #656 `e527b6e8` · #659 `711cf7bf`. — 머지 해시는 안 썩으니 값으로 적어도 된다.
>
> **배포도 이행됐다** — 타 세션이 2026-08-16 23:30~23:40 에 168+158 을 올렸고,
> **#647 의 CAD 비율표기 수정은 사용자에게 도달했다.**
> ★**지금 무엇이 배포돼 있는지는 여기 적지 않는다**(그 값이 이 자리에서 두 번 썩었다).
> §1 상단의 재측정 명령으로 재라 — `CACHE_NAME` 끝 sha vs `origin/main`.
>
> ~~남은 것은 배포 하나다 — 158 라이브 sw 실측 `propai-v505-charge-idempotency`
> (2026-08-16 17:35 기준). 즉 v506 미배포라 #647 이 아직 안 나갔다.~~
> ↑ **17:35 기준으로는 참이었고 그 뒤 뒤집혔다.** 지우지 않고 남긴다 — 이 문서에서 내가
> **현재 사실처럼 적은 것이 몇 시간 만에 거짓이 된** 실례다(§1 상단 경고 참조).
>
> ★**E-22 심각도 정정**: 아래 원문(과 이 세션의 내 발언)이 범프 누락을 "캐시에 가려 안 보인다"로
> 썼는데 **과장이다.** `sw.js` 는 정적 자산에 cache-first 가 아니라 **SWR** 을 쓰고
> (소스 주석: *"cacheFirst였던 것을 SWR로 변경 — 새 배포가 다음 로드에 자동 반영(자가치유)"*)
> 내비게이션은 network-first 다. 해악은 **첫 로드 1회 stale 후 자가치유**로 한정된다.
> 범프는 여전히 옳지만, **E-22 를 근거로 다른 작업을 막을 때는 이 크기를 써라.**

**★E-22 는 양방향이다.** 내가 이 세션에서 한쪽만 보고 틀렸다:

> "범프는 기능 머지 뒤에"를 지키려다 **이미 밀려 있던 60파일을 못 봤다.**
> 라이브 sw 와 main sw 가 **같아도** 그 사이 프론트 델타가 쌓였을 수 있다 —
> **같다는 것이 곧 "안 밀렸다"가 아니다.**

배포 전 반드시:
```bash
git diff --name-only <마지막_sw범프_커밋>..origin/main -- propai-platform/apps/web \
  | grep -viE "(^|/)(e2e|__tests__|tests)/|\.test\.|\.spec\."
```

### P2 — #650 배포 후 완결 검증 (심각도 하향됨)

`#650` PR 본문에 *"프로덕션에서 항상 비어 있었다"* 라고 적었다가 **자기정정했다**.

- **확정**: `_compat_fields` 가 `sim_metrics`·`precedent`·`qualitative` 를 평면화에서 누락(소스 대조)
- **미확정**: 사용자가 실제로 빈 섹션을 봤는지. 그 패널은 **분석 실행 후에만 렌더**되는 조건부 화면이고,
  `/ko/deliberation-review` 는 현재 소개 화면이며 페이지가 스스로 *"라이브 산출은 배포 후"* 를 고지한다
- → 배포 후 **실제 심의 분석 1회**를 태워야 완결

### P3 — (사용자 액션) 자동배포 복구 · **처방 실측 확정** (2026-08-16 갱신)

`Deploy to Oracle Cloud` 가 **마지막 성공 2026-06-08 이후 실패 385 · 성공 0**(약 10주).

> **★아래 2번의 "E-22 게이트 추가와 한 묶음으로"가 이 항목을 스스로 막고 있었다.**
> 게이트를 **코드로 만들 소유 세션이 없어서** 시크릿 등록도 미뤄졌다. 재 보니 **그 게이트는
> 이미 존재한다 — 코드가 아니라 GitHub Environment 설정이다.**
>
> 실측(`run 31936601155` jobs API): `web-gate` **성공** · Deploy 잡은
> `Validate Oracle deployment secrets` **한 단계에서만** 실패하고 SSH·스모크는 **skipped**.
> → **워크플로는 고장이 아니다. 시크릿 부재 하나다.**
> (★`gh run view --log-failed` 는 **빈 출력**이다. jobs API 로 우회해야 보인다.)
>
> **필요한 시크릿은 2개뿐**이다. 나머지 5개는 워크플로 `env:` 에 기본값이 박혀 있다 —
> `SSH_USER=ubuntu` · `SSH_PORT=22` · `INTERNAL_URL=http://localhost:80` ·
> `WEB_URL=https://4t8t.net` · `HEALTH_URL=https://api.4t8t.net/health`.
> `ORACLE_DEPLOY_PATH` 는 원격에서 `${...:-$HOME/Development_AI}` 로 폴백한다
> (서버 저장소가 거기 있다면 불요 — SSH 가 없어 확인 못 했다).
> 현재 등록 시크릿은 `CLOUDFLARE_*` **2건뿐**이고 `ORACLE_*` 은 **0건**이다.
> `known_hosts` 는 `ssh-keyscan` 단계가 처리하므로 `StrictHostKeyChecking=yes` 여도 안 막힌다.
>
> **게이트**: 워크플로는 이미 `environment: oracle-production` 을 쓰는데, 그 환경은
> **실재하면서 `protection_rules` 가 0개**다. 거기에 **required reviewers** 를 걸면
> `push: main` 이 트리거돼도 **승인 전엔 실행되지 않는다** — 순서 우회·무인 프로덕션 배포·
> 배포 큐 폭주가 **코드 변경 0** 으로 해소된다.
>
> **정직 경계**: 이것은 워크플로 **소스** 판정이지 **실행 검증이 아니다**(시크릿이 없어 실행 불가).
> 셋을 끝낸 뒤 `workflow_dispatch` 로 1회 돌려야 실행까지 확증된다.
> 개인키를 **공개 저장소** 시크릿에 넣는 보안 판단은 사용자 몫이다(배포 전용 제한키 권장).

1. **선행**: `Cloudflare Pages`·`Workers Builds` **유물 GitHub App 연결 해제**
   (Oracle 컷오버로 무의미 · required 아님 · #632·#633·#636 전부 FAILURE 인 채 머지됨 = **순수 소음**)
   → **빨강의 의미 회복이 다른 모든 처방의 선행조건이다**
   ★부수 실측: 환경이 3개인데 나머지 둘(`pleasant-friendship/production`·
   `stunning-blessing/production`)은 Cloudflare 자동생성으로 보인다 — 함께 정리 대상.
2. `ORACLE_SSH_HOST`/`ORACLE_SSH_KEY` **2개만** 등록
3. `oracle-production` 환경에 **required reviewers** 추가 ← **이것이 E-22 게이트다**

### 부채 — 착수 권하지 않음

D1 죽은 컴포넌트 22건 · D4 디자인 부채 10건 · D3 잔여 11건 · 타 세션 인계 3건.
**산출물 심각도가 하향되면 멈출 신호다 — 볼륨을 진척으로 착각하지 않는다.**

### 미확정 (판정 보류)

`project_create` 원장 **50,000원 × 3건** vs 요율 SSOT(`app/core/billing.py:35`) **2,000원** — **25배 괴리**.
관리자 요율 변경 이력을 봐야 판정 가능. **결함으로 단정하지 말 것.**

---

## 4. 이 세션에서 비싸게 배운 것 (반복 금지)

1. **자기 범위를 자기가 넓히지 못한다.** 두 세션이 독립적으로 셌고 결과가 같았다 —
   **제품 결함 자체적발 0건**(나 4건·타 세션 3건). 자체적발은 전부 **"내 오류"** 였다.
   → 바깥 장치 다섯: 파생형 락 · 독립 렌즈 · 상호 리뷰 · CI · **사용자 신고**

2. **기록은 준수를 대체한다.** *"기록만으로는 안 온다"*(수동)가 아니라 **"기록하는 행위가
   준수를 밀어낸다"**(능동). 규율을 **인용하는 커밋일수록** 그 규율의 실제 확인을 따로 밟아라.
   실증: 나는 `#642` 교훈을 PR 본문에 써 놓고 **바로 다음 PR `#650`** 에서 어겼다.

3. **"0건"은 셋 중 하나다 — 미배포 / 잘못된 대상 / 잘못된 패턴.**
   보안·과금처럼 오보 비용이 큰 건은 **정본(origin/main)에 있는지부터** 대조하라.

4. **성공 신호와 빈 결과가 같은 값인 도구는 검증에 쓰지 마라.**
   실증 5종: `| head -1 && echo` 파이프 종료코드 · `[ -r /mnt/d ]` · `git grep -c | awk` ·
   **401 을 빈 목록으로 파싱** · 잘못된 파일 grep.

5. **손으로 세면 목록이 된다 — 사람이든 에이전트든.**
   감사 에이전트가 센 "8건"을 파생 술어로 재니 14건(넓은 술어)/6건(좁힌 술어)이었다.
   ★동시에 **위양성도 결함**이다 — 넓은 술어는 면적계산·층수추정·진행률을 위반으로 신고했다.

6. **전달이 불확실성을 떨어뜨리면 원 저자의 정직성이 무효가 된다.**
   에이전트가 "정적 근거만·라이브 미확인"을 명시했는데 옮기며 한정을 뗐다.

7. **사용자의 근거가 틀려도 결론은 맞을 수 있다.** 지도 z17 건이 그랬다 —
   **근거를 반박해서 결론까지 기각하면 진짜 결함을 놓친다.**

---

## 인계 프롬프트 (그대로 복사)

```
PropAI 통합자 세션을 이어받는다. 착수 전에 반드시 순서대로 한다:

1) scripts/coord.sh status 로 멀티세션 보드를 읽는다(진행 중 claim·배포 요청 확인).
2) 옵시디언에서 다음을 조회한다:
   - AI-Sessions/conversations/2026-08-16_통합자_9차원감사_봉합_인수인계.md
   - AI-Sessions/wiki/errors/agent-lessons.md 의 마지막 15항
3) propai-platform/_workspace/HANDOFF_PROMPT_integrator_2026-08-16.md 를 읽는다(이 문서).
4) ★인계서의 수치를 그대로 믿지 말고 라이브로 재확인한다:
   - origin/main / 168 health / 라이브 sw CACHE_NAME (curl https://4t8t.net/sw.js)
   - gh pr list 로 열린 PR
   기록은 작성 시점의 사실이다. ★호스트는 4t8t.net · api.4t8t.net 이다
   (인계서 초판이 쓴 propai.4t8t.net 은 **존재하지 않는다** — NXDOMAIN 을 "사이트 죽음"으로
   오독하기 쉬우니 조회할 땐 대조군을 같이 걸어라).

5) 실행 가능한 일이 남았는지부터 판단한다. ★이 항목 자체가 **한 번 낡았다** — 앞서 "남은 것은
   배포 하나"라고 적었는데 그 배포는 타 세션이 이행했다. **먼저 재라**: 라이브 sw · 168 health ·
   배포 ref 와 origin/main 의 차. 배포되지 않은 커밋이 있으면 그것이 배포 대상이다.
   ★★다음 158 배포에는 **1회성 함정**이 있다(#658 의 `APP_BUILD_ID`) — §1 상단과 보드
   2026-08-17 00:00 노트를 먼저 읽어라. 그냥 돌리면 한 번 실패한다(안전 실패).
   배포가 막히면 P3(§3)의 사용자 액션이 유일한 진행로다 —
   **부채 D1/D3/D4 로 내려가지 마라. 볼륨을 진척으로 착각하는 자리다.**

★P0 는 취소됐다(2026-08-16 정정). 심의엔진 SoD 재배포는 **보안 조치가 아니다** —
HITLQueue 가 진입점에서 도달하지 않아 재배포해도 지나가는 요청이 없다. 근거·뒤집힘 조건은
§3 P0 절의 정정 블록과 파생형 락(tests/unit/test_deliberation_supply_wiring.py, PR #656)에 있다.
**이 항목을 다시 보안 티켓으로 올리기 전에 그 락을 먼저 돌려 보라** — 초록이면 상태가 그대로다.

배포는 E-22 를 **양방향**으로 재고(범프 전/후 둘 다) 진행한다. 단 E-22 의 크기를 과장하지
마라 — sw 는 정적 자산에 SWR 을 쓰므로 범프 누락의 해악은 **첫 로드 1회 stale 후 자가치유**다
(영구 은폐가 아니다). 이 세션이 그 심각도를 과장해 왔고 §3 P1 에 정정해 두었다.

작업 원칙:
- 결함을 주장하기 전에 대조군을 세운다("0건"은 미배포/잘못된 대상/잘못된 패턴 셋 중 하나).
- 조건부 렌더는 그 상태를 만들어서 검사한다(정적 분석 결론을 측정처럼 옮기지 않는다).
- 남의 결론을 옮길 때 출처와 검증 수준을 함께 옮긴다.
- 목록형 골든 금지 — 대상을 코드에서 파생시킨다. 단, 위양성도 결함이니 술어를 실제 결함
  클래스로 좁히고 대조군으로 검증한다.
- 산출물 심각도가 하향되면 멈춘다. 볼륨을 진척으로 착각하지 않는다.
```
