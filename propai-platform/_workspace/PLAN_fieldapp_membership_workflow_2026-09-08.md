# 분양앱 회원체계·현장 전환 통합 계획 (2026-09-08)

사용자 요구(원문): *"모든 회원은 본 플랫폼이나 분양앱에서 등록된 모든 분양현장을 확인하고
등록신청을 하고 관리자 및 상위레벨이 승인할경우 비번없이 접속가능하도록 하고 현장에서 다른현장으로
실시간 접속을 변경할수있도록하면서 상호 겹치거나 저촉되지않도록 하되 계정당사자의 데이터는
통합해 관리할수있도록"*

---

## 0. 옵시디언 조회 결과 (계획 게이트 §0)

★**부분 조회만 가능했다** — 볼트 D: 가 `Full Repair Needed`(NTFS 손상), md 8개 중 4개가 CRC 로 읽기 불가.
읽힌 것으로만 조회했고, **렌즈 3개는 전부 볼트 접근에 실패**(ENODEV)해 그들 보고에는 볼트 근거가 없다.

| 찾을 것 | 결과 |
|---|---|
| **이미 기각된 접근** | ★**있다** — `actions.py:43-48`: *"과거 정책(상위 직급은 하위가 가능한 등록을 항상 할 수 있다 — 단조성)을 **폐기**했다. 상위가 말단을 건너뛰면 **수수료 2단 배분 기준이 붕괴**한다."* ⇒ 사용자 요구의 *"상위레벨이 승인"* 을 **문자 그대로 구현하면 기각된 접근의 재도입**이다 |
| **같은 클래스의 앞선 결함** | 계정 격리 와이프 목록이 «새 키는 즉시 등재» 를 **네 번 반복하고도 2건을 또 빠뜨린** 전례(`projectSync.ts:167-176`). ⇒ 현장 축도 **목록형으로 만들면 같은 일이 난다** |
| **미결·부채** | RLS 실효 전제가 **deploy-pending**(`sales_rls_bootstrap.py:15-19` — 앱 전용 non-BYPASSRLS role 분리가 인프라 작업) |
| **이전 판단의 근거** | `site_auth.py:13-14` 가 *"멤버십 없는 현장 진입 차단"* 을 선언 — **참이다.** 그러나 *"비번 없이 우회 불가"* 는 **어디에도 강제돼 있지 않다**(§전제 표 P4) |

---

## 1. 전제 표 — 측정 방법과 **실제 값**

| # | 전제 | 확인 방법 | 결과(실측) |
|---|---|---|---|
| P1 | 두 현장 목록의 숫자가 다르다 | 라이브 두 엔드포인트 diff | `/sales/sites` **13** vs `/sales/my-sites` **14**. 차이는 `구독자현장검증` 1건 · `membership="admin"`(superadmin 전체가시 갈래) ⇒ **버그 아니라 설계된 차이** |
| P2 | 「등록신청 경로 0건」 | ★**반증됨** | `job_posts`/`apply`/`decide` 로 **이미 완전 배선**(백엔드 `market.py:438~706` + `JobMarketPanel.tsx:88,263,358,392,406`). 내가 **낱말로 찾고 목적으로 안 찾았다** |
| P3 | 승인이 만드는 노드가 정상인가 | 원문 | ★**루트 고아**. `market.py:689-693` 이 `path=라벨1개`·`parent_id` 미지정으로 raw INSERT. `actions.py:113-116` 은 루트 생성 시 AGENCY 아니면 400 — **같은 테이블 두 생산자가 정반대 규칙** |
| P4 | 2차 비번이 인가 경계인가 | 원문 + 대조군 | ★**아니다.** `deps_sales.py:290-301` 이 토큰 없으면 **멤버십만으로 통과**. 대조군: 플랫폼 `SalesSiteWorkspace.tsx` 는 `getStoredSiteToken` 참조 **0건**(현장앱은 4건)인데 같은 데이터를 렌더 |
| P5 | 현장 전환 시 정리되는가 | 파생형 + 대조군 | `key={siteId}` **0건**(대조군 `key={` 5건) · `resetSite\|siteChanged` **0건**(대조군 `clearSiteToken` 12건) ⇒ 조회기 생존. **정리 코드 없음** |
| P6 | 「내 고객」의 축 | 원문 | `crm_enhance.py:246-248` 이 `site_id IN (...)` **만**. `user`/`assigned_node_id` 절 **0개**. `:259` 가 `phone_e164` **평문** |
| P7 | 현장앱 실사용 | 성장루프 5일 창 | 관측 라우트 **63종**에 `sales\|site` **0종**(대조군 `auth` 6종) ⇒ **API 트래픽 0** |
| P8 | 조직도 실태 | 라이브 전수 | 13현장 중 조직도 보유 **3곳** · 117노드 · 깊이1 **3건 전부 AGENCY** · MEMBER **101건** ⇒ 고아 루트 **0건**(대조군이 이 0을 의미있게 만든다) |
| P9 | RLS 가 지금 실효인가 | — | ★**미측정.** `rls_status()` 의 `isolation_effective` 를 안 읽었다. 아래 여러 판정이 여기 달려 있다 |

---

## 2. ★먼저 — 요구에 **결정이 필요한 공백 넷**

내가 임의로 정하지 않는다. 각각 **선택지와 그 대가**를 적는다.

### D-1. 「저촉 없음」이 **어디까지**인가 — 현장 간인가, 사람 간인가

재 보니 **현장 A vs B 는 `site_id` 스키마가 대체로 잡고 있고**, 실제로 무너진 것은 **현장 「안」**이다.
분양상담사끼리는 **같은 고객을 놓고 경쟁**한다. 동료의 고객 명부와 평문 연락처가 보인다(P6).

- **(a) 현장 안은 공유가 정상** → `crm_enhance.py` 의 **거짓 독스트링 3줄을 지운다**(코드가 아니라 문장을 고친다)
- **(b) 조직 서브트리로 2차 격리** → `assigned_node_id` + `org_path <@` 확장. **`sales_org_nodes` 의 `p_org` 정책이 이미 그 모양**이라 형태를 빌려올 수 있다

★어느 쪽이든, **지금 상태(코드는 공유·문서는 격리)는 두 선택 어느 쪽으로도 정당화되지 않는다.**

### D-2. 통합 뷰의 **감사 주체**가 없다

한 계정이 A·B 멤버면 통합 뷰는 정의상 둘을 한 응답에 담는다. A 관리자도 B 관리자도 그 화면을 못 본다
⇒ **유출을 감사할 주체가 존재하지 않는다.** 배선 결함이 아니라 **모델의 성질**이다.
- (a) 플랫폼 운영자만 감사 · (b) 각 현장이 「우리 데이터가 어느 통합 뷰에 실렸나」를 조회 · (c) 통합을 **집계값만**으로 제한

### D-3. 격리를 완성하면 통합이 **죽는다**

RLS 는 `app.site_id` **단일값** fail-closed 라 한 트랜잭션에 두 현장을 못 담는다. 통합 엔드포인트는
그래서 `sales_ctx` 를 **안 쓴다** ⇒ **지금 통합이 도는 이유는 RLS 가 아직 실효가 아니기 때문이다**(P9).
RLS 를 켜면 통합 화면이 **에러가 아니라 조용한 0** 을 뱉는다 — 사용자에겐 *"고객이 없다"* 로 읽힌다.
- (a) 앱 계층만 담당(대신 락 필수) · (b) `app.site_ids` **배열**로 정책 재작성 · (c) 통합을 **읽기전용 집계 테이블**로 분리

### ★D-4. **규제 축** — 목록 밖으로 꺼낸다(돈·법)

**방문판매 등에 관한 법률 §2-7(후원방문판매)**: *"특정 판매원의 실적이 **그 직근 상위판매원 1인의
후원수당에만** 영향을 미치는 지급방식"*. 같은 조 §2-5(**다단계판매**)는 ①하위 모집 ②**3단계 이상**
③후원수당 을 **모두** 충족하면 성립하고 **등록 의무 + 자본금 3억원 + 소비자피해보상보험**을 진다.

우리 코드 주석은 *"수수료 **2단** 배분"* 이라 적었고 사유를 **시스템 정합성으로만** 밝혔다.
★**규제 축이 그 옆에 있을 수 있다.** 주석이 사유를 하나만 적으면 다음 사람이 그것을 전부로 읽는다.
⇒ **법률 검토 사항이고 나는 확인하지 못했다**(출처: 조문 확인. 우리 구조의 해당 여부는 **미확인**).

---

## 3. 벤치마킹 → 채택·기각 (전부 출처 확인됨)

| # | 채택 | 근거 제품 | 우리에게 왜 |
|---|---|---|---|
| **R1** | **3단계 현장 가시성**(open / visible-restricted / hidden) | Figma «Open to org / Visible / Hidden» · Notion «open/closed/private» | *"모든 회원이 확인"* 을 **전면 공개로 구현하면 현장 목록 자체가 영업정보**다(`status` 기본값 `PREP` = 분양신고 전). Figma 는 **「보인다」와 「들어갈 수 있다」를 분리**해 이 문제를 푼다 |
| **R2** | **요청 시 「희망 직속 상위(sponsor)」를 필수로 받고, 승인과 ltree 삽입을 같은 트랜잭션에** | Figma «요청 시 접근수준 선택» 의 구조만 차용 | 승인 후 삽입 위치가 비면 **그 순간 수수료 기준이 붕괴**(P3). sponsor 확정 시점이 곧 승인 시점이어야 한다 |
| **R3** | **Procore 2층**: 계정은 전역 하나, 권한은 현장별 | Procore «한 이메일 → 여러 company, 내비바 Change + Permissions Template» | *"계정 데이터는 통합 관리"* 요구와 **정확히 일치**. 우리 조직도가 이미 site 스코프라 형태가 맞다 |
| **R4** | 활성 현장을 **클라이언트가 주장하게 두지 않는다** | Notion «completely separate silos» · Slack 워크스페이스별 별도 계정 · Teams 전환 시 테넌트 리로드 | *"상호 저촉 없음"* 은 UI 문구가 아니라 **쿼리 술어**다 |
| **R5** | 2차 비번 폐지 → **신원+승인** | Zoom 이 **passcode(공유 설계)** 와 **password(공유 금지)** 를 명시적으로 가름 | 우리는 **공유되도록 설계된 값을 비밀번호로 저장**하고 있다. Zoom 조차 passcode 단독을 금지하고 **인증**을 상위 대안으로 둔다 |

| # | ★기각 | 왜 우리에겐 안 되는가 |
|---|---|---|
| **X1** | **도메인 자동가입**(Slack·Linear·Notion·Atlassian 전부 보유) | ①자동가입은 **sponsor 확정 시점을 없앤다** ⇒ R2 와 **구조적 양립 불가** ②도메인이 조직을 식별한다는 전제가 우리 모집단에서 성립하는지 **미측정**(개인메일이면 «아무 조직도 식별 못 하는 게이트») |
| **X2** | **요청자가 접근 수준을 고르는 것** | 우리 역할은 **수수료율과 직결** — 요청자가 `DIRECTOR` 를 고르면 그것은 접근 요청이 아니라 **금전 청구**다. 받는 것은 «가입 희망 + 희망 직속 상위»까지, **레벨 부여는 전적으로 승인자** |
| **X3** | **Slack Connect 식 «외부 조직이 자기 조직 그대로»** | 양측이 **각자 조직을 갖고 각각 승인**하는 구조인데, 우리 대행사 상당수는 **플랫폼 밖에 조직이 없다** ⇒ 성립 자체가 안 됨. 우리에게 맞는 것은 Procore 모델 |

★**「같은 테넌트만 배정」 제약은 두 명제로 갈라야 한다**:
- «1인 1소속» = **업계 표준이자 국내법에도 실재**(Procore 1인 1 vendor record · 공인중개사법 §12② 이중소속 금지 — ★단 **분양대행 종사자 적용 여부는 미확인**) ⇒ **지킨다**
- «외부 조직 사람을 들일 통로가 아예 없다» = ★**병목이고 업계 표준 아님**(Procore·Teams B2B·Slack 게스트 전부 통로 보유) ⇒ **고친다**

---

## 4. 변경 계획 — **3단계로 나눈다**

### ★Stage 1 — 결정과 **무관한** 실결함 봉합 (지금 착수 가능)

| # | 결함 | 좌표 | 처방 |
|---|---|---|---|
| S1-1 | 승인이 **고아 루트 노드** 생성 → 수수료 RESIDUAL 전액 귀속 | `market.py:689-693` | raw INSERT 를 `create_node`+`assign_user_to_node` 로 교체(**부모 필수**). 한 곳을 고치면 루트 AGENCY 가드·위계·테넌트 정책이 **전역으로 따라온다** |
| S1-2 | `ancestors_path` 가 고아 루트를 **조용히 배분** | `engine.py:163-182` | `chain[0].node_type != 'AGENCY'` 는 **데이터 무결성 위반**이지 정상 입력이 아니다 — 거부 |
| S1-3 | `release_holdback` **현장 필터 0** | `extension.py:124` · `lifecycle_p6.py:97` | `site_id` 를 인자로 받아 조인. 형제 `run_due_payouts` 가 **이미 옳은 패턴** |
| S1-4 | `member_node_id` **검증 0** | `actions.py:599-606` | 현장 소속 + 살아 있음 + **조상에 AGENCY 존재** |
| S1-5 | 멤버십 판정 `deleted_at` 드리프트 3곳 | `market.py:677,777,804` | `active=true AND deleted_at IS NULL` 로 통일(형제 3곳이 이미 그 형태) |
| S1-6 | `crm_enhance.py:217-221` **거짓 독스트링 3줄** | 같은 곳 | D-1 이 정해지기 전이라도 **거짓 문장은 지운다**(다음 사람이 근거로 삼는다) |

### Stage 2 — 발견·신청·승인 (D-1 결정 후)

R1(3단계 가시성) → R2(sponsor 필수 신청 + 원자적 승인) → 신청 상태기계 보강
(부분 UNIQUE 로 중복 차단 · `withdrawn` · `membership_linked` 를 **bool → 3치**).

### Stage 3 — 현장 전환·비번 폐지 (D-2·D-3 결정 후)

`key={siteId}` + `bySite[siteId]` 재구성(①+③ 조합) · 서버 스코프 스윕 · R5.
★유료 산출물은 **계정×프로젝트 축**이라 현장 전환과 교차하지 않는다 —
`clearAllProjectData` 를 **재사용하면 안 된다**(그것은 계정 전환용이고, 부르면 유료 상태를 근거 없이 비운다).

---

## 5. ★검증하지 못한 것

- **라이브 `isolation_effective`**(RLS 실효) — Stage 1·3 의 심각도가 여기 달렸다
- **A-4 라우트들이 실제 200 을 내는지** — **코드만 읽었고 요청은 안 보냈다**
- **사용자 이메일 도메인 분포** — X1 기각의 두 번째 근거가 이것에 걸려 있다
- **우리 「2단 배분」의 방문판매법상 지위** — 법률 검토 사항. 확인 못 함
- **공인중개사법 §12② 가 분양대행 종사자에게 적용되는지** — 확인 못 함
- **상위가 퇴사·비활성일 때 ltree 가 어떻게 되는지** — 업계는 compression 으로 다루는데 우리는 미확인
- **라이브 `job_applications` 행수** — 승인이 실제로 몇 번 돌았는지

## 6. 되돌리기

Stage 1 은 6건이 **서로 독립**이라 각각 `git revert` 가능하다.
Stage 2·3 은 스키마를 건드리므로 **마이그레이션 down** 을 같은 PR 에 넣는다.

## 7. 잠금

| 잠글 것 | 두 모집단(차가 0인 픽스처는 잠금이 아니다) |
|---|---|
| 노드 생성 경로 단일화 | `market` 에 raw INSERT 를 되살리는 변이 → CAUGHT |
| 루트 노드 금지 | 정상 체인(AGENCY 루트) **정상 배분** / 고아 루트 **거부** |
| 현장 스코프 | site_A 리소스+A컨텍스트 **200·비어있지 않음** / site_B 리소스+A컨텍스트 **403** |
| 통합 축=계정 | 내 고객 **보인다** / 같은 현장 동료 고객 **안 보인다**(같은 site_id 로 픽스처를 짜야 축이 갈린다) |
| 현장 전환 | 계정 축(유료 산출물) **바이트 동일** / 현장 축(selectedUnit·features) **사라짐** — 렌더 결과로 판정 |
