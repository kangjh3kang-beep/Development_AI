# 계획 — #865 가 **네 번째 층을 놓쳤다** (라이브 프로브가 잡았다)

작성 2026-08-26 · SESSION-G · 브랜치 `fix/infra-zone-fourth-callsite`

## 0. 옵시디언 조회

| 찾을 것 | 결과 |
|---|---|
| 같은 클래스의 앞선 결함 | **있음 · 바로 앞 것** — 오류 #103(*"한 층만 고치면 위층이 다시 뭉갠다"*). **그때 세 층을 고쳤는데 네 번째가 있었다** |
| 기각된 접근 | **없음** |
| 이전 판단의 근거 | ★**내 판단이 틀렸다** — #865 를 *"전 경로 전파 완료"* 라고 PR·보드에 적었다 |

## 1. 전제 표 — 실측값만

| # | 전제 | 확인 방법 | 결과 |
|---|---|---|---|
| a | #865 가 배포됐는가 | `git merge-base --is-ancestor 444cd992 f2293d05` + #867 라이브 확증 | **배포됨**(#867 의 조상) |
| b | 그런데 동작하는가 | **라이브 프로브** | **아니다** — `reason: 기반시설부담구역 **미지정**` · `confidence: None` |
| c | 왜 | 호출부 원문 | `rough_feasibility_orchestrator.py:757` 이 **옛 파서** `parse_bool_flag(...)` 로 넘긴다 |
| d | 내 락은 왜 못 잡았나 | 락 원문 | `_gate_params()` 가 **정의부만** 훑는다(`inspect.signature`). **넘기는 값은 안 본다** |
| e | 호출부가 몇 개인가 | 저장소 파생형 스캔 | 2개(`cost_blocks.py` 는 이미 3상태 · **orchestrator 가 남았다**) |

## 2. 변경 내용

호출부를 `parse_tristate_flag` 로 교체(1줄) + import 정리.

### 회귀가 아닌 근거

- **금액 무변화** — 세 상태 모두 `amount_won=0`(안전측 유지). 바뀌는 건 **사유 문자열과 신뢰도**뿐.
- 관련 전수 **90 passed**.

## 3. ★검증하지 못한 것

1. **라이브 종단** — 배포 후. (이번엔 **그것이 유일한 판정**이다 — 락이 초록인데 라이브가 틀렸다)
2. `overrides` 가 아니라 **다른 통로**로 이 값이 들어오는 경로가 있는지 — 호출부 스캔은
   `in_infra_charge_zone=` 표기만 본다. **다른 이름으로 넘기면 못 잡는다**(미측정).
3. 다른 게이트(`all_equity` 등)도 같은 형태인지 — **이 PR 범위 밖**.

## 4. 되돌리기 경로

호출부 1줄 원복. 1파일.

## 5. 잠금 — ★**정의부가 아니라 호출부를 본다**

이번에 뚫린 자리가 정확히 **호출부**다. 그래서 락을 그리로 옮긴다:

| 축 | 검사 |
|---|---|
| **호출부 파생형** | `app/**` 에서 `in_infra_charge_zone=` 으로 **넘기는 줄**을 전부 모은다(손 목록 금지) |
| 탐지 | 그중 **`parse_bool_flag` 를 쓰는 줄이 0개**인가 |
| **대조군** | `parse_tristate_flag` 를 쓰는 호출부가 **적어도 하나** 있는가(*"아무도 안 쓴다"* 가 공허하게 만들지 않게) |
| 공허 방지 | 호출부를 **2개 이상** 찾았는가(0건이 초록이 되지 않게) |

★**교훈**: 배선을 잠글 때 **정의부(시그니처)와 호출부(넘기는 값)는 다른 축**이다.
`inspect.signature` 스캔은 *"받을 수 있나"* 를 보고, 소스 스캔은 *"실제로 뭘 주나"* 를 본다.
**#865 는 앞의 것만 잠갔다.**
