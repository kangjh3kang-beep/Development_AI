# 수지·적산엔진 완성도 100% 실행계획 (2026-07-15)

> 기준: 같은 날 감사(origin/main@340993eb) + 캠페인 7PR(#285/#289/#292/#294/#297/#299/#303) 완결 후 잔여 전수.
> 원칙: 무목업·무날조(근거 없는 수치 생성 금지), 값 변경은 정확성 방향만, 성장루프(구현→적대리뷰→반영→R2→CI→사용자 머지승인) 전건 적용.

## 미해결 전수 대조 (감사 결함 목록 기준)

| # | 항목 | 분류 | 배정 |
|---|---|---|---|
| 수지10 | M01/M02/M04/M08 금융비·소프트비 자동추정 미적용(ROI 566% 잔존) | 즉효 | W1 |
| 수지9 | 판관비/분양경비 스텁(compute_other_cost 합산만) | 즉효(W1과 동일 공용화로 해소) | W1 |
| 적산2 | T2 0단가 무검증(T1 가드와 비대칭) | 즉효 | W2 |
| 테스트 | NPV/IRR 절대값 대조·제비율 11~12단계 값·QTO 무테스트 | 즉효 | W2 |
| 수지7 | 경로A NPV 단일기간 근사 → 월별 DCF | 대형 | W3 |
| 수지6 | DSCR 부재 | 대형 | W3 |
| 수지5 | "상세수지" 경로 부재 | 대형(W3로 실질 해소: /calculate에 월별 현금흐름·IRR·DSCR 통합 = 경로A가 진짜 상세수지) | W3 |
| 프론트 | special_detail 고아·tax_detail 미렌더·BoqAuto 238,504 상수·고아 엔드포인트 4종(/optimize·/finalize·/rough-scenario/report·/upload-ifc) | 즉효~중 | W4 |
| 수지11 | feasibility 서비스 3곳 병존(planning·market 사문화) | 중(삭제 금지 — deprecation 마킹·문서화, 고아≠죽은코드 교훈) | W4 |
| 수지12 | land_conversion 모순(무날조 vs 하드코딩)·calc_forest 고아 | 중 | W4 |
| 정직성 | CalculationMetadata 핵심 엔진 배선 | 중 | W3~W4에 편입 |
| — | avg_area_pyeong 규약 통일(공급/전용) → C01 경로A 교정 재개 | ★제품 결정 | D1 |
| 적산1 | T1 조달청 死계층(ingest 노무비 분해 부재 ↔ 가드) | ★정책 결정 | D2 |
| 적산4 | QTO 커버리지(토공·가설·마감·지붕·조경 전무) | ★근거 데이터 필요(원단위 날조 금지) | D3 |
| 수지3 | 광역교통 표준건축비 고시값 None | ★고시값 확보 필요 | D4 |
| 적산5 | actual G2B 실적단가 미보유 | 대형(별도 수집 파이프) | D5 |
| 적산6 | 법정요율 하드코딩(CODIL 연동 TODO) | 대형(외부 API) | D5 |
| 소건 | tests -k 순서의존 플레이크(test_design_audit_api) | 소 | W2에 원인 진단 편입 |

+ **unknown-unknowns 신선한 눈 감사** 병렬 가동(수치 정확성·경계·이중계상·계약·표시 6축) — 발견분은 트리아지 후 W-시리즈에 편입.

## 결정 필요 묶음(D) — 분석 후 권고안으로 사용자 확인

- **D1 avg_area_pyeong**: 전용/공급 통일 + revenue(공급단가×면적)·C01(전용 85㎡) 소비처 정합 일괄. 권고안 작성 예정.
- **D2 T1 정책**: 조달청 응답에 노무비 분해가 실제로 있는지 확인 → 있으면 ingest 보강, 없으면 "합계단가 T1 채택+표준 분해비율" 정책의 정직성 검토.
- **D3 QTO 원단위**: 공신력 원단위 소스 없이는 확장 금지(무날조). 커버리지 한계 라벨 강화가 정직한 대안.
- **D4 표준건축비**: 국토부 고시값 확인 후 관리자 설정 채널로 주입(코드 하드코딩 지양).
- **D5 T1-actual·CODIL**: 외부 연동 대형 — 별도 캠페인.
- **D6-부속(W3 R2 비차단)**: IRR KPI 인라인 주석/비현실치 플래그(조기유입 라벨을 KPI 옆에) · M08 NOI=0 엣지(dcf.npv_won>0 조건 병용).
- **D6 분양대금 분할 유입 모델링**(W3 리뷰 파생): 엔진 `_revenue_distribution`이 초기 집중형(전액 조기 유입)이라 무차입 IRR이 고평가됨 — 계약금·중도금·잔금(10/60/30 등) 분할 유입 모델 도입 시 rough·경로A IRR이 현실화. 현재는 assumptions 정직 라벨로 노출.

## 제품결정 권고안 (2026-07-16 분석 완료 — 사용자 확인 대기)

| # | 항목 | 권고 | 근거·비용 |
|---|---|---|---|
| D1 | avg_area_pyeong 규약 | **전용평 통일** + revenue_block이 공급 환산(전용/전용률×공급단가) | 지배 생산처(폼 라벨 "평균 전용면적"·orchestration 계약·baseline)가 이미 전용. 공급 생산처 2곳(build_module_input·precheck)만 수정. 부수효과: orchestration 경로의 매출 과소(전용×공급단가)도 함께 교정됨. 변경 후 C01 경로A 교정 재개 가능. 규모: 중(모듈 매출 수치 변동·테스트 재기준) |
| D2 | T1 조달청 死계층 | ingest 노무/경비 필드 매핑 확장 시도(표준시장단가는 원래 재료/노무/경비 분해 보유) — 실 API 응답 샘플 확보 후 | 라이브 API 검증 필요(키·호출). 분해 미제공 확인 시 "합계단가+표준 분해비율" 정책은 무날조 위반 소지라 비권고 |
| D3 | QTO 원단위 확장 | **보류 유지** — 공신력 원단위 소스(표준품셈 전자본·LH 실적) 확보 전 확장 금지(무날조). 현행 "부분적산(골조 중심)" 정직 라벨이 정답 | 즉시 작업 없음 |
| D4 | 광역교통 표준건축비 | 국토부 고시값 확인 후 **관리자 설정 채널 주입**(standard_build_cost_won_per_sqm 인자 이미 존재 — 코드 하드코딩 지양) | 고시 번호·값 웹 확인 1회 + 설정 절차 문서화 |
| D6+갭P2 | 금융·유입 정밀화(W5 후보) | ①분양대금 분할 유입(계약 10·중도 60·잔금 30 표준 스케줄, additive) → IRR 현실화 ②금융 자동추정을 분할실행 기저(평균잔액 ~50%)로 정합 → "정밀입력할수록 유리" 역설 해소 | 수치 변동 큼(전 모듈 금융비 절반 수준) — 별도 PR·리뷰 루프 |
| 고아 3종 | /optimize·/finalize·/upload-ifc | **문서화 유지 권고** — 마운트 위치는 제품 결정(고아≠죽은코드 교훈). /finalize는 Top3→확정 플로우, /optimize는 최적화 UI, /upload-ifc는 CAD 업로드 UX가 정해져야 배선 가능 | 배선 원하면 화면 위치 지정 필요 |
| disposal 0 | 양도세 총사업비 미계상 | **현행 유지(세전수지)** — 업계 관례·화면에 "세전 이익" 라벨 기존재. 양도·법인세는 별도 세후 분석 축으로 | 즉시 작업 없음 |

## 순서

W1+W2(한 PR, 즉효) → W3(상세수지 완성, 한 PR) → W4(프론트·정리, 한 PR) → D1~D4(분석·권고→결정→구현). 갭 감사 발견분은 심각도별 삽입.

## 외부 의존 라이브 배선검증 결과 (2026-07-16 — PR#322)

사용자 요청 "외부 의존·별도 캠페인 성격도 정상배선·연동 검증" 수행. 전 건 실키(루트 .env)·실호출 그라운드 트루스:

| 항목 | 라이브 결과 | 판정·조치 |
|---|---|---|
| D2 조달청 가격정보(15129415) | 200·639건. 분해 필드 mtrlcst/lbrcst/gnrlexpns **스키마 실존·전건 빈 값**(prceDiv 전건 "기타가격"). 자매 오퍼레이션(건축/기계/전기 추정명) 전부 404 — 토목만 등록 | T1 死계층 보류 = 정당(라이브 확증). ingest에 분해 매핑+정합 가드(합≠prce 1%·음수 거부) 추가 → 데이터 채워지면 자동 재활성(PR#322). ★활성 전 확정 요건: 분해가 제비율 전(순공사원가) 분해인지 — 올인 분해면 제비율 12단계 중복(30~40% 과대) |
| D5 G2B | 입찰공고 200·1,667건 / 낙찰 200·1,325건(3일) | 정상 연동 — 수정 불요. actual 실적단가 수집 파이프는 별도 캠페인 유지 |
| D5 CODIL | rate_tasks 명시 스텁·legal_rate_service:63 TODO·config BASE만 | 정직 미연동(별도 캠페인) |
| D4 표준건축비 | 준용 근거 확정: 광특법 시행령 §16조의2 → **공공건설임대주택 표준건축비 고시(층수×면적 구간표)**. 단일 상수 하드코딩 자체가 부정확. 기존 주석 "제2024-192호"는 표준**개발비** 고시(오기) | env `METRO_STANDARD_BUILD_COST_WON_PER_SQM` 운영 주입 채널 신설+오기 정정(PR#322). 값 주입은 운영 결정(사업 포트폴리오 구간값) |

성장루프: R1 REVISE(H1 분해 합-정합 무검증·M1 음수 위장 — 라이브 재현 후 봉합) → R2 APPROVE. PR#322 CI 대기.
