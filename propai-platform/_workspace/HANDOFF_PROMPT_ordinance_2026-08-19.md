PropAI 작업을 이어서 진행해줘.

## 1) 먼저 읽을 것
- 옵시디언 `AI-Sessions/conversations/2026-08-19_PropAI_조례파서_전국화_인계.md`
  (+ `wiki/errors/2026-08-19_조례파서_전면고장을_정적캐시26곳이_가렸다.md`)
- `CLAUDE.md` 검증 규율 · 회귀망 규율 A~F
- ★그 문서의 수치는 작성 시점 값이야. **착수 전에 다시 재줘.**

## 2) 지금 상태
- **PR #696**(`fix/registry-workspace-jibun`) — 지번 실종 근치. ruff 수정 완료. **통합자가 배포 대기**.
  ★배포 순서는 **168(백엔드) 먼저** — 지번은 백엔드 응답이 담아야 프론트가 표시해.
- **PR #700**(`fix/zoning-plan-far-wiring`, 워크트리 `~/My_Projects/Development_AI_zoneplan`) — 14커밋.
  조례 파서 커버리지 **자연녹지 6.7% → 98.0%**, 파서 실패 37 → 5건(전수 150곳).

## 3) 다음 작업 (우선순위 순)
1. **★부지 조건 매칭 — 이 캠페인의 원래 목표.** `conditional[]` 수집은 끝났는데 **소비처 0**이야.
   대상 부지(오산 내삼미동)는 화면 §7에 **성장관리권역**이 떠 있어서 조례 제50조
   **건폐율 30%** 가 실제 적용값일 수 있어. 지금 화면은 그걸 표시만 하고 건폐율에 반영 못 해.
2. 울산광역시(2)·창원시(3) 잔여 실패 — **원인 미상**(선택기 문제는 아님이 확인됨)
3. 제2종일반 `no_value` 4건
4. **VWorld 지구단위계획 배선** — P1~P6 계획 수립·실측 완료인데 **구현 0**.
   특히 P2: `far_tier_service.py:264` 가 `plan_payload=base.get("special_districts")` 인데
   그건 **주소 문자열에 "지구단위"라는 글자가 있어야 채워지는 휴리스틱**이야(원리적으로 거의 빈 값).
   실측 데이터는 `land_use_plan.districts` 에 있고, 정답 기준선은 `regulation_analysis_service.py:98`.
5. **면적 계약** — `/development-methods/scenarios` 가 `parcels: list[str]` 라 면적을 재파생해서
   86,755㎡ 부지를 **44㎡** 로 판정하고 19개 개발방식을 불가로 막아. 정답 기준선은
   `/analysis/comprehensive`(543㎡ 버그 수정 커밋 `dc995343`).
6. **허용용도 게이트** — 고시상 **단독주택은 명시적 불허**인데 플랫폼이 357세대로 추천 중.

## 4) 이미 실측한 것 — 다시 재지 마
- VWorld 지구단위계획: `data=LT_C_UPISUQ161` + **`domain=https://4t8t.net` 필수**(없으면 `INCORRECT_KEY`)
- PNU → `LP_PA_CBND_BUBUN`(`attrFilter=pnu:=:...`) → 폴리곤 → **POLYGON geomFilter 교차조회**.
  ★**점 조회는 틀린 방법** — 같은 사업지에서 438 지점 0건, 114 지점 1건이 나와.
- 연속지적도가 `jibun`·`addr` 을 직접 준다(#696 의 PNU 파생보다 권위 있는 소스)
- VWorld 최신성 한계: 2025-12-23 고시가 **8개월째 미반영**
- 토지이음 고시 상세는 우리 백엔드에서 200 으로 열림(EUC-KR)
- data.go.kr `UpDsupService` 는 우리 키로 호출 불가(코드12 — "없음"과 "미승인"을 **구분 안 함**)

## 5) ★작업 방식 — 이 세션이 비싸게 배운 것
- **증상에서 원인을 추론하지 마. 재.** 내가 6번 단정했고 6번 다 틀렸어(인계문서 표 참조).
- **"0건"을 보고하기 전에 대조군이 살아 있는지 봐.** 양성·음성 둘 다.
- **분모에 무엇이 들어가는지가 결론을 바꿔.** 판정을 갈라놓고 지표에서 다시 뭉뚱그리면 무의미해.
- **컨테이너 핫패치 전에 `M.__file__` 로 임포트 경로를 확인해.** 파일이 두 벌 있어
  (`/app/app/...` vs **`/app/apps/api/app/...`** ← 이쪽이 임포트됨).
- 168 라이브 컨테이너는 **`propai-api-8001`**(blue-green, 8000 아님).
- 변이도구는 **로컬 main 과 diff** 하니 stale 하면 남의 병합분이 딸려와.
- 커버리지 하네스: `scripts/ordinance_coverage.py` → `docker cp` 후 `python /tmp/cov.py [--limit N]`
  (전수 150곳 약 8분). **고치기 전후를 같은 하네스로 재서 진척을 숫자로 말해.**
- `/insight-loop` 으로 자기 전제부터 반증하고, **커밋 먼저 → 변이 → 원복** 순서를 지켜.
