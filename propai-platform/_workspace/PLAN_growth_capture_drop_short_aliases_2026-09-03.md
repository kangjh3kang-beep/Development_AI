# 계획 — `#945` 미착지분: 짧은 별칭 제거 + 실패 가능한 상한 락 (2026-09-03)

★이 계획서는 **이미 머지된 `#945` 계획의 미착지분**을 다룬다. 새 설계가 아니라 **교정**이다.

## 0. 옵시디언 조회 결과

| 찾을 것 | 결과 |
|---|---|
| 이미 기각된 접근 | **있음** — 짧은 이름으로 표시 우선순위를 얻는 것은 *"락이 아니라 경주"* 로 **동료가 반증**했고 내가 확증했다(`+sent`·`+age`·`+rate` 전부 밀어냄) |
| 같은 클래스의 앞선 결함 | `2026-09-02_jsonb_는_키_순서를_안_지킨다…` §6~14 — 특히 **§13 실패할 수 없는 단언** |
| 미결·부채 | 렌더러의 **명시 선택**(동료 담당) · `analysis_coverage` 소비처 0(#882 경합) — **둘 다 범위 밖** |
| 이전 판단의 근거 | `#945` 의 *"짧은 이름으로 앞자리를 얻는다"* — ★**이 계획이 그것을 뒤집는다** |

## 1. 전제 표 — 확인 방법과 **실제 측정값**

| # | 전제 | 확인 방법 | **결과(실측)** |
|---|---|---|---|
| P1 | `#945` 가 내 교정 2건을 빼고 머지됐다 | `origin/main` 소스 | 짧은 별칭 **2건 존재** · 실패불가 단언 **1건 존재** |
| P2 | 별칭의 목적(가시성)이 이미 달성됐나 | 라이브 `/heal-log` | `growth_capture` 17키 · 보임 17 · **버림 0** |
| P3 | 별칭이 남기는 비용이 있나 | 두 표면 대조 | `/effectors` = `queue_depth` · `/heal-log` = `depth` → **같은 사실, 두 이름** |
| P4 | 형제는 어떤 이름을 쓰나 | `analyzer.py` | `producer_build_id` (짧은 `build` 아님) |
| P5 | 종전 상한 단언이 실패할 수 있나 | 변이(키 30개 추가 → 47키 > 상한 40) | ★**SURVIVED** — 우변이 항상 참 |
| P6 | 고친 단언은 실패하나 | 같은 변이 | **CAUGHT**(`발행 키가 45개로 상한 40 을 넘었다`) |

## 2. 변경 내용과 **회귀가 아닌 근거**

- 짧은 별칭 제거 → 이름이 `capture_status()`·형제와 **일치**한다. P2 로 가시성 손실 **0**.
- 상한 단언을 `assert n <= live_cap` 로 → **거짓이 될 수 있는 입력이 존재**한다(P6).

## 3. ★검증하지 못한 것

- **상한 40 이 적절한지**는 안 쟀다 — 그것은 동료가 고른 수이고, **넘는 순간을 내 락이 잡는다**.
- 별칭 제거가 **화면에 미치는 영향**은 라이브로 안 봤다(P2 로 「전부 보인다」만 확인).
- `#947` 렌더러의 명시 선택이 들어오면 이 축 전체가 **불필요해질 수 있다** — 그때 다시 판단한다.

## 4. 되돌리기 경로

두 커밋을 되돌리면 `#945` 머지 시점 상태로 돌아간다(별칭 부활 + 실패불가 단언 부활).
★후자는 **되돌리면 안 되는 것**이라 락이 그것을 잡는다(P6).

## 5. 잠금

| 축 | 락 |
|---|---|
| 상한 초과 | `assert n <= live_cap` — 변이(키 30개) **CAUGHT** |
| 이름 일치 | payload 키를 `capture_status()` 에서 **파생**해 대조 |
| jsonb 순서 | 왕복 락 + **모델을 라이브 관측과 대조**하는 락 |
| 교차참조 | 프론트 락 파일·축이 사라지면 **여기서 빨개진다** |

★**이 PR 이 닫지 않는 것**: 화면이 「무엇을」 버렸는지는 여전히 말하지 않는다(개수만 말한다).
그 축은 렌더러의 **명시 선택**이고 동료 담당이다.
