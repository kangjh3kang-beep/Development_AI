# 성장루프 전송 — 64KiB 절벽에서의 조용한 전손 (2026-08-28)

세션 development-ai-01 [2416cd] · 브랜치 `fix/growth-transport-size-aware`
인계: SESSION-32 §4 최상위 미해결 · 보드 3회 "별건 부채"로 적히고 무주인

## 0. 옵시디언 조회 결과 (계획 전 · 필수)

Vault `/mnt/d/옵시디언기록/나의-모든-기록-최적화본` 접근 OK(대조군: `wiki/errors/` 170개 · `log.md` 실재).

| 찾을 것 | 결과 |
|---|---|
| 이미 기각된 접근 | **없음** — 이 전송부 크기 축을 다룬 기록 0건 |
| 같은 클래스의 앞선 결함 | **있음** — `2026-08-28_쌍둥이는_그_이유를_적어_뒀는데_형제는_안_쓸렸다`(형제 미스윕) · `2026-08-27_대조군이_증명하는_것과_결론이_필요로_하는_것이_다르다` |
| 미결·부채 | **있음** — 보드 7148행이 *"sendBeacon 64KiB 예산 vs 조기버퍼 최악치 — **미측정**"* 이라 적어 둠. 이 계획서가 그 미측정을 해소한다 |
| 이전 판단의 근거 | 인계서 §4 의 *"fetch keepalive 예외 없음"* — **재 보니 부정확**(§1 전제표 참조) |

★큐 정체 축은 `2026-08-23_각자_최적행동이_전체를_악화시킨다` 를 읽고 **큐를 건드리지 않기로** 했다
(직렬화는 development-ai-29 단독 · 보드 08:45 NOTE).

## 1. 전제 표 — 확인 방법과 **실제 측정값**

| # | 전제 | 확인 방법 | 실제 측정값 |
|---|---|---|---|
| 1 | `flush()` 가 보내기 **전에** 링을 비운다 | 소스 직접 판독 `event-collector.ts:291` | **참** — `const batch = ring.splice(0, MAX_BATCH)` 후 실패해도 되돌리는 코드 없음 |
| 2 | 크기/바이트 가드가 **없다** | 전수 grep + 대조군 | **참 · 0건**. 대조군 `MAX_BATCH` 3회 히트로 조회기 생존 확인 |
| 3 | 실패가 조용히 삼켜진다 | 소스 판독 `:314-322` | **참** — `.catch(() => {})` · 바깥 `catch {}` 이중 |
| 4 | 한 배치가 64KiB 를 넘을 수 있다 | 상수 파생 | **참** — `MAX_BATCH=100`, 바이트 상한 없음 |
| 5 | 조기버퍼 최악치가 절벽 위 | 상수 파생 `early-error-bootstrap.ts:21,28,29` | **참** — `CAP=20 × (MESSAGE 8000 + STACK 2000자)` ≈ **206KB**. ★**7건만 넘어도 64KiB 초과** |
| 6 | ★인계서의 *"fetch keepalive 예외 없음"* | Fetch 표준 재확인 + 소스 | **부정확 — 실제로는 더 나쁘다.** `keepalive` 요청은 sendBeacon 과 **같은 64KiB 예산**을 공유한다. 즉 폴백이 **같은 이유로** 실패하므로 구제 경로가 원리적으로 없다 |
| 7 | ★**형제 비대칭** — `js_error.message` 가 무상한 | 소스 판독 | **참 · 신규 발견**. 같은 함수의 `stack` 은 `.slice(0,2000)`, **형제** `handleRejection` 의 `message` 는 `.slice(0,1000)` 인데 `handleWindowError` 의 `message` 만 **무상한**(`:367`) |
| 8 | 백엔드 계약을 안 바꿔도 되나 | `app/routers/growth.py` 판독 | **참** — `_MAX_BATCH=100` 상한만 있고 하한 없음. **더 적게 보내는 것은 계약 내** |
| 9 | 기준선이 초록인가(변이 판정 유효성) | 실행 | **참 · rc=0 · 7 files · 43 passed**. ★빨간 기준선이면 모든 변이가 거짓 CAUGHT(#924) |

★전제 6·7 은 **인계서에 없던 것**이고 재서 나왔다. 6 은 인계서보다 **나쁜** 쪽으로 정정된다.

## 2. 변경 내용과 **회귀가 아닌 근거**

1. **바이트 상한 배치** — `MAX_BODY_BYTES` 안에서만 배치를 만든다. 건수 상한(`MAX_BATCH=100`)은 유지.
   *회귀 아님*: 백엔드는 100 **이하**를 받는다(전제 8). 더 적게·더 자주 보낼 뿐 계약 불변.
2. **실패 시 링으로 되돌린다** — 전송 실패분을 링 **앞**에 복원. 다음 틱에 재시도.
   *회귀 아님*: 기존 축출 정책(`ring.shift()` = 오래된 것부터)을 그대로 쓰므로 메모리 상한 불변.
3. **형제 비대칭 해소** — `js_error.message` 에 형제와 같은 상한.
   *회귀 아님*: 상한이 없던 필드에 상한을 주는 것이라 기존 초과 사례만 짧아진다.
4. **드롭을 센다** — 불가피하게 버린 건수를 계수. 침묵을 관측 가능하게.
   *회귀 아님*: 읽기 전용 계수기. 와이어 포맷 무변경.

★**하지 않는 것**: 와이어 포맷에 필드 추가(백엔드 스키마 위험) · 엔드포인트 변경 · 재시도 무한화.

## 3. ★검증하지 못한 것

- **실브라우저에서 64KiB 초과 시 keepalive 가 실제로 거부되는지 라이브로 태우지 않았다.**
  이유: 프로덕션 `platform_events` 에 쓰레기 이벤트를 주입하게 된다. 표준 문서와 소스 판독에 의존한다.
  ★따라서 처방은 **크기와 무관하게** 옳도록 설계했다(실패면 되돌린다) — 전제 6 이 틀려도 1·2 는 유효.
- **라이브에서 이 전손이 실제로 몇 건 났는지 세지 못했다.** 전손은 정의상 **DB 에 0행**이라
  사후 조회가 원리적으로 불가능하다. 그래서 이 PR 이 **계수기**를 넣는다(다음 세션이 셀 수 있게).
- `js_error.message` 가 라이브에서 얼마나 긴지 **미측정**(분포를 못 봤다).
- 조기버퍼 20건이 실제로 한 번에 찬 사례가 있었는지 **미측정**.

## 4. 되돌리기 경로

단일 커밋 revert. 런타임 영향은 `event-collector.ts` 한 파일에 갇힌다.
되돌리면 종전 동작(무상한 배치 · 전손)으로 복귀하며 백엔드·스키마·배포 아티팩트는 무관.

## 5. 잠금 — 이 변경을 지키는 검사

| 축 | 무엇을 잠그나 |
|---|---|
| 탐지 | 64KiB 초과 입력에서 **어떤 배치도 예산을 넘지 않는다** |
| 특이도 | 예산 **이하** 입력은 **한 번에** 나간다(잘게 쪼개지 않는다 — 항상 1건씩 보내는 구현이 만점받지 않게) |
| 배선 | 전송 실패 시 이벤트가 **링에 남는다**(두 모집단: 성공→비고 / 실패→잔존) |
| 계약 | 건수 상한 100 **유지** · 와이어 포맷 불변 |
| 형제 | `js_error.message` 상한이 `promise_rejection` 과 **같은 축**에 있다 |

★단언은 **이름이 아니라 값**을 본다(메모리 교훈). 상수 자기참조 단언 금지 — 리터럴로 못 박는다.
