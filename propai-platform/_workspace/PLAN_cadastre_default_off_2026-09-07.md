# 지적 경계선이 **항상 켜져 있고 끌 수도 없다** — 사용자 신고

작성: development-ai-fe (sid=`dcb7a4f2` · 2026-09-07 · 절대형 서명)
브랜치 `fix/cadastre-default-off` · base `0a88202a5`

---

## 0. 사용자 신고 (스크린샷 3장 · 라이브 `4t8t.net/ko`)

> ① *"지적 경계선이 항상 나타나는데 기본은 경계선이 없는 것이고 오른쪽 메뉴에서 선택 시 나타나도록 해야 하지 않나?"*
> ② *"오른쪽 메뉴 패널에서 베이스맵과 지형도·항공뷰는 같은 기능 아닌가?"*

★사용자 지시(2026-09-07): **①은 「기본을 끔으로 바꾸고 선택 시 표시」** · **②는 「먼저 실측하고 판단」**.
⇒ **이 PR 은 ①만 다룬다.** ②는 실측을 마쳤고(§3-b) 설계 결정이 남아 별건이다.

---

## 1. 전제 표 — 확인 방법과 **실측값**

| # | 전제 | 확인 방법 | 결과(관측) |
|---|---|---|---|
| P1 | 기본이 켜져 있다 | `useSatongMapPrefsStore.ts` 원문 | `defaultEnabledLayerIds() → ["cadastre"]` |
| P2 | **끌 수 없다** | `toggleLayerEnabled` 구현 | `if (has && id === "cadastre") return s;` — **한 줄이 막는다** |
| P3 | 레일 버튼이 죽어 있다 | `LAYERS_WITHOUT_POPOVER_TOGGLE` | `["terrain", "cadastre"]` · 주석 *"토글 무동작 = **죽은 버튼**"* |
| P4 | ★**그 제약의 사유가 실재하는가** | 선택 경로 전수 추적 | ★**아니다.** `showCadastreTile` 은 **WMS 타일 오버레이만** 가드한다(`SatongMultiMap.tsx:2183`). 필지 선택은 `boundaryFeatures`/`staged` 에 의존하고 **그 플래그를 참조하지 않는다** |
| P4-근거 | 코드가 스스로 적었다 | `SatongMultiMap.tsx:1500` | *"지적 안내 effect 는 deps 가 `[mapReady, showCadastreTile, aerialView]` 라 **선택을 직접 참조할 수 없다**"* |
| P5 | 기존 락이 그 결정을 잠근다 | `SatongMapShell.basemapSwitcher.test.tsx:319` | *"★cadastre(지적도)는 on/off 미노출 — 끌 수 없어 토글이 죽은 버튼이 된다"* |
| P6 | 기본값 소유자가 하나인가 | `enabledLayerIds` 전수 | ★**아니다.** 정본 `defaultEnabledLayerIds()` 외에 **하드코딩 4곳**: `GlobalAddressSearch.tsx:1640` · `LandScheduleClient.tsx:139` · `ZoningSignalMap.tsx:83` · `ParcelBoundaryMap.tsx:174~180` |

### ★P4 가 이 PR 의 근거다

*"기반 레이어라 끌 수 없다"* 는 **근거 없는 제약**이었다. 선택은 지적 타일과 **독립**이고,
코드 자신이 그 독립을 주석으로 적어 두었다. ⇒ **끄는 것이 안전하다.**

### ★P6 — 「기본값」이 다섯 곳에 있다

정본을 고쳐도 **하드코딩 4곳은 안 따라온다.** 이 저장소가 반복 경고한
*"사람이 센 목록이 곧 상한"* 의 자리다. **정본만 고치고 끝내면 신고가 절반만 해소된다.**
★단 **네 곳의 성격이 다르다**(§2-b) — 일괄 치환은 오히려 결함이다.

---

## 2. 변경 내용

### 2-a. 정본 기본값을 끔으로

`defaultEnabledLayerIds() → []`. 그리고 `toggleLayerEnabled` 의 **cadastre 예외 한 줄을 제거**한다
(끌 수 있어야 「선택 시 표시」가 성립한다).
⇒ `LAYERS_WITHOUT_POPOVER_TOGGLE` 에서 `cadastre` 를 빼 **죽은 버튼을 되살린다**.
⇒ 기존 락 `basemapSwitcher.test.tsx:319` 는 **계약이 바뀌었으므로 함께 고친다**
   (락이 옛 계약을 잠그고 있으면 그것이 회귀 신호가 아니라 **낡은 잠금**이다).

### 2-b. 하드코딩 4곳 — ★**일괄 치환하지 않는다**

| 좌표 | 성격 | 이 PR 의 처리 |
|---|---|---|
| `GlobalAddressSearch.tsx:1640` | 주소검색 **미리보기** 지도 | ★**유지** — 필지를 고르라고 띄운 지도다. 경계선이 없으면 무엇을 고르는지 안 보인다 |
| `LandScheduleClient.tsx:139` | 토지조서 지도 | ★**유지** — 같은 이유(필지 단위 작업) |
| `ZoningSignalMap.tsx:83` | 용도지역 신호 지도 | ★**유지** — `["cadastre","zoning","terrain"]` 이 그 화면의 의미다 |
| `ParcelBoundaryMap.tsx:174~180` | 구획도 | ★**유지** — 경계 자체가 주제다 |

⇒ **바꾸는 것은 「사용자가 자유롭게 탐색하는 지도」(`SatongMapShell` 경유 정본)뿐**이다.
★***목적이 다른 화면에 같은 기본값을 강요하는 것이 오히려 결함이다.*** 신고의 대상은
스크린샷의 그 화면(통합 필지 입력 · `SatongMapShell`)이고, 그것이 정본을 쓴다.

---

## 3. ★검증하지 못한 것

1. **화면(브라우저) 실측 없음** — 락은 렌더 테스트로 잠근다. 라이브 확인은 배포 후.
2. **저장된 사용자 설정(persist)** — 이미 `cadastre` 를 켠 채 저장한 사용자는
   `enabledLayersCustomized === true` 라 **새 기본값이 안 닿는다**(스토어가 그렇게 설계돼 있다).
   ⇒ 그들에게는 **변화가 없다.** 이것이 옳은지 미결 — 「골랐다」를 존중하는 것이 계약이므로 **유지**하되,
   신고자가 그 상태일 수 있다(그러면 신고 해소가 체감되지 않는다). **미측정.**
3. **신고② 는 이 PR 밖** — 실측은 끝냈다(아래).

### 3-b. 신고② 실측 결과 (별건 · 착수 전)

| | 베이스맵 팝오버 | `terrain` 레이어 |
|---|---|---|
| 항목 | 일반·위성·**하이브리드**·회색 (4) | 기본지도·위성·**항공뷰**·**표고**·회색지도 (5) |
| 상태 | **같은 `terrain` 컨트롤** | 동일 |
| 배경 결정 | `resolveVWorldBaseLayer()` **하나** | 동일 |

★**중복이 맞다** — 「하이브리드」와 「항공뷰」는 이름만 다르고 **같은 `Hybrid` 로 귀결**한다.
★**단 완전히 같지는 않다**: `elevation`(표고)은 `mapEffect: false` **미구현 자리**이고,
  `terrain` 은 **레이어 on/off 의 소유자**다(*"끄면 배경지도가 조용히 롤백·라벨도 거짓"*).
⇒ **없애는 선택지는 위험**하다. 별건 계획서에서 다룬다.

---

## 4. 되돌리기 경로

단일 PR revert. 프론트 스토어 기본값 + 토글 예외 + 락 · **백엔드·계약 변경 0**.

---

## 5. 잠금

| 잠그는 명제 | 형태 |
|---|---|
| **기본이 꺼져 있다** | `defaultEnabledLayerIds()` 가 `cadastre` 를 **포함하지 않음** |
| **★끌 수 있다** | 켠 뒤 토글 → **실제로 꺼짐**(종전엔 같은 참조를 돌려줘 무동작) |
| **두 모집단** | 켜기·끄기를 **같은 실행에서** — 한쪽만 보면 「항상 켬」도 통과 |
| **레일 버튼이 살아 있다** | `LAYERS_WITHOUT_POPOVER_TOGGLE` 에 `cadastre` **없음** |
| **★선택은 영향받지 않는다** | 지적을 끈 상태에서도 필지 선택이 동작(P4 의 독립성을 **락으로** 고정) |
| **목적이 다른 화면은 유지** | 하드코딩 4곳이 여전히 `cadastre` 를 포함(§2-b 가 의도임을 잠근다) |

★**변이로 CAUGHT 를 확인한다.** ★**닫지 못하는 것**: persist 된 사용자(§3-2) → `it.todo`.
