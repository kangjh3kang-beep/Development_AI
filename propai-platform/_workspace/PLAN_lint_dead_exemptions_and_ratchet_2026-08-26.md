# 죽은 면제 31건 제거 + eslint 경고 비성장 래칫

**2026-08-26 · SESSION-E · 브랜치 `fix/lint-dead-exemptions`**

## 0. 옵시디언·저장소 조회 결과

조회함.

- **저장소가 스스로 적어 둔 부채** — `ci.yml:202`:
  *"eslint 경고(~190)는 `--max-warnings` 미설정이라 통과 — **경고 정리 후 0 게이트화 예정**."*
  **그 예정이 오지 않았다.**
- **CLAUDE.md §G-2 36** — *"배선만 하고 면제를 남기면 락은 skip 이라 무잠금이다. …
  **죽은 면제도 실패시켜라.**"* 지금 31건이 정확히 그 대상이다.
- **형제 래칫** — `apps/web/lib/__tests__/render-clock-ratchet.test.ts`(파일별 맵 · 미등재/낡음 양방향).
  이 PR 은 그 형태를 따른다.
- **§G-2 32(ruff 사고)** — *"CI 가 린터를 핀 없이 설치하면 새 릴리스가 코드 변경 0으로 전 PR 을
  빨갛게 만든다."* → 래칫을 걸기 전에 **핀 상태를 먼저 쟀다**(아래 전제표).

## 1. 전제 표 — 확인 *방법* 과 **실제 측정값**

| 전제 | 확인 방법 | 측정값 |
|---|---|---|
| 현재 경고 수 | `npx eslint . --no-cache` 전수(`origin/main` a7838464) | 파일 1,060 · **경고 189 · 에러 0** · 경고 보유 파일 **95** |
| 규칙 분포 | 리포트 집계 | `no-unused-vars` **74** · **`ruleId=null` 31** · `set-state-in-effect` 27 · `refs` 27 · `exhaustive-deps` 16 |
| `ruleId=null` 의 정체 | 메시지 원문 | **"Unused eslint-disable" 30 + "Unused eslint-enable" 1** = 죽은 면제(★초판은 "전부 disable"이라 적었다 — 리뷰 F7) |
| 미사용 선언의 성격 | 메시지 원문 | 74 중 *"정의됐으나 미사용"* **55**(대개 미사용 import) |
| eslint 가 핀돼 있나 | `package.json` + `pnpm-lock.yaml` + 로컬 실행 | `"^9"` 캐럿이지만 **lock 에 9.39.4 고정** · 로컬 `v9.39.4` 일치 |
| CI 가 lock 을 지키나 | `ci.yml` | `pnpm install` — pnpm 은 **CI 환경에서 frozen-lockfile 기본** |
| 제거 후 경고 수 | 재집계 | **158** · 에러 0 · `ruleId=null` **0** |
| 타입 회귀 | `tsc --noEmit` | **EXIT=0** |

## 2. 왜 전면 `--max-warnings 0` 이 아닌가

그러면 **다른 세션의 무관한 PR 이 즉시 빨개진다.** 이 저장소는 동시에 여러 세션이 개발하고,
지금 158건이 84파일에 흩어져 있다. 래칫은 *"늘지 않게"* 만 하므로 **기존 경고가 있는 파일을
건드리지 않는 PR 은 영향이 없다.**
★**전면 게이트화는 팀 결정 사안**으로 남긴다 — 나 혼자 정할 것이 아니다.

## 3. 왜 단일 카운트가 아니라 파일별인가

- 단일 카운트는 **어디서 늘었는지 말하지 않는다.**
- 파일별이면 **줄었을 때도 알려 준다** → 래칫을 낮추라는 신호가 되어 **정리를 유도**한다.
  ★단 줄었다고 **실패시키지는 않는다** — 곁가지로 하나 고친 PR 을 빨갛게 하면 정리를 벌주는 꼴이다.

## 4. 검증한 것 (4축)

| 축 | 주입 | 결과 |
|---|---|---|
| 성장 탐지 | 기존 파일에 경고 1건 | **exit 1** · `8 → 9 GlobalAddressSearch.tsx` 지목 |
| 신규 파일 | 새 파일 + 경고 1건 | **exit 1** · `0 → 1 (신규 파일)` |
| 감소 | 경고 8→3 | **exit 0** + *"래칫을 낮출 수 있다"* |
| ★입력 이상 | 없는 파일 · 빈 파일 · `[]` | **전부 exit 3**(통과 아님) |

★4번이 핵심이다 — **"경고 0"과 "리포트를 못 읽음"은 같은 0으로 보인다.**
`[]` 를 "깨끗함"으로 읽으면 린트가 아무 파일도 안 봤을 때 **조용히 초록**이 된다.

## 5. ★검증하지 못한 것 (승계 금지)

1. **CI 에서 실제로 돌려 보지 않았다.** `pnpm lint --format json -o` 의 인자 전달은
   로컬에서 확인하지만, GitHub Actions 러너에서의 동작은 **이 PR 의 CI 가 첫 실측**이다.
2. **제거한 31개 면제가 정말 "죽은" 것인지는 eslint 판정을 신뢰한 것**이다.
   `--report-unused-disable-directives` 가 그렇게 말했고 나는 재검증하지 않았다.
   ★다만 `tsc` 는 통과했고 **에러는 0 그대로**다.
3. **런타임 테스트를 안 돌렸다**(vitest 전수). 주석 제거라 논리 변경이 없지만 **재지 않았다.**
4. **다른 세션이 지금 만들고 있는 PR 이 이 래칫에 걸릴 수 있다.** 그들의 브랜치는
   래칫 파일이 없는 상태에서 갈라져 나갔으므로, 머지 후 경고를 새로 만들면 빨개진다.
   ★보드에 공지했다. **불편이 예상되면 되돌리는 것이 옳다.**
5. ★**래칫은 살아 있는 `eslint-disable` 추가로 우회된다.** 카운트가 줄어 exit 0 이 된다.
   즉 이 게이트는 **코드 건강이 아니라 "보이는 경고"** 를 잰다 —
   **죽은 면제를 걷는 PR 이 살아 있는 면제를 붙일 유인을 만든다.** 그 유인은 막지 못한다.
6. **공백 정리가 범위를 조금 넘었다.** 이미 손댄 2파일(`AIAssistant.tsx` · `SatongMultiMap.tsx`)에서
   **base 에 원래 있던 공백만 있는 줄 7개**도 함께 사라졌다. 의미 변화 0 이지만 **선언한 범위 밖**이라 적는다.
7. **`react-hooks/*` 70건은 손대지 않았다** — 그건 진짜 코드 결함일 수 있고
   기계적 제거가 불가능하다. 래칫으로 **동결**만 한다.

## 6. 되돌리기

세 갈래 전부 독립적으로 되돌릴 수 있다:
① `ci.yml` 의 래칫 스텝 제거 → 게이트만 사라지고 코드는 그대로
② `lint-ratchet.json` 삭제 → 스텝이 exit 3 로 시끄럽게 죽는다(조용히 통과하지 않는다)
③ 면제 제거 커밋 revert → 원상복구

## 6-b. ★독립 리뷰가 초판에서 찾은 것 (전부 재현 확인 후 반영)

리뷰가 **저자 주장 수치는 전부 맞다**고 독립 재측정으로 확인했고(189/158/1060/95/84 ·
`tsc` EXIT=0 · `--fix` 가 논리를 안 건드림), **막아야 할 것은 다른 데 있다**고 했다. 맞았다.

| # | 무엇 | 재현 | 처리 |
|---|---|---|---|
| **F1** | **공허 진리 가드가 `[]` 하나만 막았다** — 깨끗한 레코드 **1개**짜리 리포트에 exit 0. 린트가 1,060개 중 1개만 봐도 초록 | 리뷰 실측 | `_meta.lintedFileFloor`(954) 도입 · **exit 3** |
| **F2** | **`-o` 가 CI 로그를 완전히 비운다**(에러 시 stdout 0바이트) — 초판 주석이 *"에러 자체가 보여야 한다"* 고 적고 그것을 없앴다 | 리뷰 실측 | `if: always()` **렌더 스텝** 추가(재실행 아님) |
| **F3** | 고수위 헤드룸이 **조용히 재소비**된다 | 리뷰 실측 | **부채 총량**을 매번 출력 |
| **F4** | 커밋이 *"공백 잔해 정리했다"* 고 선언했는데 **공백만 있는 줄 39개**가 남았다 | 리뷰 실측 | 줄 단위 정규식으로 재정리 → **0** |
| **F5** | 순수 rename 이 위양성으로 빨개지고 *"이 PR 이 새로 만든 것"* 이라 **단정** | 리뷰 실측 | 총량 상쇄면 **이동 가능성**을 먼저 말한다 |
| **F6** | 손상 JSON·비-dict 래칫이 exit **1(=위반)** 로 나갔다 — *"측정 무효"가 "위반"으로 읽힌다* | 리뷰 실측 | `InputError` → **exit 3** |
| **F7** | *"ruleId=null 31 = **전부** Unused eslint-disable"* → 실제는 **30 + `enable` 1건** | 리뷰 실측 | 본문 정정 |
| **F9** | **자동 회귀망 0건**이 F1 을 낳았다 | — | `propai-platform/tests/test_lint_ratchet.py` **18건** 같은 커밋에 |
| **F10** | 래칫은 **살아 있는 `eslint-disable` 추가로 우회**된다 — 이 PR 의 취지와 맞물린다 | 자명 | 아래 §5 에 명시 |

★**F9 가 인과다.** 초판은 손으로 3케이스만 태우고 *"★4축 검증 완료"* 라고 적었는데,
리뷰가 **레코드 1개짜리 입력**으로 그 축을 통과시켰다. 테스트가 없어서 확신이 잘못 섰다.

★리뷰가 *"다른 세션 피해는 오늘 작다"* 고 실측해 줬다 — 열린 PR 10건 전수에서 이 17파일을
건드리는 것 **0건**, apps/web 을 만지는 2건(#854·#769)도 **래칫에 없는 파일**이다.
계획서 §5-4 의 우려는 **과대평가**였다. 다만 F5(rename)·F8(기준선 경합)은 **미래에 열려 있다.**

## 7. 잠금

★**초판은 여기에 *"별도 테스트를 두지 않는다"* 고 적었다. 그 판단이 F1 을 낳았다.**
손으로 3케이스만 태우고 *"4축 검증 완료"* 라고 확신했는데, 리뷰가 **레코드 1개짜리 입력**
하나로 그 축을 통과시켰다.

지금은 `propai-platform/tests/test_lint_ratchet.py` **18건**이 잠근다
(`Backend (pytest)` 필수 잡이 매 PR 에서 태운다):

| 축 | 케이스 |
|---|---|
| 탐지 | 경고 증가 · 신규 파일 |
| **공허 진리(F1)** | 레코드 1개 · 경고 1건짜리 1파일 → **exit 3** · **대조군**: 모집단이 충분하면 정상 판정 |
| **입력 이상(F6)** | 손상 JSON · 리스트 아님 · 래칫이 dict 아님 · 값이 문자열 · `_meta` 하한이 정수 아님 · 빈 파일 |
| **rename(F5)** | 총량 상쇄 → 단정하지 않음 · **대조군**: 진짜 증가는 단정 |
| **헤드룸(F3)** | 부채 총량을 말하는가 |
| `--update` | `_meta` 를 남기고 **그 `_meta` 가 비교를 오염시키지 않는가** |
| **배선** | `ci.yml` 실행 라인이 래칫과 `--format json -o` 를 부르는가(+조회기 생존 대조군) |
| 실물 | 커밋된 `lint-ratchet.json` 이 `_meta` 하한을 갖는가 |

★각 결함마다 **대조군을 짝으로** 뒀다 — 하한이 항상 걸리면 F1 테스트가 공허해지고,
rename 완화가 항상 걸리면 진짜 증가의 진단이 무뎌진다.

★남는 한계: **CI 러너에서의 실제 동작**은 이 PR 의 CI 가 첫 실측이다(§5-1).
