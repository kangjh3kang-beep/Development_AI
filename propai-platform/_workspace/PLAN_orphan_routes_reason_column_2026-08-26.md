# 고아 라우트 기준선 — 사유 컬럼 승격 계획서 (2026-08-26 · SESSION-G)

## 0. 옵시디언 조회 결과

- 인계서 `2026-08-26_PropAI_모세혈관_인계_SESSION-F.md` §3 이 이 작업을 *"유일한 진짜 공백"*
  으로 넘겼다(제안: `path + kind(admin|cron|internal|legacy-dead|debt) + reason`).
  ★단서: ***"남의 도구다 — 소유 세션과 조율하라."***
- **이 주제의 볼트 전용 기록은 없다** — 없다고 적는다.
- 조율 결과: 통합자(당시)가 도구 이력을 조회해 **최초 도입이 `#694`(등기 화면 PR)의 부수
  산출물**이라 **전담 소유 세션이 없어 보인다**고 회신했고 이의 없음을 밝혔다. 현 통합자
  (SESSION-D)도 *"안 씁니다 — 가져가십시오"* 로 회신했다. 보드에 claim 했다.

## 1. 전제 — 확인 *방법* 과 **실제 측정값**

| # | 전제 | 확인 방법 | 실측값 |
|---|---|---|---|
| 1 | 기준선이 뭉뚱그려져 있다 | 파일 데이터줄 파싱 | **132행 · 전부 bare path**(종류·사유 없음) |
| 2 | 인계서가 든 두 예가 실재하고 구별 불가 | `grep -n` | **74·75행에 나란히** 있다 |
| 3 | 기준선을 읽는 곳 | `git grep` 전수 | **`test_orphan_routes_ratchet.py` 한 곳뿐** |
| 4 | 도구가 지금도 같은 수를 낸다 | `python3 scripts/orphan_routes.py` | 백엔드 548 · **확정 고아 132** · 판정불가 4 |
| 5 | `/auction/sync` 가 정당한 운영용인가 | 핸들러 원문 | 라우트 **자신의 `summary` 가 "(관리/cron)"**(`routers/auction.py:342`) |
| 6 | `/auction/opportunities` 가 죽었나 | 양방향 소비처 조회 + 대조군 | 백엔드 호출 **이 라우트뿐**(`:148`) · 프론트 실행줄 **0건**(대조군 `auction/watchlist` 는 3파일에서 잡힘) |

## 2. ★기계 분류를 시도했고 **폐기했다** — 이것이 이 계획의 핵심 판단

*"핸들러가 정의 파일 밖에서 참조되면 대체 진입점이 있다"* 로 **37/132** 를 얻었다.
**보고 전에 표본 10건의 오차율을 쟀다**(인계서 함정 목록의 지시):

| 표본 10건 | 실제 |
|---|---|
| 4건 | 로그 문자열 · 독스트링 · **`from urllib.parse import quote`(임포트)** |
| 6건 | **동명의 다른 함수 정의**(`prune`·`usage`·`estimate_value`·`fund_escrow` …) |

**정밀도 ~0%.** 표본을 안 쟀으면 *"37건이 대체 진입점을 갖는다"* 는 거짓이 나갔다
(메모리의 *"이름 충돌로 비용 추정 15배 부풀림"* 과 같은 형태).
→ **싼 기계 신호는 없다.** 132건을 유추로 채우는 것은 `#836`(*"래칫 사유가 추측이었다"*)이
고친 결함을 **132배로 반복**하는 것이다. 그래서 **대부분을 `unclassified` 로 두고,
연 사람만 사유를 적게 하는 기계를 만든다.**

★즉 **이 PR 의 값은 132건 분류가 아니라 「새 항목이 사유 없이 못 들어오는 것」이다.**

## 3. 회귀가 아닌 근거

- `test_orphan_routes_ratchet.py` + `test_orphan_routes_tool.py` **32 passed** · `ruff` 0
- ★형식을 바꾼 뒤 **기존 래칫이 실제로 깨지는 것을 먼저 확인**했다(2 failed) — 파서를 고치기
  전에 그 실패를 봤으므로 변경이 진짜라는 증거가 있다
- 기준선의 **경로 집합은 불변**이다(132 → 132). 컬럼만 늘었다

## 4. ★검증하지 못한 것

1. **130건은 여전히 미분류다.** *"아직 안 봤다"* 이지 *"문제없다"* 가 아니다.
   이 PR 은 그 수를 줄이지 않는다 — **늘지 못하게** 할 뿐이다.
2. `/auction/opportunities` 를 **`legacy-dead`(삭제 티켓)로 부르지 않았다.** 소비처 0 은
   확인했지만 **삭제해도 안전한지는 확인하지 않았다.** `legacy-suspect` 로 적었다.
3. `orphan_routes_undecided.txt`(4건)는 **손대지 않았다** — 다른 파일·다른 계약이다.
4. 종류 어휘 5개가 **충분한지 미측정**이다. 130건을 실제로 분류해 보면 모자랄 수 있다
   (그때 `_KINDS` 에 **뜻과 함께** 추가하면 된다).

## 5. 되돌리기

두 커밋 리버트로 원복된다. 기준선의 경로 집합이 불변이므로 되돌려도 래칫 판정은 같다.

## 6. 잠금

`test_orphan_routes_ratchet.py` — 형식 · 닫힌 어휘 · 근거 · 미분류 양방향 래칫.
★판정을 **순수 함수**(`validate_rows`·`check_unclassified_ceiling`)로 꺼내 **합성 입력**으로
태운다. 처음에는 테스트 본문 안에 두었다가 **변이 2종이 생존**했다 — 실제 파일이 규칙을
만족하므로 검사 로직을 지워도 초록이었다(공허한 참). 꺼낸 뒤 둘 다 CAUGHT 로 바뀌었다.
