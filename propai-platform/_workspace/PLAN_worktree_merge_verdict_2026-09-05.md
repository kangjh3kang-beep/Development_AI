# 워크트리 정리 판정을 **실행 가능한 명령**으로 (2026-09-05)

세션 development-ai-d8 [b6077f] · 브랜치 `fix/worktree-merge-verdict`

## 0. 옵시디언 조회 결과 (§0)

조회 범위 `AI-Sessions/**` · 대조군 «사통» **84파일**(조회기 생존 확인).

| 찾은 것 | 결과 |
|---|---|
| **이미 기각된 접근** | ★**있다.** `2026-08-25_PropAI_등기분석품질_인계.md:88` — *「착지 확인은 **main 의 내용**으로(`git cat-file -e origin/main:<경로>`), **커밋 해시 비교는 스쿼시에서 거짓말한다**」*. **커밋 해시로 머지를 판정하는 접근은 이미 기각돼 있었다.** |
| **같은 클래스의 앞선 결함** | `2026-08-08_PropAI_로그인장애_근본봉합_인수인계.md:109` — *「스쿼시 머지는 "첫 커밋만" 들어갈 수 있다 — `merge-base --is-ancestor` 로 착지를 확인하라」*. `CLAUDE.md` §성장루프·배포 판정에 **배포 축의 형제**가 이미 있다(*「브랜치 sha 로 재지 마라 — mergeCommit 으로」*). **워크트리 축에만 그 형제가 없었다.** |
| **미결·부채** | 워크트리 정리 판정에 대한 `it.todo`·스크립트 **없음**(조회 0건). |
| **이전 판단의 근거** | 내가 어제(2026-09-04) 인계서에 쓴 「고유 커밋 0」은 **근거 없이 관행으로 적은 것**이고, 위 두 기록과 **정면으로 모순**된다. |

★**따라서 이 결함은 신규가 아니라 재발이다.** 기록은 있었고 내가 조회하지 않았다.

## 1. 전제 표 (확인 *방법* + **실제 측정값**)

| # | 전제 | 확인 방법 | 결과(실측 2026-09-05 03:0x UTC) |
|---|---|---|---|
| P1 | 머지된 브랜치도 `origin/main..HEAD` 가 안 빈다 | `git -C <wt> log --oneline origin/main..HEAD \| wc -l` | `layers`/#966 **MERGED 인데 14** · `offscreen`/#975 **MERGED 인데 2** |
| P2 | `git diff origin/main HEAD` 는 판별력이 없다 | 머지 완료 **대조군** `wipekeys`/#810 에 같은 명령 | 대상 57·53파일 대량 deletions ↔ **대조군도 649파일 85,113 deletions** → **판별력 0** |
| P3 | `tip != PR headRefOid` 는 방향이 둘이다 | `rev-list --count` **양방향** | `offscreen`: 로컬고유 **0** · 뒤처짐 **7** / `wipekeys`: **0** · **4** → 「미머지」가 아니라 **뒤처짐** |
| P4 | 올바른 축은 판별력이 있다 | 5개 모집단 실행 | A=SAFE(0) · B=UNSAFE(1,축3) · C=UNSAFE(1,축2) · D=거부(2) · E=거부(2) |
| P5 | 인계서의 틀린 조건이 이미 main 에 있다 | `git cat-file -e origin/main:<경로>` | **있음** — `HANDOFF_satong_map_ux_2026-09-04.md:160` |
| P6 | 그 조건이 다른 곳에도 퍼졌나 | 저장소 전수 grep + 대조군(«워크트리» **667파일**) | 저장소 **1곳**(그 인계서) · 보드 **2건** |

## 2. 변경 내용과 회귀가 아닌 근거

- 신규 `scripts/worktree_safe_to_remove.sh` (읽기 전용 판정 · **파괴 동작 없음** — `git worktree remove` 를 **출력만** 한다)
- 인계서 1줄 → 명령 + 틀린 축 둘의 기록으로 교체

**회귀가 아닌 근거**: 런타임 코드 변경 **0**. 기존 스크립트·테스트·CI 잡을 **건드리지 않는다**.
새 파일은 아무도 아직 호출하지 않으므로 기존 경로에 영향이 없다(§소비처 0 은 여기서는
「배선 결함」이 아니라 **도구의 정상 상태**다 — 사람이 손으로 부르는 도구다).

## 3. ★검증하지 못한 것

- **CI 실행 미측정.** `gh` 인증과 워크트리 존재를 전제하므로 CI 잡으로 묶지 않았다.
  CI 에서는 축1이 판정 불가(exit 2)로 떨어질 것으로 **추정**하며 **미측정**이다.
  (미측정 사유 = **장치 부재** — CI 에 이 도구를 부를 잡을 만들지 않았다.)
- **512개 워크트리 전수 스윕 미실시.** 이 PR 은 판정 도구만 제공하고 실제 정리는 하지 않는다.
- **다른 세션 소유 워크트리 미처리.** `offscreen` 은 development-ai-1b 의 claim 영역이라
  판정만 하고 제거하지 않았다.
- **`--no-pr` 면제의 남용 여부 미측정** — 사유를 강제하지만 사유의 **질**은 기계가 못 잰다.

## 4. 되돌리기 경로

`git revert` 한 번. 문서 1줄 + 신규 파일 1개이며 런타임 참조가 없다.

## 5. 잠금 — 이 변경을 지키는 것이 무엇인가

★**정직하게: 자동 테스트는 없다.** 이 도구는 `gh` 와 실제 워크트리를 필요로 해
유닛 테스트로 태우기 어렵다. 대신 **도구 자신이 잠금 장치**다:

- **못 재면 「안전」이 아니라 exit 2** — 침묵을 이상 없음으로 읽지 않는다(§경보를 안 냄 ≠ 이상 없음)
- **`--no-pr` 사유가 필수 인자** — 익명 면제가 **문법적으로 불가능**하다(§36 죽은 면제)
- **5개 모집단으로 판별력 실증** — 각 축이 **혼자서** 판정을 뒤집는 것을 확인했다(§공허한 초록 방지)

★**부채: CI 잡이 없다.** 이 사실을 숨기지 않고 여기 적는다. 다음 사람이
`tests/` 에서 이 도구를 태우고 싶다면 `gh` 를 스텁하고 임시 워크트리 두 개(청결/오염)를
만드는 형태가 될 것이다 — **미착수**다.
