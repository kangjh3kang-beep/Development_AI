# PLAN — 도급단가의 **포함 범위**를 명시한다 (2026-08-27 · SESSION-H2)

## 0. 옵시디언 조회 결과 (계획 게이트 §0)

| 찾을 것 | 결과 |
|---|---|
| **이 문제의 출처** | [[2026-08-27_PropAI_region축_침묵미부과_인계_SESSION-H2]] §16-1 — *"도급단가의 포함 범위를 명시한 문서가 저장소에 없다. #913 의 소방 제거는 **추론**이고, 반대라면 **과소계상**"* |
| **같은 클래스의 앞선 결함** | `2026-07-15 수지·적산엔진 감사` — *"QTO 총액=시장가 1/3 실측 → 교체 대신 공용 개산식 SSOT 추출"* · PR#346 T2 파괴(표준품셈 시드 노무비=0) |
| **이미 기각된 접근** | 없음(이 주제로 기각 기록 없음) |
| **이전 판단의 근거** | 독립 리뷰: *"3,500 이 1/7.8 이다 ⟹ 도급단가 포함이다"* 는 **따라 나오지 않는다** — 정확한 지적 |

## 1. 전제 표 — 실측

| # | 전제 | 확인 방법 | 결과 |
|---|---|---|---|
| 1 | 도급단가에 포함 범위 서술이 있나 | 원문 | ❌ **없다**(리뷰도 동일 확인) |
| 2 | 적산 모델이 설비를 공사비로 보나 | `standard_quantity_estimator` | ✅ `mep_ratio=0.35`·`elec_ratio=0.15` **전 건물유형** |
| 3 | QTO 가 설비 행을 **실제로 내나** | 산출 실행 | ✅ `07-기계설비` 29.9B · `08-전기설비` 13.2B **일식** |
| 4 | ★내가 앞서 *"QTO 는 골조 6공종만"* 이라 한 것 | 재측정 | ❌ **틀렸다** — `UNIT_PRICES_2026` 키(6)만 보고 **일식 2행을 놓쳤다.** 실제 **8행** |
| 5 | 적산 실적에 소방이 설비 안에 있나 | `boq_master` | ✅ `mechanical.json` 「소화설비공사」 · `electrical.json` 「소방공사」 |
| 6 | 크기가 포함을 허용하나 | 계산 | ✅ QTO(설비 포함) **549,704원/㎡** vs 도급단가 **2,400,000원/㎡**(**4.4배**) |
| 7 | *"약 1/3"* 서술이 지금도 맞나 | 재측정 | ⚠️ **0.229** — 근사이고 낡았다 |
| 8 | 1차 출처로 확인 가능한가 | — | ❌ **미측정**(도급계약서·원가계산서 없음) |

★**전제 4 가 내 자기정정**이다 — 사용자에게 *"QTO 는 골조만"* 이라 말한 직후 재보니 틀렸다.

## 2. 변경 내용과 **회귀가 아닌 근거**

- `unit_price_repository._DIRECT_SQM_FALLBACK` 위에 **포함/제외를 명시**한다.
  **포함**: 골조·조적·방수·창호·마감 + **기계설비(소화 포함)** + **전기설비(소방전기·통신 포함)**
  **제외**: 간접공사비 · 토지비 · 금융비 · 제세금 · **공급자 인입 분담금**(도급 밖)
- **근거 세 축**을 함께 적는다(단일 출처가 없으므로 그것이 대신한다) + **미측정 명시**.
- **런타임 동작 변경 0** — 주석·문서와 테스트만. 값·산식 불변.

## 3. ★검증하지 못한 것

1. **1차 출처(도급계약서·원가계산서)로 범위를 확인하지 못했다.** 세 축은 **저장소 내부
   일관성**이고, 외부 실무 관례와 어긋날 여지가 남는다.
2. `mep_ratio`·`elec_ratio` **자체의 출처 미측정** — 실무 관례치로 보이나 인용이 없다.
3. `overview_estimator` 의 *"약 1/3"* 이 **0.229** 로 낡았다 — 이 PR 은 그 주석을 **고치지 않았다**
   (별건 · 값에 영향 없음).

## 4. 되돌리기 경로

주석 블록 + 테스트 파일. 되돌려도 런타임 동작은 그대로다.
★**트리거**: 1차 출처로 *"도급단가가 설비를 제외한다"* 가 확인되면 — 이 서술을 뒤집고
**`#913` 의 소방 제거(−23,002,000)를 재검토**해야 한다.

## 5. 잠금

| 잠글 것 | 검사 | 한쪽만 걸지 않기 |
|---|---|---|
| 축① 모델이 설비를 공사비로 | `mep_ratio`·`elec_ratio` 존재·양수(전 유형 파생) | ★**선언 ≠ 발화** — QTO 가 설비 **행을 실제로 내는지** 별도 단언 |
| 축② 소방이 설비 안에 | 실적 데이터에 「소화」·「소방」 | ★음성 대조군(없는 낱말은 없다고 나온다) |
| 축③ 크기 | 도급단가 > QTO(설비 포함) | ★단순 부등호가 아니라 **1.5배 여유**까지 |
| 범위 서술 | `**포함**:`/`**제외**:` **구조**로 판정 | ★**토큰 존재로 판정 금지**(변이 실증: 「포함」 절을 지워도 통과했다) · 항목이 **포함에 있고 제외에 없어야**(맞바꾸기 변이 탐지) |
| #913 결속 | 소방이 **부담금으로** 되살아나면 실패 | |


---

## 6. ★독립 리뷰 정정 — **내가 세운 세 축 중 둘이 무너졌다**

| 축 | 초안 주장 | 판정 |
|---|---|---|
| ① `mep_ratio`/`elec_ratio` | 적산 모델이 설비를 공사비로 본다 | ✘ **범주 오류.** 그것은 **QTO 산식의 계수**이지 도급단가의 계수가 아니다. `overview_estimator` 가 **같은 저장소 언어로 「혼용 금지」**를 적어 두었다 |
| ② 적산 실적 데이터 | 소방이 설비 안에 있다 | ◎ **살아남았다 — 유일한 직접 증거** |
| ③ 크기 비교 | *"설비를 빼고는 이 단가가 성립하지 않는다"* | ✘ **사실과 반대.** 실측: 설비 = 180,775원/㎡(단가의 **7.5%**), 배수 **포함 4.37 vs 제외 6.51** — **빼면 오히려 커진다.** 간극은 QTO 미모델 공종(마감·토공·승강기)이 만든다 → **판별력 0** |

★**내 락도 셋이 무너졌다**:
- **라벨만 봤다** — `work_code` 에 `"기계설비"` 문자열이 있는지만 봐서 **설비 금액을 0으로 만드는
  변이 3종이 전부 생존**(0원짜리 라벨 행이면 초록). → **금액**에 걸도록 교체.
- **고정 900자 창**이 인접 절을 침범해 **무관한 문서 수정을 틀린 사유로** 막았다(위양성).
  → 창을 **구조에서 파생**(`\n#:\n` 경계).
- **`building_type="apartment"` 가 조용히 폴백**했다 — `STANDARD_QUANTITIES` 에 그 키가 **없어서**
  `공동주택`(mep **0.34**)으로 떨어졌다. 주석은 `0.35`(아파트)를 인용하고 계산은 0.34로 했다 —
  **두 축이 서로 다른 건물유형에서 나왔다.** → 프로덕션 매핑(`_BT_KR`)을 명시적으로 쓴다.

### ★그래서 결론이 약해졌다 — 정직하게 적는다

`#913` 의 소방 제거를 **되돌리지는 않는다**(리뷰 권고와 같다). **축②가 단독으로 살아남고**,
실적 내역서에서 소방이 기계·전기 공종 **안에** 있다는 것은 *"3,500원/㎡ 부담금이 별도 항목이
아니다"* 를 지지한다. 그러나:

★**「포함/제외」는 증명된 사실이 아니라 이 저장소의 「의도」**이고, 그것을 지지하는 직접 증거는
**하나**다. 나머지는 **모순되지 않을 뿐**이다 — 주석에 그렇게 적었다.

### 기각한 가설을 기록으로 남긴다

축①·축③을 **주석에서 지우지 않고 「기각」으로 표시**했다. 그리고 그 기록이 사라지지 않게
`test_the_rejected_axes_stay_recorded` 로 잠갔다 — 다음 사람이 같은 길을 다시 가지 않게.

**변이 재검: 5종 전부 CAUGHT**(리뷰가 생존시킨 금액 축 3종 포함).
