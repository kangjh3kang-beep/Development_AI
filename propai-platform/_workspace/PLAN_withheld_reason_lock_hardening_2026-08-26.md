# 계획 — `#843` 배선 락 경화 (2026-08-26)

## 0. 옵시디언 조회 결과

**있음.** `AI-Sessions/wiki/design/2026-08-26_실거래_계약상태_3층구조와_MOLIT_필드_실측.md` §6
가 *"락이 **깨진 파일을 통과시켰다**(두 번, 같은 세션)"* 를 기록하고, 규칙을 적어 뒀다:
*"문자열 락은 ①문법을 먼저 태우고 ②가능하면 순수 함수를 꺼내 행동으로 잠근다."*
그리고 `log.md` 11:2x 에 통합자가 취합한 **★락이 한 칸 앞에서 멈춘다 — 참인 신호가 목표를
증명하지 않는다**(다섯 세션 하루 실측)가 있다.

★**이 PR 은 그 문서가 말한 실패의 네 번째 사례를 내가 저지른 것을 고치는 것**이다.
`#843` 은 `ast.parse()` 로 문법을 태우는 데까지는 갔는데, **어느 함수인지를 안 봤다.**

## 1. 전제 — 확인 방법과 실측값

| # | 전제 | 방법 | 실측 |
|---|---|---|---|
| P1 | 배선 락이 호출 **위치**를 안 본다 | 조립 원복 + 다른 함수에 decoy 호출 주입 후 실행 | **9 passed = SURVIVED** |
| P2 | 대조군이 내 독스트링에 걸린다 | `grep -c site_management_detail console.py` | **2건** — 287행이 `#843` 이 쓴 독스트링 |
| P3 | P2 로 인해 대상 삭제가 안 잡힌다 | 함수명 변경 변이 | **SURVIVED**(수정 전) |
| P4 | 깨끗한 입력에서 위반을 발명한다 | `validate_withheld_pair` 입력/출력 대조 | 입력 위반 **0** · 출력 위반 **2** |
| P5 | `balanced_reason` 은 생산자 0 | `git grep -c balanced_reason` | **1건 = 그 튜플 자신뿐** |
| P6 | 수정 후 전부 잡힌다 | 변이 R1~R6 재실행 | **6/6 CAUGHT** |

## 2. 처방

1. 배선 락을 `site_management_detail` **함수로 스코프**
2. 대조군을 **문법 구조**에 결속(`next(...)` → 없으면 `StopIteration`)
3. `"balanced" not in r` → `{}` (없는 판정을 발명하지 않는다) + 그 테스트도 계약을 태운다
4. 승계를 **화이트리스트 → 조립부 소유키 제외**(파생형)

## 3. 회귀가 아닌 근거

런타임 동작 변화는 **두 곳뿐이고 둘 다 현재 도달 불가**다 —
㉠`balanced` 없는 입력(실제 `_reconcile` 은 항상 담는다) ㉡접두가 다른 사유 키(오늘은 `balanced_*` 뿐).
따라서 **라이브 응답 불변**. 사실상 락 경화다.

## 4. ★검증하지 못한 것 (정직)

- **라이브 재확인 불가** — 이 PR 은 응답을 안 바꾸므로 라이브로 갈릴 것이 없다.
  (`#843` 본체의 종단 확증은 배포 후 별도로 돌린다.)
- **`balanced=False` 갈래 라이브 표본 0건** — 픽스처로만.
- **형제 조립부 미스윕** — `actions.py::accounting_summary` 가 `site_management_detail`
  결과를 **똑같이 키로 손으로 골라** 재조립한다(`reconciliation` 제외). 의도적 제외로
  **보이지만 확증 안 했다**(추론). 별건으로 남긴다.
- **독스트링 제외는 선두 문장만** — 선두가 아닌 문자열 표현식은 여전히 코드로 세어진다(알려진 한계).

## 5. 되돌리기

단일 커밋 revert. 스키마·응답 무변경.

## 6. 잠금

변이 **6종 전부 CAUGHT**. 특히 **R5**(조립 원복 + 호출을 다른 함수로)와 **R6**(대상 함수 삭제)는
**수정 전에는 둘 다 SURVIVED** 였다 — 이 PR 이 실제로 무엇을 잠갔는지가 그 두 개로 갈린다.

★**교훈**: `ast` 를 쓴다고 배선이 잠기지 않는다. **"어느 함수 안에서인가"** 를 묻지 않으면
`ast` 도 문자열 검사와 같은 층에서 멈춘다.
