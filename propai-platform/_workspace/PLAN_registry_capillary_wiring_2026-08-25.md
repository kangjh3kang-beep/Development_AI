# 계획 — 기능은 있는데 화면이 침묵한다 (모세혈관 배선 W1·W2)

브랜치 `fix/registry-capillary-wiring` · 베이스 `origin/main`

## ① 옵시디언 조회 결과

조회함(`obsidian-brain` · Vault `나의-모든-기록-최적화본`).

| 문서 | 무엇을 줬나 |
|---|---|
| `wiki/errors/2026-08-18_표시층을_두_번_고쳤는데_결함은_상류였다.md` | **같은 프로젝트(오산 내삼미동 77필지)**. *"동일 처방이 서로 다른 소비처에서 연속 실패하면 결함은 공급처에 있다"* · *"플레이스홀더로 채운 필드는 결측보다 위험하다 — 결측은 시끄럽고 복제는 조용하다"* |
| `wiki/errors/2026-08-21_표시구현이_몇벌인지_먼저_세라.md` | 표시 구현이 여러 벌일 수 있다 |
| `wiki/decisions/2026-08-24_완성도_100퍼센트_한계분석과_목표_재정의.md` | 100% 를 **정답률이 아니라 "거짓말 0"** 으로 재정의 |

**없었던 것**: **경·공매 ↔ 필지 연동의 설계 정본이 없다**(6건은 로그인 장애·celery 배포 등
부수 언급). 즉 그 배선은 *"설계대로 안 됐다"* 가 아니라 **설계가 기록된 적이 없어 아무도
미구현으로 세지 않았다.** W2 에서 다룬다.

## ② 전제 표 — 확인 *방법* 과 **실제 측정값**

| # | 전제 | 확인 방법 | 실제 측정값 | 판정 |
|---|---|---|---|---|
| P1 | 권리분석 보고서 기능이 없다(사용자 신고) | 소스·백엔드 라우트 조회 | **있다** — `RegistryRightsReportButton`(PDF/DOCX) + `routers/registry.py:1060` `/rights-report` | **기각** |
| P2 | 그 기능이 배포됐나 | `curl sw.js` → sha → `merge-base --is-ancestor b8396e58 <sha>` | **배포됨** | 관측 |
| P3 | 왜 안 보이나 | 렌더 조건 추적 | `{batchResults && length>0}` 블록 **안에만** 렌더(`:508`) | 관측 |
| P4 | `batchResults` 는 어디서 오나 | 복원 effect 읽기 | `savedAnalyses`(`useRegistryAnalysisStore`)에서 1회 복원(`:293-304`) | 관측 |
| P5 | 저장은 되나 | 호출부 전수 | 개별·일괄 **공통 경로**에서 저장(`:162`) · 키 `propai-registry-analysis` 는 **와이프 면제** 등재 | 관측 |
| P6 | 그런데 왜 비어 있나 | 배포 시점 대조 | 결과 보관은 최근 추가분 — **그 이전 분석은 저장 대상이 아니었다** | **추론**(반증조건: 저장분이 있는데도 안 뜨면 이 설명은 틀림) |
| P7 | 소유자·PDF 는 왜 남아 있나 | 행 렌더 출처 | `useLandScheduleStore` 행에 **따로 영속**(`r.owner`·`r.pdf_url`) | 관측 |
| P8 | 매입전략 버튼 비활성 원인 | disabled 경로 전수 | `disabled={!scheme \|\| overCap}` **두 곳뿐** · `STRATEGY_SCHEMES` 9종 · `MAX_STRATEGY_PARCELS=100` > 77 → `overCap` 거짓 | 관측 |
| P9 | 비활성 사유가 화면에 있나 | JSX 전문 | **없다**(라벨의 빨간 `*` 하나) | 관측 |
| P10 | "재조회 무료" 가 항상 참인가 | 캐시·과금 규칙 | `_ANALYZE_DB_TTL=7일` · `_cache_success()` 는 **성공만** 저장 · 미스면 재발급 → **사용자 청구는 막히나 벤더 선불금은 탄다** | 관측 → **조건부 참** |
| P11 | 사용자의 77필지가 7일 이내 성공분인가 | — | **미측정**(서버 캐시 상태를 안 봤다) | 미측정 |

## ③ 회귀가 아닌 근거

세 변경 모두 **가산**이다 — 기존 렌더 경로를 바꾸지 않는다.
· 고지는 `batchResults` 가 **빈** 경우에만 새로 뜬다(있으면 종전대로 보고서 블록)
· 매입전략 사유는 **비활성일 때만** 뜨고 `overCap` 경고와 중복하지 않는다
· 문구 변경은 상수 인용으로 바뀔 뿐 조건 로직이 없다
특이도 케이스로 "아무 때나 뜨지 않음"을 각각 잠갔다.

## ④ ★검증하지 못한 것

- **P11 미측정**: 사용자의 77필지가 재분석 시 실제로 무과금인지는 서버 캐시 상태에 달렸다.
  그래서 화면 문구를 *"무료"* 가 아니라 **"{N}일 이내에 성공했던 필지는 재청구되지 않고,
  그보다 오래됐거나 실패했던 필지는 청구됩니다"** 로 **조건부**로 썼다.
- **P6 는 추론이다.** 저장분이 있는데도 안 뜨는 사례가 나오면 이 설명은 틀렸고, 그때는
  복원 effect 를 다시 봐야 한다(특이도 테스트가 그 반대 방향을 이미 잠갔다).
- 라이브 재현은 **하지 않았다** — 재현하려면 유료 발급이 필요하다(「유료·비가역 산출물 규율」).
- **W3(도달률 일반화)** 는 이 PR 범위 밖이다. W2 는 아래에 포함했다(포화된 머지 큐에서 슬롯을 아끼려 한 PR 로 묶었다).

## ⑤ 되돌리기

프론트 4파일 순수 가산 · 마이그레이션·스키마 변경 없음 · `git revert` 로 원복 가능.

## ⑥ 잠금

| 축 | 어디서 | 변이 | 결과 |
|---|---|---|---|
| 탐지(고지) | `RegistryAnalysis.priorAnalysisNotice.test.tsx` | 조건 삭제 | CAUGHT |
| 특이도(고지) | 〃 | 항상 표시 | CAUGHT |
| 탐지(매입전략) | `ParcelPurchaseStrategyPanel.disabledReason.test.tsx` | 사유 삭제 | CAUGHT |
| 패리티(기간) | `registry-free-requery-parity.test.ts` | 상수 7→30 | CAUGHT |

★락을 **초록부터 만들지 않았다** — 패리티 락은 쓰자마자 돌려 실제 문구를 지목하는 것을
확인한 뒤 문구를 고쳤다.
★첫 판 락이 **더 나은 설계를 막았다**(리터럴만 허용 → 상수 결속을 위반으로 신고) ·
손으로 짠 주석 필터가 **JSX 주석**을 집었다(공용 헬퍼가 옆에 있었다). 둘 다 교정했다.


---

# W2 — 경·공매를 **필지 문맥에 놓는다**

## ① 옵시디언 조회 결과

**경·공매 ↔ 필지 연동의 설계 정본이 없다**(`경공매` 6건은 로그인 장애·celery 배포 등 부수 언급).
즉 *"설계대로 안 됐다"* 가 아니라 **설계가 기록된 적이 없어 아무도 미구현으로 세지 않았다.**
이 사실 자체를 여기 남긴다 — 다음 사람이 "미구현"으로 오독하지 않도록.

## ② 전제 표

| # | 전제 | 확인 방법 | 실제 측정값 | 판정 |
|---|---|---|---|---|
| Q1 | 경공매가 필지에 연동돼 있나 | 라우트 전수 + 서비스 추적 | **되어 있다** — `/auction/watchlist` 가 호출마다 `sync_landschedule_targets()` 로 **토지조서 필지를 자동 등록** | 관측 |
| Q2 | 라우트가 몇 개인가 | `grep -c '^@router\.'` | **21종**(처음 `head -40` 으로 9종으로 오독 — **자른 목록으로 추론하지 마라**) | 관측·**자기정정** |
| Q3 | 필지 화면에 경공매가 있나 | 렌더 트리 추적 | **있다** — `SatongMapShell` 의 `공·경매` 레이어가 `LandScheduleClient:634`·`ComprehensiveAnalysisPanel:633` 에서 렌더 | 관측·**자기정정**(첫 조회는 파일 단위 grep 이라 **합성을 못 뚫은 위음성**) |
| Q4 | 그 레이어가 기본 켜짐인가 | 초기값 | **꺼짐** — `useState(() => new Set(["cadastre"]))` | 관측 |
| Q5 | 매칭 결과를 필지 화면에서 보나 | 소비처 전수 | **못 본다** — `/auction/monitor`·`/auction/my` 소비처는 `components/auction/` 뿐 | 관측 |
| Q6 | 어느 엔드포인트가 싼가 | 구현 읽기 | `/auction/my` = `my_listings()` **순수 SQL 조인**(외부 호출 0 · 구독 게이트 없음) vs `/auction/monitor` = **구획 지오코딩 가능 + 구독자 전용** | 관측 |
| Q7 | 소비처 0인 라우트 | 대조군 후 전수 | **5종**(`/opportunities`·`/analyze`·`/listings/{id}`·`/sync`·`/items/{id}`) — 이들은 **레거시 `services/auction_service.py`(G95)** 소관으로 신형 `app/services/auction/` 와 **다른 서브시스템** | 관측 |

## ③ 회귀가 아닌 근거 / ④ 검증하지 못한 것

- 배지는 **가산 컴포넌트**다. 다만 `useQuery` 를 쓰므로 **프로바이더가 필요**하고, 그 때문에
  `RegistryAnalysisWorkspaceClient` 를 렌더하는 **기존 테스트 2종이 깨졌다**(제가 만든 회귀).
  공용 헬퍼 `test/render-with-query-client.tsx` 로 감쌌고 **단언은 하나도 약화시키지 않았다.**
  프로덕션은 `lib/providers.tsx` 가 이미 제공한다(`useQuery` 소비처 26곳).
- **전수 회귀 실측**: `vitest run` **337파일 / 3,075 통과**.
  ★첫 실행에서 `ProfileClient` 1건이 실패했는데, **같은 환경 재실행에서 통과**해 **플레이크**로
  판정했다. 처음엔 `origin/main` 워크트리에 `node_modules` 를 심링크해 기준선을 뜨려 했으나
  **75파일이 수집 실패**해 그 기준선은 **무효**였다 — 환경이 다른 기준선은 기준선이 아니다.
- **미측정**: 라이브에서 실제로 매칭이 잡히는지(현재 이 계정의 `auction_items` 캐시 상태를 안 봤다).
  그래서 배지는 **0건을 "연동 없음"으로 말하지 않는다** — 네 상태를 각각 다르게 말한다.
- Q7 의 레거시 5종은 **잇지도 지우지도 않았다**(제품 결정) — W3 에서 `Reach` 로 선언할 대상이다.

## ⑥ 잠금 (W2)

| 축 | 변이 | 결과 |
|---|---|---|
| 핵심(0건 뭉개기 금지) | `if (q.isError)` 무력화 | CAUGHT |
| 오해 재생산 방지 | "자동 감시 중" 문구 삭제 | CAUGHT |
| 특이도(프로젝트 격리) | 프로젝트 무시하고 전건 계수 | CAUGHT |
