# 조례 파서가 **주거지역을 통째로 못 읽는다** — 표기 공백 (2026-08-28)

세션 development-ai-01 [2416cd] · 브랜치 `fix/ordinance-zone-name-normalize`
발단: 사용자 신고(오산 78필지) 조사 중 용적률 250 vs 조례 230 격차

## 0. 옵시디언 조회 결과 (§0 · 필수)

| 찾을 것 | 결과 |
|---|---|
| **이미 기각된 접근** | **있음** — `2026-08-19_조례파서_전면고장을_정적캐시26곳이_가렸다`. 같은 부지(오산시)로 세 겹(‘안’ 표기·공허한 초록·상호참조 앵커)을 고쳤으나 **커밋 보류**했다. 이유: *"값이 나왔지만 틀렸다"*(자연녹지 bcr 30 ← 원문 20). ★**값이 나온 것은 고쳐진 것이 아니다** |
| 같은 클래스의 앞선 결함 | **표기변형**이 이미 두 번(‘안에서의’ 필수 · 분수형 `100분의 40`). 이번 건은 **세 번째 표기변형** |
| 미결·부채 | 그 문서의 근본원인 = `용도지역 → 값 하나` 모델. 이후 **조건부 모델이 구현됨**(내 실측에 `conditional_limits`·`condition_key=growth_management_plan` 존재) |
| 이전 판단의 근거 | S계층 가드(법정상한 초과 기각)가 PR `#700`(`6488fa58`)로 들어감 → 내 값 230 ≤ 국가 250 이라 통과 |

★그 문서가 경고한 **§F23 파일 두 벌 함정**을 먼저 확인했다 — 저장소에는 **1벌**뿐이고
`M.__file__` 로 임포트 경로 확인함(두 벌은 컨테이너 아티팩트였다).

## 1. 전제 표 — 확인 방법과 **실제 측정값**

| # | 전제 | 확인 방법 | 실제 측정값 |
|---|---|---|---|
| 1 | 조례 원문이 **공백 표기**를 쓴다 | 원문 전수 카운트 | `제2종 일반주거지역` **3건** / `제2종일반주거지역` **0건** |
| 2 | 파서가 주거지역을 못 읽는다 | 저장소 파서를 **실제 원문**에 직접 실행(venv·스텁 아님) | 제2종 **None** · 제3종 **None** · 자연녹지 bcr 20 far 100(정상) |
| 3 | 공백만이 원인인가 | **두 모집단**(같은 파서·공백만 정규화) | 원문 → **None** / 정규화 → **bcr 60 far 230 conf 0.95** |
| 4 | 조례 정답 | law.go.kr DRF 원문(자치법규ID 2097518 · 시행 20260506) §51①4호 | **230% 이하**(단, 지구단위계획 수립 시 250%) · §45①4호 bcr **60%** |
| 5 | 라이브가 틀린 값을 낸다 | `/zoning/integrated-analysis` 오산 437-1 | `far_eff` **250** (조례 230 대비 **+20%p**) |
| 6 | 키가 원인이 아님(별건 해소) | 키 교체 후 재측정 + 기능검증 | 고시검색 양성 matches=3·음성 0 → **키 정상** · 그런데 `far_eff` **여전히 250** |
| 7 | 정규화 헬퍼가 이미 있나 | 전수 grep | **있음** `legal_zone_limits.normalize_zone_name`(공백 제거) — 단 **입력 이름**용이지 **본문**용이 아니다 |
| 8 | 파일 두 벌인가(§F23) | `find` + `M.__file__` | 저장소 **1벌** |

## 2. 변경 내용과 **회귀가 아닌 근거**

**② 본문 매칭을 공백 허용으로** — 공용 헬퍼 하나를 만들어 **두 매칭 지점**에 함께 적용:
  · `_basic_items` 기본항 `NN. <용도지역> : NN퍼센트` (현 `re.escape(zone)`)
  · `_iter_zone_fragments` 앵커 `alt` (현 `re.escape(z)`)

★**문서를 정규화하지 않는다.** 내가 실험에서 쓴 `re.sub`(문서 전체 공백 제거)는
`evidence_span`·`context` 발췌의 **문자 오프셋을 어긋나게** 해 증거 품질을 떨어뜨린다.
**needle(패턴) 쪽만** 공백 허용으로 만든다.

*회귀 아님*: `\s*` 는 **공백만** 매칭하므로 기존 무공백 표기는 그대로 매칭된다(상위집합).
기존 자연녹지 결과(bcr 20·far 100)가 불변임을 락으로 고정한다.

**⑤ DRF 200-실패를 조례 경로에서도 감지** — `moleg_drf_envelope` 에 **XML 형제**를 추가.
현 검증기는 `isinstance(payload, dict)` 라 **JSON 전용**이고 조례는 `type=XML` 이라 무방비였다.
형제 2곳(`regulation_monitor`·`gosi_search_service`)은 이미 검증하는데 **조례만 빠져 있다**.

*회귀 아님*: 실패 감지만 추가하고 성공 경로의 파싱은 건드리지 않는다.

## 3. ★검증하지 못한 것

- **오산 외 지자체에서 이 공백 표기가 얼마나 퍼져 있는지 미측정.** 표본 1건(오산)이다.
  다른 지자체가 또 다른 표기변형을 쓸 수 있다(선례: ‘안에서의’·분수형).
- **`far_eff` 가 실제로 230 으로 바뀌는지 라이브 확인은 배포 후에만 가능**하다.
  로컬 파서 단위로는 230 을 확인했다.
- **성장관리권역 조건부 값(제50조 30%)이 이 부지의 실제 적용값인지** 판단하지 않았다 —
  2026-08-19 문서가 남긴 미결이고 **이 PR 범위 밖**이다.
- **`conditional_limits` 의 우선순위 모델**은 손대지 않는다(같은 문서의 미결).
- 조례 원문의 **별표(別表)** 경로는 태우지 않았다.

## 4. 되돌리기 경로

두 파일 단일 revert(`ordinance_service.py` · `moleg_drf_envelope.py`).
런타임 영향은 조례 파싱에 갇히고, 실패 시 종전과 같이 폴백(국가상한)으로 돌아간다.

## 5. 잠금

| 축 | 무엇을 잠그나 |
|---|---|
| 탐지 | **실제 오산 조례 원문**에서 제2종=far 230·bcr 60 을 뽑는다(값 못 박기) |
| 특이도 | 무공백 표기(자연녹지)가 **종전과 같은 값**(bcr 20·far 100) — 상위집합임을 증명 |
| 두 모집단 | 공백 표기 → 값 나옴 / **없는 용도지역** → None(과잉 매칭 방지) |
| 계약 | 국가상한 초과 기각(S계층 가드)이 여전히 동작 |
| ⑤ | DRF 실패 XML(`<Response><result>…실패`) → **조용한 None 이 아니라 시끄러운 예외** |

★픽스처는 **합성 문자열이 아니라 실제 조례 원문**을 쓴다(2026-08-19 교훈: 값이 나온 것과
맞는 것은 다르다 — 원문 그라운드 트루스와 대조해야 한다).


## 6. ★변이 검증이 드러낸 것 (착수 후 추가)

기준선 **rc=0 · 11 passed** 확인 후 판정. **처음 판정에서 3종이 생존**했고 전부 진짜 구멍이었다.

| 변이 | 1차 | 원인 | 봉합 후 |
|---|---|---|---|
| ④ 앵커 소비처 `re.escape` 되돌리기 | **SURVIVED** | 헬퍼를 **두 곳**이 쓰는데 기본항만 태우는 락뿐 | CAUGHT |
| ⑧ 사유 추출 제거 | **SURVIVED** | ★내 단언이 **공허** — 폴백이 *본문 앞 120자를 그대로 echo* 해서 `"사용자 정보 검증에 실패" in reason` 이 참이 됐다 | CAUGHT |
| ⑩ 본문조회 검증 제거 | **SURVIVED** | 대역이 ①모든 호출에 같은 본문 → Step 1 에서 먼저 예외 ②`AsyncClient(...)` 마다 새 대역 → **호출 순번 카운터 리셋** | CAUGHT |

**최종 10/10 CAUGHT** — 헬퍼 2축(원결함 되살리기·과잉허용) · 소비처 2곳 · 상수 · 검증기 3축 ·
배선 2곳. ★"10개 층"은 **내가 센 것**이고 독립 리뷰에 반증을 맡긴다.

★**이 경로는 관측 지점이 로그뿐이다** — 광범위 `except Exception: return None` 이 성공/실패의
반환값을 똑같이 `None` 으로 만든다. 그래서 배선 락은 *"사유가 로그에 실리는가"* 를 단언하고,
정상 응답에서 그 문구가 **안 찍히는지**를 대조 모집단으로 함께 건다.

## 7. 추가로 확인한 것

- **줄바꿈 독립성**: 픽스처가 CRLF 였는데 git 은 LF 로 저장한다 — **다른 사람이 체크아웃하면
  내가 시험한 것과 다른 바이트**다. LF 로 변환해 재실행(통과) 후 커밋했다.
- **ruff 0.16.3**(CI 와 같은 버전)로 검사 — `F401` 1건 적발·수정 후 `All checks passed`.
- 관련 회귀 **604 passed · 1 skipped · 0 failed**.

## 8. ★여전히 안 잠근 것 / 미해결 (초록 안에 드러낸다)

- **광범위 `except Exception: return None` 을 그대로 뒀다.** 실패 사유가 로그에만 남고
  **호출부는 성공/실패를 구별할 수 없다**. 구조적 처방(전용 결과형)은 이 PR 범위 밖 —
  ★그래서 배선 락이 반환값이 아니라 로그를 본다(약한 관측점임을 인정한다).
- **오산 외 지자체 표기 분포 미측정**(표본 1). 다른 표기변형이 또 있을 수 있다
  (선례: `안에서의` 필수 · 분수형 `100분의 40` — 이번이 **세 번째**).
- **라이브 `far_eff` 가 250→230 으로 바뀌는지는 배포 후에만** 확인 가능(로컬 파서는 230 확인).
- **조건부 값 우선순위 모델**(성장관리권역 제50조 30% 등)은 손대지 않았다 — 2026-08-19 의 미결.
- **별표(別表) 경로** 미검증.
