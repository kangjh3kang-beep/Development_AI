# 인사이트가 0건인 구간의 **커버리지가 어디에도 저장되지 않는다**

- 세션 development-ai-cd [863fc5] · **sid=79cfa3eb** · 브랜치 `fix/publish-analysis-coverage`
- 발단: 성장루프가 **11.3시간째 산출 0**(관측 최대 8.7시간 → 배수 1.30, **전례 초과**)인데
  화면은 `open 175` 로 가득 차 **건강해 보였다**

## 0. 옵시디언 조회 결과 (§0)

| 찾을 것 | 결과 |
|---|---|
| **이미 기각된 접근** | **임계·하한 변경은 기각**(`FALLBACK_MIN_CALLS` 10→3 이 트래픽 0 스택에서 프로덕션 타임아웃을 곱한 사고). ★이 PR 은 임계를 **건드리지 않는다** |
| **같은 클래스의 앞선 결함** | [[2026-09-02_침묵이_무엇인지_말해주는_것이_침묵과_함께_사라진다]] — *"그 앎이 저장되는 유일한 자리가 **인사이트 행의 `metrics_json`** 이다. 인사이트가 0건이면 **설명이 통째로 버려진다** — 설명이 가장 필요한 바로 그때."* **오늘이 그 부채의 라이브 실현이다** |
| **미결·부채** | 같은 문서: *"워터마크는 **「돌았다」를 말하지 「됐다」를 말하지 않는다**"*. 라이브 확인 — `growth_last_run.analyze` 는 **맨 타임스탬프 문자열**이다 |
| **이전 판단의 근거** | ★같은 문서가 *"저장소가 이 부채를 `xfail(strict)` 로 들고 있다"* 고 적었는데 **낡았다** — 그 부채는 이미 갚혀 정식 락(`test_a_surface_reads_analysis_coverage`)이 됐다. **읽는 쪽은 있다. 없는 것은 쓰는 쪽이다.** |

★**형제가 처방을 이미 갖고 있다**(재발명 0): `capture_service.publish_capture_status` 가
수집 축에서 **정확히 같은 일**을 한다 — *"새 표면을 만들지 않는다 — `growth_last_run.*`
워터마크가 이미 쓰는 통로를 그대로 탄다"*, **TTL 로 「행 부재」를 세 번째 모집단으로** 만든다.

## 1. 전제 표 — 전부 실측 (2026-09-05 13:0x~13:2xZ)

| # | 전제 | 확인 방법 | **결과** |
|---|---|---|---|
| 1 | 분석기는 돌고 있다 | 워터마크 · sid=0a08a179 컨테이너 로그 | **참** — `growth_last_run.analyze` = `13:11:41Z`, 매시 |
| 2 | 이벤트도 쌓인다 | 동 로그 `INSERT INTO platform_events` | **참** — 86건/180분 |
| 3 | 침묵의 원인은 **표본 부족** | 7일치 시간대별 «하한 20 초과 키 수» | **참** — `09-05 01` 이후 **모든 시간대 0** (침묵 시작과 일치) |
| 4 | 0건 실행도 커버리지를 **로그**한다 | `analyzer.py:644~649` + 락 | **참** — `if insights:` 로 감싸지 못하게 락이 강제 |
| 5 | 그 커버리지가 **어디에도 저장 안 됨** | `metrics_json` 은 인사이트 행에만 | **참** — 0건이면 저장 자체가 없다 |
| 6 | 읽는 쪽은 **이미 있다** | `/growth/heal-log` → `active_flags` → `GrowthDashboard.tsx:644` | **참** — 라이브 `active_flags` **9건** 수신 중 |
| 7 | 형제가 같은 통로를 쓴다 | 라이브 `growth_capture [api]·[worker]` | **참** |
| 8 | ★`value` 가 **list 면 읽기 500** | `ActiveFlagOut.value` 주석의 실측표 | **참** — dict ◎ · list ✘. **dict 로 낸다** |
| 9 | ★**jsonb 가 키를 재정렬**한다 | 형제 주석의 라이브 실측 | **참** — (키 길이, 바이트순). *"판별 필드를 앞에"* 는 저장을 통과하며 무효화된다 |
| 10 | ★화면 상한 | `ACTION_PARAMS_RENDER_CAP = 4` · 값마다 **24자** 절단 | **참** — **4키 이하 · 값 24자 이하**로 설계한다 |
| 11 | analyze 주기 | `schedule.JOB_SPECS["analyze"] = 60분` | **참** → TTL 은 형제 규칙(3배) = **180분** |

### ★기각한 가설
①`analyze_growth` 미실행 ③이벤트 미적재 — **둘 다 기각**(전제 1·2, sid=0a08a179 실측).
남은 것은 ②전부 withheld 이고 그것이 정답이다. **구조 결함이 아니라 표본 부족이다.**

## 2. 변경 내용과 회귀가 아닌 근거

`analyze_window` 가 **매 실행마다**(0건 포함) 커버리지 스냅샷을 `platform_settings` 의
`growth_analysis` 키로 **TTL 180분**과 함께 발행한다. **읽기 경로는 한 줄도 안 바꾼다.**

세 모집단이 갈린다:

| 관측 | 뜻 |
|---|---|
| 행 있음 · `state=judged` | 정상 판정 중 |
| 행 있음 · `state=starved` | **돌았으나 전부 하한 미달** — 오늘의 상태. 처방은 **대기** |
| **행 부재**(TTL 만료) | 분석기가 안 돈다 = 고장 |

**회귀가 아닌 근거**: 발행은 **best-effort** 이고 실패해도 배치를 죽이지 않는다(형제와 동형).
인사이트 생성·INSERT 경로는 손대지 않는다. **임계·하한 불변.**

★**「생산 0」과 「효과 0」은 다른 축**이다(sid=0a08a179 지적) — 이 키는 **생산** 축만 말한다.

## 3. ★검증하지 못한 것

1. 화면에서 실제로 어떻게 보이는지 — **미측정**(라이브 렌더 확인은 배포 후)
2. TTL 180분이 적정한지 — 형제 규칙(주기 3배)을 따랐을 뿐 **독립 근거는 없다**
3. 「저트래픽 주말」인지 「사용자 유입 감소」인지 — **내 축 밖**이고 미측정
4. 이 발행이 `/heal-log` 응답 크기에 주는 영향 — 미측정(키 1개 추가)

## 4. 되돌리기 경로

발행 호출 한 곳을 지우면 원복. TTL 이 있으므로 **행은 스스로 사라진다** — 잔재가 없다.

## 5. 잠금

1. **두 모집단** — 인사이트 0건인 실행에서도 **발행된다**(있는 실행과 같은 실행에서 대조)
2. **세 모집단 가름** — `state` 가 `judged`/`starved`/`idle` 를 실제로 가른다
3. **TTL 락** — TTL 없이 쓰면 「멈춤」이 「정상」으로 읽힌다 → TTL 이 붙는지 단언
4. ★**표시 계약 락** — 키 **4개 이하**, 값 **24자 이하**(상한 상수에서 **파생**, 손 목록 금지)
5. **dict 락** — `value` 가 dict 다(list 면 읽기 500 — 전제 8)
6. **best-effort 락** — 발행이 실패해도 `analyze_window` 가 인사이트를 그대로 반환한다
