# 계획 — `coord.sh status` 요약이 요약하지 않는다 (2026-09-03 · development-ai-3a)

## 0. 옵시디언 조회 결과 (착수 전 · §계획 게이트 0)

| 찾을 것 | 결과 |
|---|---|
| **이미 기각된 접근** | ★**있다. 결정적이다.** 2026-08-27 한 세션이 CLAIM↔RELEASE **짝짓기**를 구현했고 **자기 양성 대조군에 실패**했다(`본문 앞 60자` 키가 *"내 자신의 확실한 쌍조차 못 맺음"*). 그 세션은 파생 수치를 **8개 세션에 뿌린 뒤 철회**했다. 추가 반례도 기록돼 있다 — **`RELEASE` 1줄이 `CLAIM` 둘을 닫는 실례**. → **이 PR 은 짝짓기를 구현하지 않는다.** |
| **같은 클래스의 앞선 결함** | 같은 날 기록: *"「미해제 CLAIM」 절이 RELEASE 까지 전부 덤프한다 → **제목이 사실과 다르고**, 소유 지도로 읽는 것 자체가 틀렸다"* · *"주석은 필터를 선언하는데 코드엔 없다"*. **이 결함은 기록돼 있고 6일간 미수정이다.** |
| **미결·부채** | *"`coord.sh claim` 이 세션 이름을 함께 적어야 `ListAgents` 조인이 성립한다"* — **미착수**. 보드 꼬리표가 전부 `<- <브랜치> (시각)` 이라 CLAIM 과 세션을 맺을 **키가 없다**(527건 중 `SESSION-` 표기 7건 = 1.3%). |
| **이전 판단의 근거** | *"claimer 생존을 재라"* 는 처방이 **구조적 실행 불가**로 판정돼 있다(위 키 부재). 승계하지 않는다. |

★**이 조회가 이 PR 의 설계를 바꿨다.** 조회 전 내 설계는 *"「미해제 CLAIM」 절이 거짓말하니 진짜
미해제를 계산하자"* 였고, 나는 이미 그 계산을 돌려 **352건**이라는 수를 얻어 두었다.
**그 수는 위 기각된 짝짓기와 같은 휴리스틱(영역 문자열 일치)의 산물이다** — 폐기하고 인용하지 않는다.

## 1. 전제 표 (★결과 칸은 전부 실측값 · 예상값 아님)

| # | 전제 | 확인 방법 | 결과(실측 2026-09-03 14:2x~14:5x KST) |
|---|---|---|---|
| P1 | `status` 꼬리 절이 `[NOTE]` 를 안 보여 준다 | `scripts/coord.sh:39` 원문 + 내 노트로 실행 대조 | **참.** 39행 = `grep -nE '\[(CLAIM\|RELEASE)\]'`. 내 NOTE(보드 9985행)가 `status` 출력에 **0건**. |
| P2 | 그런데 NOTE 가 보드의 **다수**다 | 행두 앵커 계수 | **NOTE 1,772 · CLAIM 641 · RELEASE 547**. 음성 대조군 `ZZNOPE` **0**. |
| P3 | 39행 절의 **제목이 사실과 다르다** | 절 제목 vs 실제 출력 | 제목 `미해제 CLAIM(편집 중인 공유영역)` · 실제 출력 **1,224줄**(RELEASE 포함 전량). |
| P4 | `status` 가 보드 **전문**을 덤프해 하류 슬라이싱이 본문과 충돌한다 | 동료 development-ai-3c 실측 재현 | **참.** 보드 본문에 문자열 `미해제 CLAIM` 이 **11회** 등장 → `sed -n '/미해제 CLAIM/,$p'` 가 **첫 발생**에서 잘라 보드 후반을 절에 포함. 그 세션은 그 때문에 *"NOTE 685건 보인다"* 로 **틀리게** 읽었다가, 마지막 발생으로 다시 잘라 **자기 부채 3건 전부 0건**임을 확인했다. |
| P5 | 보드 규모 | `wc -l` | **10,005줄**. `status` 는 이 전문을 매번 `cat` 한다. |
| P6 | CLAIM/RELEASE 줄 중 **형식이 안 맞아 못 읽는 것**이 있다 | 정규식 파싱 계수 | **109줄**(행두는 맞으나 `<- 브랜치 (시각)` 꼬리 없음 = 여러 줄 엔트리). 내 첫 NOTE 도 같은 형태였다. |
| P7 | 짝짓기로 「미해제」를 계산할 수 있는가 | §0 볼트 + 자체 시도 | **아니오.** 볼트에 **양성 대조군 실패**로 기록. 내 시도도 같은 휴리스틱이라 **폐기**. |

## 2. 변경 내용과 그것이 회귀가 아닌 근거

**(가) `status` 에 `=== 최근 NOTE ===` 절을 추가**(꼬리에 위치) — P1·P2 해소.
**(나) 39행 절의 제목을 사실과 일치**시키고 최근 N건으로 절단 — P3 해소. ★**짝짓기는 하지 않는다**(P7).
**(다) `summary` 서브커맨드 신설** — 보드 전문을 **덤프하지 않고** 계산된 절만 출력. P4 해소
(본문을 안 뱉으므로 하류 슬라이싱이 본문과 충돌할 수 없다).

★**회귀가 아닌 근거**: `status` 는 **전량 무절단**을 유지한다 — 보드 전문 `cat` 도, 종전 꼬리 절의
제목과 출력도 그대로다. 요약·절단은 **`summary` 에서만** 한다.
★**1판은 이것이 거짓이었다**(적대 리뷰 실측): 1판은 `status` 의 꼬리 절도 최근 N건으로 잘라
실제 보드에서 **1,226줄 → 12줄, 1,214줄이 소실**됐다. 그런데 **문서화된 소비자가 실재한다** —
인계서들이 **"★자르지 마라 — NOTE 줄에 배포 요청이 숨는다(내가 30분 놓쳤다)"** 라고 명시한다.
즉 1판의 「후방호환」·「가산적」 선언은 **선언이었고 측정이 아니었다.** 2판에서 실제로 가산으로 바꿨고
`test_status_section_is_not_truncated` 로 잠갔다.
★**「미해제」라는 말을 제목에서 뺀다** — 계산하지 않는 것을 계산한다고 말하지 않는다(P3·P7).

## 3. ★검증하지 못한 것 (비워 두지 않는다)

- **짝짓기 기반 「진짜 미해제 수」는 미측정이며 이 PR 은 그것을 시도하지 않는다.** 볼트에 양성 대조군
  실패로 기록돼 있다. 내가 얻은 **352 라는 수는 폐기**한다 — 같은 결함을 가진 휴리스틱의 산물이다.
- **여러 줄 NOTE 의 2번째 줄 이후는 요약에 안 나온다**(첫 줄만). 이건 **의도된 절단**이며 그렇게 **표기**한다.
  근본 해소(여러 줄 엔트리 정규화)는 이 PR 범위 밖 — 부채로 남긴다.
- **동시 `>>` 추가의 원자성 미측정.** 6개 세션이 동시에 쓴다. 4KiB 미만 단일 `printf` 는 실무상 원자적이나
  **재지 않았다**. 긴 NOTE 는 초과할 수 있다. 부채로 남긴다.
- **`claim` 이 세션 이름을 안 적는 것**(볼트의 미착수 부채)은 **이 PR 에서 고치지 않는다** — CLAIM 줄
  형식 변경은 이미 109줄이 못 읽히는 상황에서 다른 세션의 파서를 깰 수 있다.

## 4. 되돌리기 경로

**표시 전용**이다. `git revert` 로 완전 복구된다 — **전방 비가역 요소 없음**(`#952` 와 달리 저장 부작용 없음).
★**1판의 「보드를 쓰지 않는다(읽기만)」는 거짓이었다**(적대 리뷰 실측): 조회 명령도 상단 초기화를 타
`mkdir -p` + `BOARD.md` 를 **생성**했다. `COORD_DIR` 이 틀리면 **빈 유령 보드를 만들어 놓고 자신 있게
「(없음)」을 보고**한다 — §26 「0건 위음성」 그 자체다. → 2판은 `summary` 만 고쳤고 **`status` 는 여전히 `ensure_board` 를 불렀다** — 즉 위 문장은
2판 시점에 **거짓이었고, 나는 그것을 「실측」으로 단정했다**(증거 규율 §1 위반: 추론을 관측처럼).
3판에서 `status` 도 `require_board` 로 바꿨다. ★그리고 첫 봉합은 «COORD_DIR 을 명시했으면 거부»
였는데 그러면 **writer 도 막혀** 새 보드를 만들 수 없었다 — 축은 «명시했나» 가 아니라
**«읽으러 왔나 쓰러 왔나»** 다. 내가 그 회귀를 **새로 만든 왕복 테스트로** 첫 실행에서 잡았다.

## 5. 잠금 — 이 변경을 지키는 검사

`propai-platform/tests/test_coord_summary_contract.py`:

1. **NOTE 가시성**(P1) — 합성 보드에 NOTE 를 넣고 `summary`/`status` 출력에 나오는지.
   ★**두 모집단**: NOTE 가 보이고 **동시에** 음성 대조군 문자열은 안 보인다.
2. **공허 진리 방지** — 단언 앞에 **대상 존재**를 먼저 단언(합성 보드에 NOTE 가 실제로 있다).
3. **제목 정직성**(P3) — 요약 제목에 **「미해제」가 없다**. ★이것이 P7 의 기계 강제다:
   누군가 짝짓기 없이 「미해제」라고 다시 쓰면 빨개진다.
4. **절단 표기**(§3) — 절단하는 절은 **전문 조회 방법**을 함께 인쇄한다.
5. **`summary` 는 보드 전문을 덤프하지 않는다**(P4) — 본문에만 있는 카나리가 `summary` 출력에 **없어야** 한다.
   ★이것이 하류 슬라이싱 충돌 재발을 막는 축이다.

★**부채는 초록 안에 보이게** 둔다(`@pytest.mark.xfail` / `it.todo` 상당) — 짝짓기 미구현·여러 줄 절단·원자성.

---

## 6. ★2판 — 독립 적대 리뷰가 1판을 무너뜨렸다 (반증 임무)

1판은 **작성자가 고른 변이 5종에 5/5 CAUGHT** 를 받았다. 리뷰어가 **자기 변이 10종을 넣었고
10종 전부 SURVIVED** 했다(양성 대조군 2종은 CAUGHT — 판정 하네스는 살아 있었다).

> ★**「N/N CAUGHT」는 내가 고른 변이에만 참이다.** 이 저장소에서 반복 실측된 형태이고,
> 나는 그것을 알면서도 1판을 「잠겼다」고 부를 뻔했다. **막은 것은 규율이 아니라 독립 리뷰다.**

### 실코드 결함(락이 아니라 **내 코드**가 틀렸다)

| # | 결함 | 실측 |
|---|---|---|
| C1 | **`cut -c` 는 바이트 기반**이라 한글을 문자 중간에서 자른다 | 라이브 보드 NOTE **1,780건 중 1,369건 절단 · 533건 UTF-8 파손**. ★이 PR 이 **새로 보이게 만든 바로 그 노트**가 깨진다 |
| C2 | `(없음)` 폴백이 **조회기 사망과 진짜 0건을 뭉갠다** | `COORD_SUMMARY_N=abc` 에서 노트 수천 건이 있는데 **`(없음)` · rc=0** |
| C3 | 조회 명령이 **유령 보드를 생성** | 빈 디렉토리로 실행하면 `BOARD.md` 가 새로 생기고 자신 있게 「없음」 |
| C4 | `status` 절단이 **후방호환을 깼다** | 1,226 → 12줄. 문서화된 소비자가 *"자르지 마라"* 를 명시 |
| C5 | 주석에 **휘발성 수치를 박았다** | 몇 시간 만에 낡음(§28 위반 — 이 PR 이 그 규율을 인용하면서) → **재측정 명령으로 교체** |

### 락 결함(잠갔다고 믿었는데 안 잠긴 축)

M1 환경변수 배선 · M2/M3 **행두 앵커** · M4 절단 안내(**두 절 중 한쪽만 봄**) ·
M6 **순서**(픽스처가 NOTE 1건이라 `head`↔`tail` 차가 **0** — 차가 0인 픽스처는 잠금이 아니다) ·
M10 제목 정직성(**첫 절만** 봐서 다른 절 제목은 무방비).

### ★구조적 결함 — 부채가 상환 불가였다

1판은 `_BODY_CANARY` 하나가 두 락에서 **정반대 요구**를 받았다(락 ⑤ 는 없어야, 부채 ② 는 있어야).
부채를 정직하게 갚으면 락이 빨개진다 — **CLAUDE.md 「완결 가능한 구현계획」 위반**이다.
→ 카나리를 **셋으로 분리**했다(`_NOTE_CANARY` · `_CONT_CANARY` · `_BODY_CANARY`).
이제 「NOTE 의 이어지는 줄만 내보내기」로 부채를 **갚을 수 있다**.

### 2판 검증

`15 passed · 2 xfailed`. ★**리뷰어의 sed 표현식을 그대로 재실행**해 CAUGHT 를 확인한다
(내 변이가 아니라 **그의 변이**로 재는 것이 요점이다).


---

## 7. ★3판 — 두 번째 독립 리뷰도 REJECT 했다 (21종 중 15종 생존)

    1판: 내 변이 5/5 CAUGHT      → 리뷰어 변이 **10/10 SURVIVED**
    2판: 리뷰어 변이 11/12 CAUGHT → 다른 리뷰어 변이 **15/21 SURVIVED**

★**2판이 실패한 이유가 3판의 설계 원리다.** 2판은 «표지 하나 → 표지 전부» 로 일반화했는데
모집단은 **한 층 위**였다: 읽는 자리가 **셋**(`summary`-CR · `summary`-NOTE · `status`-NOTE)인데
각 축을 **한 칸에서만** 잠갔다. → 칸을 `_CELLS` 로 **파생**시키고 모든 축을 그 위에 `parametrize`.

| # | 지적 | 실측 | 처방 |
|---|---|---|---|
| C1 | `status` 가 **유령 보드**를 만들고 rc=0 으로 *"실제로 0건"* 단정 | `COORD_DIR=/tmp/typo bash coord.sh status` → 보드 생성 · rc=0 | `status` → `require_board`. **계획서의 거짓 단정도 정정**(위 §4) |
| C2 | 부채 xfail 이 **수학적으로 만족 불가** | 같은 `seg` 에 `"영역0" in` 과 `not in` 을 동시 요구 → 짝짓기를 **정확히 구현해도 XPASS 안 남** = 영구 초록 장식 | 전제를 **별도 non-xfail 테스트**로 분리 |
| M2 | 모든 축이 **세 칸 중 한 칸**만 잠금 | env×2 · order×2 · anchor×1 = **5종 생존** | `_CELLS` 파생 + `parametrize` |
| M3 | `SUMMARY_N` 게이트가 **열거형** | `00`·`000` 이 새어 **rc=141 · stdout·stderr 둘 다 빈** 무성 사망 | **모양** 검사 + **파티션** 락(12값) |
| M4 | 절단이 **문자 단위인지** 무잠금 | 바이트 절단 복귀 변이 SURVIVED(파손 없이 240자→약 80자) | **문자 수 == 240** 단언(양방향 동시 차단) |
| M5 | **생산자↔소비자 왕복 무잠금** | `printf '- [NOTE]'` → `'* NOTE'` SURVIVED. 픽스처가 전부 손제작이었다 | `note`/`claim`/`release` 를 **실제로 실행**하고 읽는 왕복 테스트 |
| M6 | CI 경로 필터가 `scripts/` 를 **안 본다** | `scripts/coord.sh` 미매치 → 잡 skip → **skip 이 필수 충족으로 계수** | 필터에 `^scripts/` + `test_ci_path_filter_covers_locked_scripts.py`(파생) |
| M7 | *"재현 수단이 없다"* 는 주석이 **거짓** | `PATH` 앞에 `exit 2` 짜리 `grep` 스텁으로 3줄 재현 | 산문 대신 **락**. ★*"재현할 수 없다"* 도 측정 대상이다 |
| M8 | 계획서가 거짓을 **실측으로 단정** | §4 의 `ensure_board` 문장 · §6 의 통과 수 | 정정(위) |

★**부채 셋을 전부 `xfail(strict)` 로** 초록 안에 뒀다 — 짝짓기 · 여러 줄 이어짐(★상환 시 앵커 락과
충돌하지 않는 **제약까지 명시**) · **동시 추가 원자성**(2판은 이것을 계획서에만 적고 초록에 안 뒀다).

★**3판 검증**: 38 passed · 3 xfailed(코드 필터 락 3 포함). 리뷰어 sed 재실행 결과는 커밋에 싣는다.
