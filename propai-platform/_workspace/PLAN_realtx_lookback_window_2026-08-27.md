# 계획 — 실거래 수집 창(등기 정정을 놓치는 구조적 결함)

> 기준 커밋 `3dc036e2` · 전제 측정 **2026-08-26 21:2x~21:3xZ · 활성 컨테이너 propai-api-8000** · 브랜치 `fix/realtx-lookback-window`

## 0. 옵시디언·보드 조회 (계획 게이트 §0)

| 찾을 것 | 결과 |
|---|---|
| **이미 기각된 접근** | **없음** |
| **같은 클래스의 앞선 결함** | ★**있다 — 오늘 내가 낸 것**. `#855` 의 멱등키가 라이브 4%를 덮어썼고 독립 리뷰가 잡았다. 그때 교훈이 *"출하 전에 라이브로 재라"* 였는데, **수집 창은 안 쟀다**(코드가 돌기 전이라 잴 데이터가 없었다) |
| **미결·부채** | 새 테이블 3종 **소비처 0**(`#860` 에서 `xfail` 로 표시) — 별건 |
| **이전 판단의 근거** | 인계서 *"등기일자는 원천에서 약 30%만 기재"* → **모집단 미표기라 무의미**(§1 P2) |

## 1. 전제 표 — 측정 방법과 **실제 값**

| # | 전제 | 확인 방법 | 결과(관측) |
|---|---|---|---|
| P1 | 프로덕션 첫 실행이 성공했다 | arq 로그 + DB 대조 | **참.** `19:10:00 → 19:11:43(103.58s)` · `targets 6 · months 3 · submitted 4898 · corrections 0`. DB `realtx_trades` **4,898** = 투입과 **일치(손실 0)** · `scan_state` **36** = 6×3×2 전부 `baseline_done`(→ **실패 스코프 0** 을 파생으로 증명) |
| P2 | 쌍둥이 봉합이 실제로 필요했다 | 저장분에서 불변키 중복 집계 | **참.** 쌍둥이 **172그룹 · 280행(5.7%)** — 봉합 없었으면 소실. 출하 전 예측 4.0% 보다 **크다** |
| P3 | 인계서 *"등기 약 30% 기재"* | 노출 기간별 재측정(41465 apt) | **모집단 미표기로 무의미.** 기재율은 **시간의 함수**다 |
| **P4** | ★**3개월 창이 등기를 충분히 본다** | 노출 1·2·3·5·7개월 비교 | **거짓.** 5.4% → 24.3% → **54.1%** → 96.1% → 97.4%. **3개월은 54%만** 본다 |
| **P5** | 계약→등기 간격 | **완전관측** 달(202601 · 7개월 노출 · 표본 629) | 중앙 **72일** · p90 **103** · p95 **109** · 최대 **150**. **90일 초과 148건(23.5%)** |
| **P6** | ★**절단된 표본은 무엇을 말하나** | 같은 지표를 1개월 노출 달로 | **90일 초과 0건(0.0%)** — **확신에 찬 오답**. 내 저장분(3개월)만 봐도 같은 0%가 나온다 |
| P7 | 해제도 같은 시간상수인가 | 노출별 해제율 | **아니다.** 2.9 · 2.3 · 2.6 · 2.1 · 2.5% — **1개월부터 평평**. 해제는 3개월로 충분 |
| P8 | 쿼터 실측 | arq 로그 소요시간 | 36 스코프 **103.58초**. MOLIT 키는 G2B 와 **동일**(2시간마다 `fetched:300` 소모) |
| P9 | 파이썬 기준선 | CI 동일 명령 | §회귀에 기재 |

### ★P6 이 이 계획의 핵심이다

**내가 저장한 데이터만 보면 "3개월로 충분하다"는 답이 나온다.** 창이 관측을 자르기 때문이다
(우측 절단). **완전 관측된 옛 달을 따로 태우고서야** 23.5%가 보였다.
→ *"내가 잰 것이 사용자가 쓰는 그것인가"* 의 **시간축 판**이다.

## 2. 변경 내용 — **두 속도**로 나눈다

해제와 등기는 **시간상수가 다르다**(P4 vs P7). 하나의 창으로 둘을 덮으면
등기에 맞춘 창이 매일 불필요한 요청을 내고, 해제에 맞춘 창이 등기를 놓친다.

| 축 | 창 | 주기 | 근거 |
|---|---|---|---|
| **최근** | 3개월 | **매일** | 해제가 1개월부터 평평(P7) · 신규 거래 유입 |
| **꼬리** | 4~7개월째 | **주 1회**(요일 고정) | 등기가 5개월에 포화(P4) · 그 구간은 하루 단위로 변하지 않는다 |

**쿼터(P8 기준 계산)**: 현재 36 스코프/일. 꼬리 4개월 추가 시 **주 1회 48 스코프**.
주간 평균 = `36 + 48/7 ≈ 43 스코프/일` — **19% 증가**. 소요시간 추정 103.58s × 43/36 ≈ **124초**.

**회귀가 아닌 근거**: 저장 계약(`persist_scope`·멱등키·baseline)은 **손대지 않는다**.
꼬리 달은 처음 수집될 때 그 스코프의 baseline 이 되므로 **첫 실행에 정정 0** 은 유지된다.

## 2-5. ★§3-1 의 미측정을 재 봤더니 **설계가 바뀌었다**

계획서 §3-1 에 *"표본이 시군구 1개(41465 apt) · land 미측정"* 이라 적어 두고, **바로 재 봤다.**
저장분 **전수**(4,898행) 실측:

| 유형 | 행 | **등기** | 해제 |
|---|---|---|---|
| `apt` | 4,110 | **737 (17.9%)** | 79 (1.9%) |
| **`land`** | 788 | **0 (0.0%)** — 6개 시군구 **전부 0** | 49 (**6.2%**) |

**MOLIT 토지 API 는 등기일자를 아예 주지 않는다.** 대조군으로 같은 조회에서 `apt` 는 나온다
(노출별 28.6% · 12.8% · 3.8%) → **조회기가 죽은 것이 아니라 원천이 안 준다.**

★꼬리 창은 **오직 등기를 잡으려고** 있으므로 `land` 를 넣으면 **잡을 것이 없는 요청**이다.
→ `TAIL_PROP_TYPES = ("apt",)`. **쿼터가 개선됐다**: 꼬리일 84 → **60 스코프(29% 절감)**,
주간 평균 `36 + 24/7 ≈ 39.4/일` — 종전 계획 43 대비 낮고 **현행 대비 9.5% 증가**에 그친다.
★토지의 신호는 **해제**(6.2% = apt 의 3배)이고 해제는 1개월부터 평평하므로
  **최근 3개월 창으로 충분**하다 — 최근 창에서는 `land` 를 그대로 조회한다.

## 3. ★검증하지 못한 것

1. **등기 시간상수의 표본이 시군구 1개(`41465` · `apt`)** 다. 다른 시군구의 포화 곡선은
   **미측정**(단, `land` 의 등기 0%는 §2-5 에서 **6개 시군구 전수**로 확인했다).
2. **7개월이 진짜 포화점인지** 모른다 — 5개월 96.1% → 7개월 97.4% 로 완만하지만,
   10개월·12개월은 안 쟀다. 꼬리를 **7개월로 자른 것 자체가 새 절단**이다(P6 의 재발 가능성).
3. **MOLIT 일일 쿼터의 실제 수치**는 여전히 미측정. 19% 증가가 안전한지 **모른다** —
   초과 시 429/403 은 이미 사유를 싣고 재시도하지 않는다(그건 기존 설계).
4. **주간 실행이 실제로 발화하는지**는 배포 후에만 안다.
5. 새 테이블 **소비처 0** — 4,898행이 프로덕션에 있는데 **읽는 코드가 없다**(별건 부채).

## 3-2. ★독립 적대 리뷰가 낸 것 (2026-08-27 · 판정 REQUEST CHANGES → 봉합)

**전부 저자가 재현해 확증했다.** 특히 **C1 은 이 저장소 교훈의 세 번째 재발**이다.

| # | 지적 | 상태 |
|---|---|---|
| **C1** | ★**배선이 통째로 무잠금** — PR 전체를 되돌려도 락 15건이 전부 초록. `sync_realtx_trades` 를 임포트하는 테스트가 저장소 **0건**. 저자 주장 「변이 9/9 CAUGHT」는 **거짓**(9건 전부 함수 안·상수) | **봉합** — 스텁으로 `sync_realtx_trades` 를 태워 **호출 인자 삼중쌍**을 단언. 리뷰어 변이 M2·M6·M7 이 **CAUGHT 로 뒤집힘** |
| **H1** | 소스 주석의 쿼터 수치가 **두 번째 커밋에서 낡았는데 안 고쳐짐**(48/43 → 실제 24/39.4) | 봉합 + 테스트에 결속 |
| **H2** | 꼬리일 쿼터 소진 시 **뒤쪽 시군구가 최근 창까지 잃는다**. `targets` 정렬이라 **매주 같은 시군구**가 희생 — 이 파일이 저장 실패에 대해 이미 적어 둔 결함의 **조회 판** | 봉합 — 루프를 **월-major** 로. ★**단, 그 봉합은 2026-08-27T23:5xZ 까지 무잠금이었다**(§3-4) |
| **H3** | `TAIL_MONTHS=7` 이 **캐시 TTL 절벽 바로 위**. 8로 올리면 TTL **24h → 720h** 라 주간 조회가 캐시에 삼켜지는데 `submitted` 는 정상이라 **성공처럼 보인다** | 봉합 — 상수 결속 락 + **절벽 실재를 실제 함수로** 확인(공허 방지) |
| **M1** | `tail_included` **소비처 0** — 내 주석의 *"조용히 멈춰도 드러난다"* 가 **거짓** | 주석을 사실로 낮추고 **부채로 명시** |
| **M2** | *"MOLIT 이 안 준다"* 는 **추론을 관측으로** 적은 것. 대조군이 **다른 엔드포인트**이고 표본이 **우측 절단**(이 PR 의 핵심 통찰을 land 판정에서 반복) | 주석을 **관측/추론으로 갈라** 다시 씀 |
| **M3** | `status` 가 `fetch_errors` 를 무시 — 전 스코프 429 여도 `"ok"` | 봉합. ★첫 락이 **내 주석에 걸려** SURVIVED → **파서로** 교체 |
| **M5** | 쿼터 락이 `TAIL_MONTHS==7` 을 **양방향 고정**해 문서가 권하는 변경을 막음(위양성) | 상수 **파생**으로 |
| **M6** | `# 수요일` 이 **UTC** 인데 실행은 **KST 목요일 04:10** | 표기 정정 |
| **L1** | `RECENT_MONTHS >= 1` 은 **공허**(`max(1, months)` 때문) | `== 3` + 사유 |

★리뷰가 **반증에 실패한 것**(= 저자가 옳았던 것): `recent_months` 의 월말·윤년·연말 산술 ·
`_now` 단일 평가 · 쿼터 산술 39.4/일 · `429` 가 "거래 0건"으로 위조되지 않음 ·
`ON CONFLICT` 가 실제로 `registered_date` 를 갱신(이 전제가 깨졌으면 PR 전체가 무의미).

## 3-3. ★리뷰가 지목한 **빠진 미검증**(§3 에 승격)

| # | 항목 |
|---|---|
| P-a | `TAIL_MONTHS` ↔ `_RECENT_MONTHS_SHORT_TTL` **캐시 절벽 결합** → **락으로 봉합** |
| P-b | **배선 무잠금** → **락으로 봉합** |
| P-c | 꼬리일 쿼터 절단 시 **무엇이 잘리는가** → 월-major 로 완화(**쿼터 실수치는 여전히 미측정**) |
| P-d | `targets` 수가 **사용자 증가에 따라 무제한** — 39.4/일 산술은 시군구 6 고정 가정. **미봉합** |
| P-e | `tail_included` **소비처 0** — 주석은 낮췄으나 **관측 경로는 여전히 없음** |
| P-f | `status` 가 `fetch_errors` 무시 → **봉합** |
| P-g | 일일 캐시 TTL(86,400s)이 **크론 간격과 동일** — 최근 창 재조회가 부분 무효일 수 있다. **미측정** |
| P-h | land 등기 0% 판정의 **표본이 우측 절단** — 주석을 추론으로 낮췄으나 **원문 키 목록 미확인** |
| P-i | arq 크론은 **놓친 주간 실행을 백필하지 않는다** |

## 3-4. ★★후속 세션 실측 — **H2 봉합에 락이 없었다** (2026-08-27 · development-ai-ae)

인계서 §5-1 이 *"변이 9/9 CAUGHT 는 거짓이었다 — 전부 함수 안이었다"* 라고 경고했으므로
**「봉합됨」 라벨을 승계하지 않고 다시 쟀다.** 6층에 변이를 넣었다(`scripts/mutate_manual.sh`):

| 변이 | 층 | 결과 |
|---|---|---|
| `months_for(_now)` 호출 우회(꼬리가 영원히 안 돈다) | **배선** | CAUGHT (2건) |
| `prop_types_for(...)` → `DEFAULT_PROP_TYPES`(꼬리에 land) | **배선** | CAUGHT (2건) |
| `recent` 를 `months` 로(꼬리 달을 '최근'으로 오분류) | **배선** | CAUGHT (2건) |
| `TAIL_MONTHS = 3`(**원래 결함 복원**) | 상수 | CAUGHT (8건) |
| `TAIL_MONTHS = 8`(캐시 TTL 절벽 넘김) | 계약 결속 | CAUGHT (2건) |
| `include_tail = True`(주기 조건 반전) | 주기 | CAUGHT (5건) |
| `status` 가 `fetch_errors` 무시 | 결과 판정 | CAUGHT (1건) |
| ★**루프를 시군구-major 로 되돌림**(= H2 봉합 무력화) | **루프 순서** | ★**SURVIVED — 22건 전부 초록** |

★**근본은 수집기가 아니라 필터였다.** 락은 호출 인자 삼중쌍을 파생형으로 모아 놓고도
`_run_and_capture` 가 그것을 **`set` 으로 접어 순서를 구조적으로 버렸다.** 즉 *"무엇을
조회하는가"* 는 잠갔는데 *"어떤 순서로"* 는 **어떤 단언도 볼 수 없었다.**
→ `_run_and_capture_client` 를 갈라내고 락 2건을 추가했다(22 → **24 passed**):

- `test_tail_run_is_month_major_not_region_major` — **순서**(공허 방지: 시군구≥2·월≥2 선단언)
- `test_quota_truncation_never_starves_a_region_of_the_recent_window` — ★**행위**.
  쿼터를 `RECENT_MONTHS × 유형 × 시군구` 에서 끊고 **모든 시군구가 최근 창을 온전히
  받았는지**를 단언한다. 음성 대조군으로 *"쿼터가 실제로 끊겼는가"* 를 먼저 단언한다.
  ★순서만 잠그면 *"부른다"* 를 잠그는 것이다 — H2 가 말한 **피해**는 순서가 아니라
  **뒤쪽 시군구의 해제 신호 상실**이므로 그 행위를 태운다.

★**교훈**: 「봉합」은 라벨이고, **락이 산출물**이다. 리뷰가 지적한 항목을 고쳤다고
해서 그 수정이 잠긴 것은 아니다 — 리뷰 대응 커밋마다 *"이 봉합을 되돌리는 변이가
CAUGHT 인가"* 를 따로 물어야 한다.

## 4. 되돌리기 경로

상수 2개와 분기 하나. 되돌리면 종전 동작(3개월 매일). 저장 데이터는 **버려지지 않는다**.

## 5. 잠금

- **두 모집단**: 꼬리 실행일과 비실행일이 **다른 스코프 집합**을 낸다(같으면 배선이 무잠금)
- **`recent_months` 는 결정적** — `now` 를 인자로 받는다(기존 계약 유지)
- 꼬리 구간이 **최근 구간과 겹치지 않는다**(중복 요청 방지)
- ★**절단 회피 락**: 꼬리 상한이 **P4 의 포화점 이상**임을 상수로 결속(3으로 되돌리면 실패)
- 변이: 꼬리 제거 · 주기 조건 반전 · 구간 겹침 — 각각 CAUGHT 여야 한다
- ★**루프 순서**: 시군구-major 로 되돌리는 변이가 CAUGHT 여야 한다(§3-4 — 종전 SURVIVED)
- ★**쿼터 절단 하 기아**: 최근 창을 잃는 시군구가 0이어야 한다(H2 의 피해 그 자체)
