# 인계 프롬프트 — 층위 조상 가드 이후 (2026-08-12 → 다음 세션)

아래를 그대로 다음 세션 첫 지시로 쓰면 된다.

---

## 0. 착수 전 필수

1. `CLAUDE.md` 의 "회귀망·검증 규율"을 읽어라.
2. `gh pr list` 를 **제목까지** 읽어라. `git log origin/main --oneline -15` 도 함께.
3. `scripts/coord.sh status` — 여러 세션이 동시에 돈다.
4. 옵시디언: `AI-Sessions/wiki/errors/2026-08-12_인계받은_심각도를_그대로_믿으면_없는_결함을_고친다.md`
5. `git fetch` 후 **origin/main 기준 전용 워크트리**. 공유 메인에서 feature checkout 금지.

## 1. 지금 상태

- **PR #607** — 층위 조상 가드 v4 재정립. **제품 코드 수정 0**(감시망·계약 주석만).
- 선행 인계(#606)가 넘긴 ②"계약값이 안 먹는 마운트 5곳"의 **처방은 하지 않았다** —
  아래 §2 의 이유로 **고칠 대상이 없다**.

## 2. ★선행 인계의 심각도 주장은 반증됐다 (라이브 실측 2026-08-12)

| 화면 | 잘림 | 스태킹 트랩 | 판정 |
|---|---|---|---|
| `/ko/projects/new` | **0px** | `relative z-10` ×2 → 실효 10 | 겹치는 경쟁자 **0** · 팝오버 3점 전부 자기 히트 |
| `/ko/precheck?legacy=1` | **0px** | **없음** | z-650 유효 |

- 인용됐던 `scrollHeight 341 vs height 256` 은 **팝오버 자신의 `max-h-64 overflow-y-auto`**
  (설계된 내부 스크롤)다. 조상 클리핑의 증거가 아니다.
- 구조적으로도 잘릴 수 없다: 다필지 패널이 검색줄 아래로 **`min-h-[500px]` 본문을 무조건**
  렌더하고, 클리핑 조상 6곳 중 **높이 제약을 가진 곳은 0곳**이다.
- ★`?legacy=1` 은 sticky ContextHeader(600)와 팝오버가 **공존하는 유일한 화면**이다
  (`stickyContextHeader` 소비처는 `SatongMapShell` 뿐). 거기서 트랩이 0개였다.

**재측정 방법**(다시 의심되면 그대로 재현):
로그인 → 주소창에 `역삼동` **입력만**(Enter 는 첫 후보를 선택해 버린다) → 3.5초 대기 →
조상을 훑어 `overflow!==visible` 인 상자들의 하한과 팝오버 하한을 비교(잘림) +
`scrollIntoView` 후 `elementFromPoint` 3점(가림). ★뷰포트 밖 좌표는 `null` 을 돌려준다 —
스크롤 없이 재면 "가려졌다"로 오독한다.

## 3. 남은 것

1. **(선택·증상 없음)** `/ko/projects/new` 의 `relative z-10` ×2 에서 `z-10` 만 제거하면
   계약이 실효화된다. 장식(`.cc-grid-bg`)이 DOM 앞에 오므로 `relative` 만 남겨도 페인트
   순서는 유지된다(정적 근거 — 제거한다면 라이브로 재확인할 것).
   ★그 화면에 sticky 헤더가 생기면 이건 즉시 실결함이 된다.
2. **(부채)** 소비처 조상 체인까지 태우는 **파생형** 가드. 현재 가드는 단독 렌더라
   소비처 트랩을 구조적으로 못 본다. `it.todo` 로 초록 안에 노출돼 있다.
   렌더 사이트 실측: `<GlobalAddressSearch` 14곳 + `<ProjectAddressInput` 14곳.
3. **(사각지대)** 수작업 CSS(`.glass` 27회 · `.cc-panel` 33파일 · `.leaflet-container`) ·
   인라인 style · framer-motion 인라인 transform 은 **클래스 토큰 가드로 원리적으로 못 본다.**
   그 층은 e2e 페인트 순서로만 잡힌다.
4. **(보류)** 포털 처방 — 고칠 결함이 없어 보류했다. 하려면 조건 3개를 같은 커밋에:
   ①`top-full` 을 죽은 클래스로 남기지 말 것(가드 셀렉터를 하중 지는 것으로 교체 +
   `layer-ladder.contract.test.tsx` 의 파생·명시 단언 갱신) ②`contentPopover 650 < appModal 800`
   서열을 **의식적으로 결정** ③rect·스크롤·visualViewport 는 jsdom 에서 검증 불가임을 명시하고
   모바일 키보드 상태까지 라이브 측정.

## 4. 재생성 금지 — 이미 반증한 것

1. "팝오버가 85px 잘려 후보 2행 접근 불가" → **거짓**(실측 0px)
2. "`transform`/`filter`/`backdrop-filter` 를 가드에 넣어야 한다" → v4 에서 **층 상자를 안 만든다**
   (저장소 tailwind 4.2.1 직접 컴파일: 빈 변수 합성 → 무효 → `none`)
3. "정규식만 v4 로 넓히면 소비처 트랩이 드러난다" → **거짓**. 가드가 단독 렌더라 조상 체인에
   소비처가 없다. 넓혀도 그 5건은 안 잡힌다(그래서 트랩 픽스처를 따로 세웠다)
4. "`components/sales/ProjectPipelinePanel.tsx`" → 그런 경로는 없다(`components/pipeline/`)

## 5. 작업 방식

실측 → `/insight-loop` 독립 렌즈 → 구현 → **커밋 먼저 → 변이 → 원복** → 게이트 →
독립 적대검증(자기승인 금지) → 머지 → sw 범프(보드 claim 확인) → 배포 → 라이브 검증 → 기록.

★**변이 도구가 러너를 멈춰 세울 수 있다.** 동기 루프의 전진을 지우는 변이는 `testTimeout`
으로 안 끊긴다 — 실행 전체가 타임아웃으로 죽고 **파일이 변이된 채 남는다**(실제로 겪었다).
동기 순회에는 상한을 두고, 실행 후 `git status` 로 잔재를 확인하라.
★**생존 = 결함 아님.** 타입 라인 생존 3건은 `tsc --noEmit` 이 잡는다 — **추정하지 말고
주입해서 확인**할 것(이번에 그렇게 확인했다). 설명 못 하는 생존만 진짜 구멍이다.
