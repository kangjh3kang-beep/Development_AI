# FastAPI Core
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
httpx==0.27.2
aiohttp==3.10.10

# Database
sqlalchemy[asyncio]==2.0.35
asyncpg==0.30.0
alembic==1.14.0
geoalchemy2==0.15.2
psycopg2-binary==2.9.10

# Cache / Queue
redis==5.2.0
celery==5.3.6
flower==2.0.1
arq>=0.25  # P0-1: apps/worker/main.py가 arq.cron 사용 — 미선언 시 worker 부팅 크래시
prometheus-client>=0.20.0  # P0-2: metrics.py/main.py unguarded import — 미선언 시 API 부팅 실패(매니페스트 SSOT drift)

# AI/ML
torch==2.4.1
torchvision==0.19.1
scikit-learn==1.5.2
xgboost==2.1.2
lightgbm==4.5.0
numpy==1.26.4
pandas==2.2.3
scipy==1.14.1

# LLM / Agents
langchain==0.3.7
langchain-openai==0.2.8
langchain-community==0.3.7
langgraph==0.2.40
openai==1.54.3
faiss-cpu==1.9.0

# GIS / CAD
geopandas==1.0.1
shapely==2.0.6
pyproj==3.7.0
# gdal은 시스템 libgdal 버전과 일치해야 소스빌드된다. 정확핀은 빌드 환경(CI/로컬)의 libgdal과
# 불일치 시 빌드 실패하므로, 시스템에서 제공 가능한 범위로 둔다. CI는 gdal==$(gdal-config --version)로 선설치.
gdal>=3.6,<3.12
folium==0.18.0
ezdxf==1.3.4
svgwrite==1.4.3
ifcopenshell==0.8.0
pygltflib==1.16.5  # IFC4→glTF(.glb) 변환(3D BIM 뷰어). 순수파이썬 경량.

# Image / CV
opencv-python-headless==4.10.0.84
Pillow==10.4.0

# Documents
reportlab==4.2.5
pymupdf==1.24.10  # 해촉증명서 PDF→PNG/JPEG 래스터(순수 wheel·시스템의존 0)
python-pptx==1.0.2
python-docx==1.1.2
openpyxl==3.1.5
fpdf2==2.8.1

# Security
bcrypt>=4.0.0,<5.0.0  # passlib 호환성: bcrypt 5.x와 충돌
PyJWT==2.9.0
cryptography==43.0.3
python-jose[cryptography]==3.3.0

# HTTP / Utils
pydantic[email]==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1
tenacity==9.0.0
structlog==24.4.0
pytz==2024.2

# Energy Simulation
# eppy 제거: 코드 import 0건(미구현 계획 의존)인데 beautifulsoup4<=4.8.0을 강제해 전체 의존성
# 설치를 ResolutionImpossible로 차단(langchain 등은 bs4 4.12 필요). 에너지 시뮬 실제 구현 시
# bs4 호환 버전으로 재도입할 것.

# ML Experiment Tracking
mlflow==2.17.2

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
slowapi==0.1.9
qdrant-client==1.11.3
sse-starlette==2.1.0
langchain-anthropic==0.3.0
casbin==1.43.0
casbin-async-sqlalchemy-adapter==1.17.0
passlib==1.7.4

# v62 분양관리 ERP / 모델하우스 데스크 (Part2~3 사용: 지명매칭/명함OCR/푸시/도면파싱)
phonenumbers==8.13.48
rapidfuzz==3.10.1
firebase-admin==6.5.0
PyMuPDF==1.24.10

# 경공매: 법원경매(courtauction) 스크래핑 파서(무목업, 지연·예의). Selenium/chromium은
# Micro 호스트 OOM 위험이라 미도입 — requests+BeautifulSoup 경량 파서만 사용.
beautifulsoup4==4.12.3
lxml==5.3.0

# LiveKit 화상회의 (Phase 3)
livekit-api==1.1.0
