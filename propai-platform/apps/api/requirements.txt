# FastAPI Core
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
httpx==0.27.2
aiohttp==3.10.10

# Database
sqlalchemy[asyncio]==2.0.35
asyncpg==0.30.0
alembic==1.14.0
geoalchemy2==0.15.2
psycopg2-binary==2.9.10

# Cache / Queue
redis==5.2.0
celery==5.3.6
flower==2.0.1
arq>=0.25  # P0-1: apps/worker/main.py가 arq.cron 사용 — 미선언 시 worker 부팅 크래시
prometheus-client>=0.20.0  # P0-2: metrics.py/main.py unguarded import — 미선언 시 API 부팅 실패(매니페스트 SSOT drift)

# AI/ML
torch==2.4.1
torchvision==0.19.1
scikit-learn==1.5.2
xgboost==2.1.2
lightgbm==4.5.0
numpy==1.26.4
pandas==2.2.3
scipy==1.14.1

# LLM / Agents
langchain==0.3.7
langchain-openai==0.2.8
# Gemini(google) 프로바이더 — get_llm provider="google" 실배선용.
#   ★2.1.5 핀(2.1.12 아님): 2.1.6+는 langchain-core>=0.3.75를 강제 → langchain-core 0.3.79+가
#   langsmith>=0.3.45를 요구하는데, langchain 0.3.7/langchain-community 0.3.7은 langsmith<0.2.0이라
#   교집합 공집합(ResolutionImpossible). 2.1.5는 langchain-core 0.3.63(langsmith 0.1.x)로 해결.
#   ★oracle 빌드(requirements.oracle.txt)에도 동일 버전 필수(prod=oracle).
langchain-google-genai==2.1.5
langchain-community==0.3.7
langgraph==0.2.40
openai==1.54.3
# 이미지 생성 SDK(image_provider.py) — Gemini 이미지("나노바나나") 네이티브 생성용.
#   ★1.2.0 핀: 1.3.0+는 httpx>=0.28.1 요구→openai/qdrant의 httpx==0.27.2 핀과 ResolutionImpossible.
#   ★oracle 빌드(requirements.oracle.txt)에도 동일 버전 필수(prod=oracle).
google-genai==1.2.0
faiss-cpu==1.9.0

# GIS / CAD
geopandas==1.0.1
shapely==2.0.6
pyproj==3.7.0
# gdal은 시스템 libgdal 버전과 일치해야 소스빌드된다. 정확핀은 빌드 환경(CI/로컬)의 libgdal과
# 불일치 시 빌드 실패하므로, 시스템에서 제공 가능한 범위로 둔다. CI는 gdal==$(gdal-config --version)로 선설치.
gdal>=3.6,<3.12
folium==0.18.0
ezdxf==1.3.4
svgwrite==1.4.3
# ★prod(Oracle Dockerfile.oracle→requirements.oracle.txt)가 0.8.4로 동작 중이므로 여기도 0.8.4로 통일.
#   (과거 0.8.0↔0.8.4 불일치 = 로컬/railway와 prod가 다른 ifcopenshell 버전 → 개발-prod 패리티 붕괴·회귀 은폐. C2R P0 치명3)
ifcopenshell==0.8.4
pygltflib==1.16.5  # IFC4→glTF(.glb) 변환(3D BIM 뷰어). 순수파이썬 경량.

# Image / CV
opencv-python-headless==4.10.0.84
Pillow==10.4.0
matplotlib==3.9.2  # 구획도 PNG export(parcel_boundary_export.export_png) — 미선언 시 프로덕션 ModuleNotFoundError

# Documents
reportlab==4.2.5
pymupdf==1.24.10  # 해촉증명서 PDF→PNG/JPEG 래스터(순수 wheel·시스템의존 0)
python-pptx==1.0.2
python-docx==1.1.2
openpyxl==3.1.5
fpdf2==2.8.1

# Security
bcrypt>=4.0.0,<5.0.0  # passlib 호환성: bcrypt 5.x와 충돌
PyJWT==2.9.0
cryptography==43.0.3
python-jose[cryptography]==3.3.0

# HTTP / Utils
pydantic[email]==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1
tenacity==9.0.0
structlog==24.4.0
pytz==2024.2

# Energy Simulation
# eppy 제거: 코드 import 0건(미구현 계획 의존)인데 beautifulsoup4<=4.8.0을 강제해 전체 의존성
# 설치를 ResolutionImpossible로 차단(langchain 등은 bs4 4.12 필요). 에너지 시뮬 실제 구현 시
# bs4 호환 버전으로 재도입할 것.

# ML Experiment Tracking
mlflow==2.17.2

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
slowapi==0.1.9
qdrant-client==1.11.3
sse-starlette==2.1.0
langchain-anthropic==0.3.0
casbin==1.43.0
casbin-async-sqlalchemy-adapter==1.17.0
passlib==1.7.4

# v62 분양관리 ERP / 모델하우스 데스크 (Part2~3 사용: 지명매칭/명함OCR/푸시/도면파싱)
phonenumbers==8.13.48
rapidfuzz==3.10.1
firebase-admin==6.5.0
PyMuPDF==1.24.10

# 경공매: 법원경매(courtauction) 스크래핑 파서(무목업, 지연·예의). Selenium/chromium은
# Micro 호스트 OOM 위험이라 미도입 — requests+BeautifulSoup 경량 파서만 사용.
beautifulsoup4==4.12.3
lxml==5.3.0
# MOLIT 실거래가 XML 파싱 1차 파서(molit_client; 미설치 시 regex 폴백). 누락 시 폴백이라 동작은
# 하나, 1차 경로 활성화·완전성 위해 명시(경량 순수파이썬).
xmltodict==0.14.2

# LiveKit 화상회의 (Phase 3)
livekit-api==1.1.0
