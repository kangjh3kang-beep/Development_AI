/**
 * 사통맵 z-index 계약(SSOT) — Leaflet 내부 pane와 지도 위 React 오버레이 UI의 층위를 한 곳에서 관리한다.
 *
 * ★배경(왜 한 곳에 모으나): 지도(.leaflet-container)는 globals.css에서
 *   `isolation:isolate; z-index:0` 으로 독립 스택 컨텍스트에 격리된다. 따라서 지도 위(형제)에
 *   놓이는 React 오버레이는 z-index ≥ 1 이면 지도 안 모든 pane(타일·폴리곤·마커·툴팁·팝업)
 *   위에 그려진다 → 상시 라벨(툴팁)이 범례·칩·확인카드를 덮던 문제(S5)를 구조적으로 차단한다.
 *   아래 UI_* 상수는 그 격리를 전제로 '오버레이끼리'의 상대 층위를 정의한다.
 *
 * ★사용 규칙: JSX에서는 `style={{ zIndex: SATONG_UI_Z.* }}` 로 인라인 적용한다.
 *   Tailwind v4 는 `z-[${동적값}]` 같은 런타임 문자열 클래스를 생성하지 못하므로(JIT 미탐지),
 *   상수를 클래스가 아닌 인라인 스타일로 흘려보내 SSOT를 깨지 않는다.
 */

/**
 * Leaflet 기본 pane z-index.
 *
 * ★2026-08-17 — **이 값들은 다시 살아 있다.** 종전 주석은 "참고용"이라 했고 실제로도 그랬다:
 *   `globals.css` 의 `.leaflet-pane { z-index:1 !important }` 가 pane 을 전부 1 로 눌러
 *   **지도 내부 사다리 자체가 없었다**(라이브 실측 `.leaflet-popup-pane` = 1).
 *   그 평탄화가 `isolation: isolate` 앞에 **잉여**임을 라이브 실험(합성 z=50 형제 + 음성대조)
 *   으로 확인하고 걷어냈다. 이제 Leaflet 이 실제로 적용하는 값이다.
 *
 * ★단, 이 파일 안에서 `LEAFLET_PANE_Z` 와 `SATONG_PANE_Z` 의 대소를 비교하는 것은
 *   **같은 모듈 리터럴끼리의 항등식**이라 그 자체로는 런타임을 보증하지 못한다.
 *   효과를 보증하는 것은 `lib/__tests__/satong-pane-ladder.test.ts`(CSS 불변식)다.
 */
export const LEAFLET_PANE_Z = {
  tile: 200,
  overlay: 400, // 폴리곤
  shadow: 500,
  marker: 600,
  tooltip: 650,
  popup: 700,
} as const;

/**
 * 커스텀 pane z-index.
 *   label pane 은 폴리곤(overlay=400) 위, 마커(600) 아래에 둔다 —
 *   Hybrid 오버레이 타일(도로·라벨)이 필지 폴리곤 위로 올라오되 마커/툴팁 흐름은 가리지 않게.
 */
export const SATONG_PANE_Z = {
  label: 450,
} as const;

/**
 * 지도 위 React 오버레이(형제) 상대 층위 — 값이 클수록 위.
 *   확인 카드(confirmCard)는 항상 최상위: 사용자 결정(＋추가/제거) 흐름이 어떤 오버레이에도
 *   가려지지 않아야 한다.
 *
 * ★코너 슬롯 레지스트리(x/y — 2026-07-17 겹침 구조 진단의 처방②):
 *   z는 이 레지스트리로 충돌이 사라졌지만 x/y는 각자 absolute 좌표를 고르다 겹침이
 *   3회 재발했다(측정rail↔줌·칩행↔스위처·배너↔완료바). 신규 오버레이는 아래 슬롯
 *   소유권을 확인하고 **빈 슬롯에만** 배치하거나 기존 도크 flow에 합류할 것.
 *   ┌ 좌상: 검색/네비(셸) · 우상: 풀스크린 버튼 + 레이어 레일(right-4 top-20)
 *   ├ 좌중앙: 측정 rail(top-1/2 left-4 — 콜러 계약: 지도높이 500px 미만 배치 금지. 282px는 기하 충돌점이지 계약이 아님 — MultiMap 주석 참조)
 *   ├ 상중앙: 측정 상태 칩(top-14)
 *   ├ 하단 전폭: cornerDock(bottom-16 left-14~right-3) = 상태 칩(좌) + bottomDockSlot(우,
 *   │   베이스맵 스위처) — 하단 신규 요소는 **이 도크 flow에 합류**(독립 absolute 금지:
 *   │   암묵 예약값이 겹침의 근원이었다. 계약 테스트 SatongMultiMap.bottomDock.test.tsx)
 *   └ 지도 기준(Leaflet): 줌=bottomleft · 출처표기=bottomright — 래퍼 기준 absolute와
 *     좌표계가 다름(완료바 높이만큼 어긋남)에 주의.
 */
/**
 * ★본문 ↔ 지도 오버레이 층위 계약 (2026-08-06 — 사용자 실측 지적의 근원).
 *
 * 종전에 이 파일은 **오버레이끼리의 상대 층위만** 정의했고, 지도 밖 본문과의 층위는
 * 어디에도 계약이 없었다. 그 공백에서 실제 결함이 났다:
 *   · 지도 래퍼(SatongMultiMap 의 wrapperRef)는 `relative` 뿐이라 **스태킹 컨텍스트를
 *     만들지 않는다** → 오버레이 380~500 이 **루트에서** 본문과 경쟁한다.
 *   · 그런데 지도 타일은 `.leaflet-container { isolation:isolate; z-index:0 }`(globals.css)
 *     으로 격리돼 본문 아래로 얌전히 지나간다. **지도는 헤더 밑으로 가는데 지도 위
 *     컨트롤만 헤더 위로 뜨는** 비대칭이 여기서 나온다.
 *   · 그 결과 sticky ContextHeader(종전 z-30)가 스크롤 중 지도 영역에 들어오면
 *     레이어 레일·팝오버·코너 도크에 **가려졌다**. sticky 를 건 목적(집계 SSOT 를 스크롤
 *     중에도 보이게)을 오버레이가 무효화한 셈이다.
 *
 * ★그래서 "지도 위에 떠 있어야 하는 본문"의 층위를 여기서 함께 계약한다 —
 *   오버레이 최대(confirmCard 500) **위**, 앱 셸 크롬(헤더 z-1000) **아래**.
 *   ※ 대안으로 지도 래퍼에 `isolate` 를 넣어 오버레이를 지도 박스에 가두는 방법이 있으나,
 *     그러면 루트의 플로팅 AI 버튼(z-95)이 지도 컨트롤 **위**로 올라와 의도한 서열
 *     (지도 위에서는 지도 컨트롤이 이긴다)이 뒤집힌다. 그래서 본문 쪽을 올린다.
 */
/**
 * ★★이 계약이 성립할 **전제 조건** — 2026-08-12 추가. 값만 적고 조건을 안 적어 두 번 데였다.
 *
 * 아래 값은 "화면 전체에서 몇 번째로 위인가"가 **아니다.** 두 조건이 지켜질 때만 의미를 갖는다.
 * 조건이 깨지면 값은 **그대로인데 효력만 사라진다** — 가장 찾기 어려운 형태의 결함이다.
 *
 *   ① 조상이 **스태킹 컨텍스트(자기만의 층 상자)를 만들지 않아야 한다.**
 *      `relative`+`z-*` · `isolate` · `backdrop-blur-*` · `blur-*` · `opacity-N`(N<100) ·
 *      `scale-*`/`rotate-*`/`translate-*` · `mix-blend-*` · `will-change-*` · `contain-paint`,
 *      그리고 framer-motion 처럼 **인라인 `transform`** 을 쓰는 래퍼까지 포함된다.
 *      하나라도 있으면 그 상자 **안에서만** 유효하고, 바깥에서는 **조상의 z** 로 취급된다.
 *      (실제 사고: CAD 전체화면 `z-[9990]` 이 `relative z-10` 에 갇혀 **실효 10**)
 *   ② 조상에 **`overflow-hidden|clip|auto|scroll` 이 없어야 한다.**
 *      있으면 z 와 무관하게 상자 밖이 잘린다. 상자 안 여유가 충분하면 무해할 수 있으나
 *      그건 **기하 우연**이다 — 무해하다고 판단했다면 그 근거(여유 높이)를 테스트로 잠글 것.
 *      (예: `GlobalAddressSearch.popoverRung.test.tsx` 가 "본문 최소높이 > 팝오버 최대높이"를 잠근다)
 *
 * ★조건을 판정하는 도구는 `lib/stacking-context.ts` 의 `scanAncestorTraps` 다.
 *   새 소비처를 붙일 때 값만 쓰지 말고 **조상 체인을 그 판정기로 검사하는 회귀망**을 같이 둔다.
 * ★이 문단이 없어서 실제로 반복됐다 — CAD 전체화면에서 겪고도 조건이 계약에 안 적혀,
 *   주소 팝오버(`contentPopover`)가 소비처 조상에 같은 방식으로 갇혔다.
 *   ※ 조상 트랩이 실재하는 소비처는 2026-08-12 실측 8곳이며, 처방(포털)은 별건으로 진행 중이다.
 */
export const SATONG_CONTENT_Z = {
  /** 지도와 화면을 공유하는 sticky 본문(ContextHeader). Tailwind 로는 `z-[600]`. */
  stickyContextHeader: 600,
  /**
   * 앱 워크스페이스 네비 드롭다운(WorkspaceNavBar 플라이아웃). Tailwind 로는 `z-[700]`.
   *
   * ★왜 본문(600)보다 위인가 — **전역 내비게이션은 본문 정보보다 항상 위**여야 한다.
   *   그리고 이 값이 없으면 실제로 뒤집힌다: 종전 드롭다운 z-50 은 ContextHeader 가 z-30
   *   이던 시절엔 이겼지만, 위 stickyContextHeader 를 600 으로 올리는 순간 **네비 메뉴가
   *   본문 카드에 가려 클릭 불가**가 된다(전역 z 스윕이 적발한 신규 역전).
   * ★모바일 네비(MobileSidebarToggle z-[100]/[101])는 앱 헤더(z-[1000]) **안**에 렌더돼
   *   컨텍스트째 1000 으로 올라가므로 이 계약이 필요 없다 — 데스크톱 네비만 헤더 밖 형제라
   *   맨몸으로 경쟁한다. 이 비대칭이 결함의 서식지였다.
   */
  appNavFlyout: 700,
  /**
   * 본문 입력에 붙는 드롭다운(주소 후보 리스트박스 등). Tailwind 로는 `z-[650]`.
   *
   * ★왜 sticky 본문(600)보다 위인가 — **입력에 붙는 팝오버는 그 입력이 속한 본문보다 위**여야
   *   한다. 아래로 펼쳐지는 목록이 sticky 헤더 밑으로 흘러 들어가면 상단 항목이 클릭 불가가 된다.
   * ★★이 칸은 **`stickyContextHeader` 를 600 으로 올린 것의 짝 결함**에서 나왔다(라이브 실측):
   *   `/ko` 에서 주소 후보 목록을 연 채 240px 스크롤하면 ContextHeader(600)가 목록(z-30)을
   *   **127px 덮어** 상단 후보 2~3행이 사라졌다. 종전엔 둘 다 z-30 이라 DOM 순서로 목록이
   *   이겼는데, 한쪽만 600 으로 올리며 서열이 뒤집힌 것이다.
   *   ★전역 값을 올리면 **그 값과 경쟁하던 모든 것을 다시 봐야 한다** — 이번엔 네비 플라이아웃
   *   (700 칸으로 해결)에 이어 **두 번째** 짝 결함이다.
   * ★네비 플라이아웃(700)보다 아래인 이유 — 전역 내비는 본문 팝오버보다 항상 위다.
   */
  contentPopover: 650,
  /**
   * 지도와 화면을 공유하는 모달(백드롭 `fixed inset-0`). Tailwind 로는 `z-[800]`.
   *
   * ★왜 필요한가 — 지도 공존 화면의 모달이 **지도 오버레이·본문 sticky 아래로 깔려 있었다**:
   *   온보딩 위저드 z-50(DashboardHome — **신규 사용자 첫 화면**) · DeskAppraisalModal z-50 ·
   *   LandShareModal z-100(LandScheduleClient·GlobalAddressSearch) · 경매 상세 z-50.
   *   모달을 열었는데 지도 레일·팝오버(380~500)와 ContextHeader(600)가 백드롭을 관통했다.
   * ★네비 플라이아웃(700)보다 위인 이유 — 모달이 열린 동안에는 전역 내비보다 모달이 우선이다
   *   (키보드로 두 개가 동시에 열릴 수 있는 경로가 실재한다: 모달에 포커스 트랩이 없어
   *   Shift+Tab 으로 네비 버튼에 도달하고 onFocus 가 플라이아웃을 연다).
   * ★앱 크롬(헤더 z-1000)은 그대로 모달 위에 남긴다 — 현행 동작이고, 바꾸면 별개 회귀다.
   * ★적용 범위는 **지도와 공존하는 모달**로 한정한다. 저장소 전역 모달 z 는 50/60/70/100/120/1000
   *   여섯 등급으로 흩어져 있고 일괄 조정은 회귀 위험이 커서 여기서 다루지 않는다.
   */
  appModal: 800,
  /**
   * 전체화면 뷰포트 오버레이(지도 풀스크린 CSS 폴백·CAD/BIM 전체화면). Tailwind 로는 `z-[9990]`.
   *
   * ★유일하게 **앱 크롬(헤더 z-1000)보다 위**인 칸이다 — "전체화면"이 전역 크롬에 가리면
   *   그 이름이 거짓이 된다. 지도 풀스크린(useMapFullscreen 의 CSS 폴백)이 이미 9990 을 쓰고
   *   있어 값을 맞춘다.
   * ★주의: 이 값을 쓰려면 조상에 **스태킹 컨텍스트가 없어야** 한다. CAD 전체화면이 실제로
   *   DesignWorkspace 의 `relative z-10` 에 갇혀 실효 10 이었고(=헤더·FAB 아래), z 를 올려도
   *   소용없는 상태였다 — 그래서 그 컨텍스트를 함께 걷어냈다.
   */
  appFullscreen: 9990,
} as const;

export const SATONG_UI_Z = {
  /** 좌상단 활성 레이어 배지행(`SatongMapShell`). 종전 `z-[380]` 하드코딩. */
  badgeRow: 380,
  fullscreenButton: 400,
  cornerDock: 410, // 좌하단 코너 도크(상태 칩 + 노후도 범례)
  /**
   * 우상단 레이어 레일(`SatongMapShell`). 종전 `z-[420]` 하드코딩 = **`tileFailure` 와 동률**.
   *
   * ★2026-08-17 — 동률을 없애고 **순서를 선언한다.** 종전에도 화면 결과는 옳았다:
   *   타일실패 스크림이 레일 **위**로 그려졌고, 그건 `SATONG_POPUP_YIELD` 가 선언한 분류
   *   (스크림 = **불양보**(오류 고지) · 레일 = **양보**(상시 크롬))와 일치한다.
   *   문제는 그 옳은 순서가 **DOM 순서에서 우연히** 나왔다는 것이다 — 레일이 셸에서
   *   `<SatongMultiMap>` 보다 **앞**에 있고 z 가 같아서 나중 것이 이겼을 뿐이다.
   *   ★셸의 JSX 순서를 바꾸는 무해해 보이는 리팩토링 하나로 **조용히 뒤집힌다.**
   *
   *   라이브 실측(대조군 포함): 레일 rect 128×402 가 스크림 `inset-0` 영역과 겹치고,
   *   동률에서 스크림이 이겼다(양성대조 z=421 스크림 승 · 음성대조 z=419 레일 승).
   *   지도 래퍼는 `relative`(z 없음)라 **스택 컨텍스트를 만들지 않는다** — 둘은 같은 층에서 경쟁한다.
   *
   * → 415 로 내려 **`tileFailure`(420) 보다 아래임을 값으로 선언**한다. `cornerDock`(410)
   *   보다는 위라 화면 결과는 종전과 같다(동작 보존 · 취약성만 제거).
   * ★한계: 스크림은 `pointer-events:none` 이라 이 겹침은 **시각 가림**이고 클릭은 통과한다.
   *   그래서 `elementFromPoint` 판정(§회귀망 D.18)은 이 결함에 **눈이 먼다** — 위 실측은
   *   같은 부모·같은 z 에 pointer-events 를 켠 **대리 스크림**으로 쌓임만 갈라 낸 것이다.
   */
  layerRail: 415,
  tileFailure: 420,
  /** 레일 팝오버 3종(베이스맵·레이어설정·범례). 종전 `z-[430]` 하드코딩. **불양보**(사용자가 연 것). */
  railPopover: 430,
  bottomBar: 460, // 선택 현황·완료/전체취소 바(풀스크린 오버레이 모드)
  clickMenu: 470, // 지도 클릭 팝오버(단일 팝오버) + 거리재기 상태 칩 — 확인 카드 아래
  confirmCard: 500,
} as const;

/**
 * ★상세정보팝업 **양보 계약** (2026-08-17 — 사용자 신고 "팝업이 다른 정보에 가려진다").
 *
 * ## 왜 z 로 못 고치나 — 실측으로 확정된 제약
 *
 * 상세팝업은 Leaflet `bindPopup`(**9곳**)이라 `.leaflet-container` **안**에 산다. 그 컨테이너는
 * `globals.css` 에서 `isolation:isolate; z-index:0` 이다(라이브 실측 `isolate/0`).
 * → **컨테이너 안에서 아무리 올려도** 바깥 형제(오버레이 380~500)와의 서열은 **컨테이너의 0**
 *   으로 결정된다. 실제로 프로덕션 `.leaflet-popup-pane` 의 계산된 z 는 **1** 이다
 *   (`.leaflet-pane { z-index:1 !important }` 가 pane 서열을 통째로 평탄화한다).
 *
 * 그래서 아래 셋 중 **①은 원리적으로 불가능**하다 — 처음 세운 처방이었고 폐기했다:
 *   ① ~~popupPane 을 !important 예외로 승격~~ → 격리 안이라 바깥과의 서열이 안 바뀐다
 *   ② 컨테이너 자체를 승격 → **타일까지** 올라가 레일 위를 덮는다(부작용이 더 크다)
 *   ③ **팝업이 열리면 수동적 크롬이 양보한다** ← 채택
 *
 * ## 무엇이 양보하고 무엇이 안 하나 — 기준은 "누가 열었나"
 *
 * **양보(passive)**: 사용자가 열지 않았는데 상시 떠 있는 것. 상세를 읽는 동안 물러난다.
 * **불양보(active)**: 사용자가 직접 연 것 · 결정을 기다리는 것 · 오류 고지.
 *   양보시키면 사용자의 진행을 막거나 장애를 숨기게 된다.
 *
 * ## 사용법
 *
 * 지도 래퍼에 `data-satong-popup-open="true"` 가 붙고, 양보 대상 오버레이는
 * `data-satong-chrome="passive"` 를 단다. 실제 감쇄는 `globals.css` 의 한 규칙이 한다 —
 * **컴포넌트마다 조건부 클래스를 흩뿌리지 않는다**(그 방식이 x/y 겹침을 3회 재발시킨 원인이다).
 *
 * ★한계(정직): 이것은 **가림을 없애는 게 아니라 줄이는** 처방이다. 구조적 정답은 팝업을
 *   React 오버레이로 **포탈**해 격리 밖으로 꺼내는 것이고(그러면 SATONG_UI_Z 사다리에
 *   정식으로 편입된다), `bindPopup` 9곳 교체가 필요해 후속으로 남긴다.
 */
export const SATONG_POPUP_YIELD = {
  /** 지도 래퍼에 붙는 속성 — Leaflet `popupopen`/`popupclose` 로 토글한다. */
  wrapperAttr: "data-satong-popup-open",
  /** 양보 대상 표시 — 이 속성이 있는 오버레이만 감쇄된다. */
  passiveAttr: "data-satong-chrome",
  passiveValue: "passive",
  /** 감쇄 강도(globals.css 와 같은 값이어야 한다 — 테스트가 이 일치를 잠근다). */
  dimOpacity: 0.25,
} as const;
