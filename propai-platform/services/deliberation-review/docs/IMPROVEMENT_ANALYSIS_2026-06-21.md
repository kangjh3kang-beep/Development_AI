# 진행 중 도출·확인된 개선방안·추가아이디어·기능제안 분석 (2026-06-21)

> 이번 세션(36 커밋 + 9.5 게이트 + 법규 전수조사 + 설계/구현 + 투자조사)에서 **실제로 확인된** 개선점·아이디어·제안을
> 출처(근거)와 함께 분류·우선순위화. 추측 아닌 산출물 기반. 관련: SESSION_PROGRESS·DEPLOYMENT_HANDOFF·LEGAL_REFS_AUDIT.

## A. 9.5 적대 게이트가 확인한 결함 → 재발방지 개선 패턴 (이미 해소, 표준화 가치)

게이트가 배포 전 잡은 실버그 각각이 재사용 가능한 개선 패턴을 드러냄:

| # | 확인된 결함(증분) | 일반화 개선 패턴(표준 후보) |
|---|---|---|
| 1 | 정량 단위 불일치 m² vs %(PD2) | 비교 전 단위 정규화(비율 환산), 불가 시 HELD |
| 2 | 미상 센티넬 누수 — HELD→'부합'/'보통' 세탁(PD2·P2A) | 미상(HELD)은 전 레이어에서 미상 전파(약긍정 세탁 금지) |
| 3 | 음수/비정상 입력 통과(PD2) | 경계(음수·0·None) 가드 후 HELD |
| 4 | 테스트가 실 데이터 형상 미사용(PD2 정성·P2A) | 회귀는 run_analysis 실파이프라인 형상으로 검증 |
| 5 | 마이그레이션-메타데이터 드리프트(PD3) | 신규 모델 __init__ 등록 + 명시 Index명, autogenerate 드리프트 0 검증 |
| 6 | 비대칭 격리 누수 — 공용테이블 한쪽만 필터(DL3) | 분리는 store 계층 SSOT(spec_id)로 양방향 |
| 7 | 근거 해소 오매칭 — 시행규칙→부모법·합성명(LAW2)·슬롯 혼선(SLOT1) | prefix 앵커·최장일치, 법령 슬롯엔 법령만, 미해소 None(날조 금지) |

→ **제안**: 7개를 9.5 게이트 렌즈 체크리스트 + 코드리뷰 표준으로 명문화. #5(autogenerate 드리프트)·#1(단위정합)·#7(static_scan)은 CI 자동 게이트화 가능.

## B. 이월된 개선 항목 (in-scope·확인됨·미완)

1. **orphan 조문 §84(둘 이상 용도지역 안분)·§36(용도지역 지정)** 배선 — 다필지 안분 로직 도입 시 인용 연결.
2. **정밀 조문화** — 추가 법령 법령수준→정밀 조문·요지(law.go.kr 라이브 검증, 키·승인).
3. **결과예측 per-stage 확대 + pluggable ML** — 현재 터미널 단계 휴리스틱만.
4. **비동기 경로(analyze_async) tenant/project 미전파** — PD project_id 게이트가 확인한 기존 일관 한계(비동기 분석도 귀속·격리 필요).
5. **매스 캐파 proposed_gfa 1급 입력화** — 현재 provided dict 경유 동작(라우트 명시 필드 권장).
6. **자치법규(elis) 실파싱·동적 소싱** — 현재 인터페이스만(LIVE_NETWORK·운영 준비 시 점등).

## C. 신규 기능 제안 (도출됨)

1. **사업성·금융 연동(P1)** — 검증된 capacity(GFA/세대수) → 공사비 → 분양가 → ROI/NPV. ★플랫폼 사업성(§D-1)과 SSOT 연결 시 사용자 핵심 가치.
2. **생성형 설계 본격** — 매스/세대수/배치 생성(design_gen 트랙 통합; 엔진은 MASS1 검증형으로 비중복 유지).
3. **인허가 로드맵 시각화 + 심의 대응 패키지** — PermitProcessResult를 위원회 제출용 체크리스트/PDF로.
4. **결과예측 ML** — 승인 이력 학습 → 확률·예상 보완사이클(현재 휴리스틱 등급의 pluggable 후속).
5. **정북일조 envelope 캐파 통합** — design_gen solar_envelope 재사용으로 일조 제약 반영 캐파.
6. **버전드 동적 SSOT 운영** — 법령/조례 드리프트 자동 재분석(reconcile_mirror 가동, elis 라이브).

## D. 아키텍처/타트랙 이슈 (확인됨·결정 필요)

1. **★플랫폼 사업성 dataflow SSOT 단선 (task #11, 최우선 미해결)**: 공시지가 0·세대수 40 vs 323·ROI−100%·NPV 쓰레기값. 6개 근본원인 진단됨(이중 store·매출 파라미터 미시드·세대수 3중 단선·공시지가 환류 비대칭·한도 2갈래·빌더블 읽기전용). Q1(현실최고층 ceil)만 해소, **Q2~R4 미완** — 사용자 최초 지적의 핵심.
2. **dual Project schema** — app/models(organization_id) vs database/models(tenant_id·pnu_codes); pipeline write 폴백 silent no-persist 위험.
3. **design_gen ↔ deliberation 통합 경계** — capacity 생성(design_gen) vs 검증(deliberation) 정합·데이터 흐름 정의.
4. **design_gen 전용률 0.75 하드코딩(INV-11 의심)** — design_gen 트랙 매개변수화.
5. **cost_tool 플랫폼 의존** — 엔진 venv 임포트 불가(패키징/의존 경계 정리).

## E. 프로세스/방법론 인사이트 (확인됨)

1. **9.5 적대 게이트 ROI 입증** — 무날조·차원·격리·드리프트 실버그를 배포 전 차단. 수렴 루프(최대 3회)로 근본수정 유도.
2. **verify-gaps 효과** — design_gen 생성형 중복 회피(MASS1을 검증형으로 전환).
3. **접근법 C(절차=데이터)** — 두 스킬(심의·설계) 코드 무중복 재사용 입증(동일 실행기).
4. **실시간 진행기록 + 자동 메모리** — 멀티세션·멀티클로드 인계 효과.

## F. 우선순위 추천 (impact × 검증가능성)

- **P0 — 사용자 비전 직결·확인된 결함**: 플랫폼 dataflow SSOT(task #11) Q2~R4(공시지가/세대수/ROI 단선·이중 store 해소). 이미 근본원인·계획 확정, 성장루프로 즉시 착수 가능.
- **P1 — 가치·비중복·검증가능**: 사업성 연동(capacity→cost→ROI), 비동기 경로 귀속(#B-4), §84 안분(#B-1).
- **P2 — 운영/데이터**: elis 라이브·정밀 조문화(#B-2,6), 예측 ML(#B-3).
- **P3 — 재발방지**: 게이트 7대 패턴 표준화·일부 CI 자동화(#A).
- **타트랙(별도 세션)**: design_gen 전용률 fix·dual schema 결정(#D-2,4).
