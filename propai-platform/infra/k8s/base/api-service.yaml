# PropAI v30.0 - API ClusterIP 서비스
apiVersion: v1
kind: Service
metadata:
  name: propai-api
  namespace: propai
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: propai-api
  ports:
    - name: http
      port: 8000
      targetPort: http
