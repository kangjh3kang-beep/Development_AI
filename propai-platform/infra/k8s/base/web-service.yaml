# PropAI v30.0 - 웹 ClusterIP 서비스
apiVersion: v1
kind: Service
metadata:
  name: propai-web
  namespace: propai
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: propai-web
  ports:
    - name: http
      port: 3000
      targetPort: http
