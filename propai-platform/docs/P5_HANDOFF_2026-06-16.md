# P5 인계 회신 — 데이터품질 반복루프 (검증·원장 read 경로)

발신: feature/trust-infra-2026-06-11 세션 / 수신: 배포 코디네이터 세션
일자: 2026-06-16 · 브랜치 `feature/trust-infra-2026-06-11` · **HEAD a1592f1, origin 푸시 완료, 미배포(머지·배포는 배포 코디)**

---

## 0. 요약
P5의 **CRITICAL 2건 구현·푸시 완료**(실DB 검증, skipped==0). HIGH 2건은 **실측 결과 일부 오판**이라 범위를 정정해 보고합니다. 머지·prod 배포·마이그레이션은 규약대로 배포 코디께 인계합니다.

## 1. ⚠️ 갭 실측 보정 (구현 전 현재 feature 코드로 재검증 — 중복구현 방지)
| 감사 항목 | 실측(file:line) | 조치 |
|---|---|---|
| #2 [CRIT] 원장 read 소비 0건 | **부분 오판** — `load_prior`/`get_latest` 소비자 다수: comprehensive_analysis_service.py:151 · agents/specialist_agent.py:64 · ledger_adapters.py:144(`_append_with_lineage`). 변경감지=`ledger/contradiction.py`(Phase 2) 이미 존재 | read 루프 자체는 **Phase 1/2가 이미 폐쇄**. P5는 그 위에 **staleness+재분석 제안**만 신규(아래 2-A) |
| #4 [HIGH] Freshness/Anomaly/CalcMeta/PublicDataRegistry 미연결 | **부분 오판** — precheck_service · auction_service · tax_ai_service · feasibility/revenue_engine · routers/data_integrity 에서 **이미 연결됨** | 미적용 경로(pipeline)만 가드 추가 대상(아래 4) |
| #1 [CRIT] pipeline 검증 0건 | **OPEN 확정** — `app/services/pipeline/`에 verify 호출 0건 | 구현(2-B) |
| #3 [HIGH] cross_validate 2곳만 | **OPEN 확정** — avm_service.py:146 · sales/pricing/suggest.py:169 뿐 | 범위 정정(아래 4) |

> 참고 메모리 규칙: 감사 §3가 EXISTS를 MISSING으로 표기하는 경우가 있어 file:line 선검증 후 구현했습니다.

## 2. 구현 완료 (CRITICAL 2건)

### 2-A. [#2] 원장 staleness/재분석 제안 — 분석루프 폐쇄 (`5ac0fdf`)
- 신규 `app/services/ledger/staleness.py`:
  - `check_staleness(*, analysis_type, tenant_id, pnu, address, project_id, current=None, max_age_days=90)` — 동일 체인 최신 분석의 **age(DB `now()-created_at`)** + (current 제공 시) **변경감지**(Phase 2 `detect_contradictions`) → `{recommend_reanalysis, stale, changed, age_days, max_severity, reasons}`. prior 없으면 `reason="no_prior"`(정직).
  - `staleness_report(...)` — **`get_history` 소비**(버전 타임라인 요약, 재분석 추세).
  - `_recommend(...)` — 순수 임계 로직(무 DB 단위테스트).
- 원장·산출 수치 **불변**(read·표면화 전용).
- 테스트 `tests/ledger/test_staleness.py` — 실DB **4 passed, skipped==0**.

### 2-B. [#1] 파이프라인 단계별 검증 강제 (`a1592f1`)
- `app/services/pipeline/project_pipeline.py` `run()` 루프 각 단계 **COMPLETED 직후 `_verify_stage` 호출** → `VerifierService.verify(stage, source, output=stage.data)` 결과를 `stage.data["verification"]`에 **additive 부착**.
  - LLM 부재 시 **규칙기반 verdict**(_prescan + calc_ledger + range_rules) — graceful.
  - source = 단계간 payload(site_to_design/design_to_cost/cost_to_feasibility) 우선, 없으면 자기일관성.
  - **COMPLETED 단계만** 검증(skip/실패 단계 제외 — 정직), 결정론 산출 수치 불변.
- 테스트 `tests/pipeline/test_pipeline_verification.py` — 3 passed + **full_pipeline 회귀 13 passed**.

## 3. 검증 (라이브검증 가능)
```bash
cd apps/api
export DATABASE_URL='postgresql+asyncpg://propai_user:propai_pass_dev@localhost:5432/propai_db'
export INTERP_REDIS_CACHE=0
.venv/bin/python -m pytest tests/ledger tests/pipeline tests/agents -q -rs   # 64 passed, skipped==0
.venv/bin/python -m pytest tests/integration/test_full_pipeline.py -q -rs    # 13 passed (회귀)
```
- **라이브검증 엔드포인트**: 원장 read/무결성은 기존 `routers/analysis_ledger.py`(GET 조회·`/verify`). staleness/단계검증은 현재 **서비스 레벨**(staleness=`ledger/staleness.py`, 검증블록=파이프라인 응답 `stages[*].data.verification`). 라우터 노출이 필요하면 알려주시면 additive로 추가합니다.
- 변경 파일(머지 대상): `app/services/ledger/staleness.py`(신규) · `app/services/pipeline/project_pipeline.py`(run 루프 + `_verify_stage`/`_verify_source_for` 추가) · 테스트 2종 + plan(`docs/superpowers/plans/2026-06-16-p5-pipeline-verify-staleness.md`).

## 4. 미완 — 범위 정정·후속 제안 (HIGH)
- **[#3] cross_validate 확대**: 현재 avm·분양가만. site_analysis/feasibility/cost **공공데이터 입력**에 가드 필요. 단 그 호출부는 land_intelligence/feasibility 서비스(제 소유 외)라, 충돌 방지 위해 **pipeline 레벨(`project_pipeline.py`, 제 소유)에서 trust_guard로 additive 부착**하는 설계를 plan T3에 명시했습니다. 미구현(후속).
- **[#4] Freshness/Anomaly 가드**: 정의·타 경로 연결됨. pipeline 경로에만 미적용 → T3와 동반.
- 두 건 모두 plan에 무-플레이스홀더로 기재 — 승인 시 동일 패턴(TDD·skipped==0)으로 이어 구현 가능합니다.

## 5. 규약 준수
additive·하위호환(원장 스키마·8엔진·StageResult 계약 불변) · 결정론 코어/수치 불변(검증·staleness=read·표면화 전용) · LLM 비수치(citation_gate/규칙기반) · 정직표기(verdict/issues/reason/skipped) · **feature 브랜치 푸시까지만**(머지·prod 배포·마이그레이션 적용 = 배포 코디). 충돌 방지: 변경은 전부 `ledger/**`·`pipeline/**` 내.
