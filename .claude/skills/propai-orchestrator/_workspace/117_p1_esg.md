# 117 · P1 ESG 단계 목업 제거 → 실엔진 연결

## 범위
- 프론트: `app/[locale]/(dashboard)/projects/[id]/esg/page.tsx`, `components/esg/GresbScoreCard.tsx`
- 백엔드: `app/services/esg/lca_service.py`, `app/routers/esg.py`(LCA 인자 패스스루)

## 1. 하드코딩 목업 제거 (page.tsx)
- **LCA 카드 4개(2,450/3,120/-670/1+++) + "AI Insight 82%" 전면 삭제.** 해당 motion.div를 `<GresbScoreCard />` 렌더로 교체.
  - 실 LCA 데이터(전생애 단계·체화/운영 탄소·자재별·AI 해석)는 이미 하단 `ProjectEsgWorkspaceClient`(/esg/lca/calculate)가 표시하므로 중복 목업만 제거.
- **ESG Risk Matrix 합성 S/G 제거.** 기존 `S=E*0.85, G=E*1.05, overall` 파생 가짜점수 삭제.
  - E(환경)만 실 LCA 탄소 원단위(`esgData.totalCarbonPerSqm`)에서 산출(데이터 없으면 "—" 정직 표기).
  - S/G는 정량 데이터 없음 → 점수 미표기 + "LCA(E) 중심, S/G는 GRESB 스코어링에서 정량 산출" 안내(가짜 정밀점수 금지).

## 2. GRESB 백엔드 연결 + 페이지 렌더 (고아 해소)
- `GresbScoreCard.tsx` **프론트 자체 산식(195-231) 전부 제거** → `apiClient.post("/gresb/score")` 호출로 백엔드 산식 일원화.
  - 요청 스키마 = `GresbScoreRequest`(routers/gresb.py) 정합: building_type/energy_kwh_per_sqm/ghg/water/has_esg_policy/has_green_cert/green_cert_level/waste_recycling_pct/renewable_energy_pct/lca_total_carbon_kg/floor_area_sqm. 빈 입력은 null로 전송(Optional).
  - 입력 자동시드: store `esgData`(ghg=totalCarbonPerSqm, lca_total_carbon_kg=embodiedCarbonKg) + `designData`(floor_area=totalGfaSqm, building_type) — 기존 `loadFromEsg` 유지.
  - 응답 컴포넌트 max를 백엔드 값 사용(mgmt30/perf50/dev20). 기존 하드코딩 perf60/dev10 불일치 제거.
  - 인증/게이트: `canUseLiveApi`(mode=live or hasAccessToken)로 버튼 비활성+안내, 401/403·오류 메시지 표기. 무자료 정직.
  - `benchmark_meta`(version/source) 결과에 표기.
- `page.tsx`에 `import GresbScoreCard` + 우측 컬럼 렌더(grep 0건 → 페이지 마운트).

## 3. B6 운영에너지 디커플링 (lca_service.py)
- `calculate_total_lca(..., building_type, energy_intensity_kwh_per_sqm=None)` 신설 인자.
  - **고정 120 제거.** 입력 원단위 있으면 그 값을 B6에 반영(출처="입력값(BEEC 1차에너지 원단위)").
  - 없으면 `_beec_intensity_for(building_type)`가 **energy_service.ENERGY_INTENSITY_BY_TYPE의 BEEC 3등급 표준원단위**를 출처와 함께 사용(graceful fallback 120).
- `calculate_b6_operational_energy`에 `intensity_source`/`energy_intensity_kwh_per_sqm` 응답 필드 추가(출처 명시).
- `esg.py` LCARequest에 `building_type`/`energy_intensity_kwh_per_sqm` 추가 → 서비스로 패스스루.

## 라이브 검증 (venv 서비스레벨)
- `LCAService.calculate_total_lca`:
  - default(apartment): B6 intensity **210** kWh/㎡ ← energy_service(BEEC 3등급). 출처 "BEEC 3등급 표준원단위(apartment, energy_service)". → **고정 120 단절 해소 확인.**
  - input(office, 85): B6 intensity **85**, 출처 "입력값". per_sqm 5059→2071로 변동(B6가 에너지 입력에 반응).
- `GresbScoringService.calculate_score`: total=68/grade=B, benchmark_meta={version:2025.1, source:Internal baseline...} 정상.
- 프론트 `npx tsc --noEmit` **EXIT 0**(에러 0).
- 백엔드 `py_compile` 4파일 OK.
- HTTP 라이브(/gresb/score, /esg/lca/calculate): 로컬 API 미기동·배포금지로 **스킵**(서비스레벨로 대체 검증). 컨테이너 기동 시 인증 헤더 필요(get_current_user 게이트).

## 규율 준수
- import 트랩: git diff 확인 — 추가만(`apiClient`/`GresbScoreCard`), 삭제 없음.
- 신규 의존성 0. push/배포 안 함. git add 미실행(명시경로 한정 예정).
- 디버그/TODO/console 잔존 0.

## 미진 / 후속
- S/G ESG 정량 평가 엔진 부재(거버넌스 문서·커뮤니티 영향 데이터 소스 없음) → 현재는 GRESB Management/Development 항목이 G 일부 대체. 별도 S/G 데이터 파이프라인 필요.
- LCA `energy_intensity_kwh_per_sqm`를 프론트 LCA 폼에서 자동 전달하는 배선은 미적용(현재 백엔드 기본=BEEC 표준원단위). 향후 ProjectEsgWorkspaceClient에서 energy 분석결과를 LCA 요청에 시드하면 완전 연동.
- HTTP 엔드투엔드 라이브검증은 API 컨테이너 기동 환경에서 1회 필요.
